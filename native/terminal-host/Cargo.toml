[package]
name = "addone-terminal-host"
version = "0.1.0-dev"
edition = "2024"
rust-version = "1.85"
license = "MIT"
publish = false

[dependencies]
crossterm = "=0.29.0"
portable-pty = "=0.9.0"

[profile.dev]
opt-level = 1

[profile.release]
lto = "thin"
codegen-units = 1
