## 1. Establish exposure and protected-data baselines

- [ ] 1.1 Inventory owned identifier forms and all environment definitions/uses across source, entry points, native code, scripts, tests, configuration, and workflows; verify coverage of the current identity registry and keys declared outside it.
- [ ] 1.2 Classify each key's one supported spelling, owner, producers/consumers, exposure evidence, and value semantics, distinguishing protocol markers and proven-unused definitions; verify no private legacy-alias category or wildcard exception remains and unresolved external interfaces have exact review entries.
- [ ] 1.3 Capture public-setting, path, user-data-format, and protected settings/session/history expectations; verify the delivered baseline distinguishes disposable runtime/release paths from user data without requiring old executable fixtures.

## 2. Strengthen syntax-aware naming governance

- [ ] 2.1 Extend TypeScript/JavaScript inspection to decoded case-insensitive `a1` matches across bindings, parameters, aliases, classes/types/enums, private identifiers, accessors, and quoted or statically known computed members; verify positive and negative fixtures for each supported source extension.
- [ ] 2.2 Replace blanket uppercase-property exemptions with exact supported external-contract and boundary classification; verify arbitrary `A1_*` properties and type members fail while declared public/serialized boundary uses pass.
- [ ] 2.3 Add token-aware Rust inspection for relevant identifier forms without misreading raw strings, lifetimes, character literals, raw identifiers, nested comments, or macro tokens; verify valid and unsupported-syntax fixtures produce findings or explicit failures as appropriate.
- [ ] 2.4 Inspect string/constant environment definitions, registry references, owned JSON/workflow maps, and supported shell assignments; verify branded private settings and legacy fallback definitions fail even when surrounding names are neutral, while explicit rejection-test data is not treated as active configuration.
- [ ] 2.5 Define tracked-source ownership and explicit dependency/vendor/generated exclusions, including owned source ports and test helpers; verify only declared non-owned content is excluded and uninspectable owned inputs fail.
- [ ] 2.6 Refactor internal-name violations and reconcile name evidence without grandfathering them; verify internal-name findings reach zero while supported external values and intentional source snippets remain intact.

## 3. Validate new and changed PR code through the required gate

- [ ] 3.1 Extend authoritative validation impact with naming inputs and changed/full mode, preserving base/head identity and rename/copy metadata; verify selection for additions, modifications, copies, deletions, type changes, and renames into/out of scope.
- [ ] 3.2 Implement complete-file changed-mode inspection and fail-closed PR base/head resolution; verify untouched-line violations are found, unusual path characters work, and missing refs or unreadable/unsupported inputs cannot yield empty success.
- [ ] 3.3 Escalate policy, external exceptions, environment definitions, ownership, parser dependencies, and check-integration changes to a full audit and checker regressions; verify a policy-only PR detects violations in otherwise unchanged files and cannot introduce private legacy exemptions.
- [ ] 3.4 Emit naming evidence with revisions, mode, inspected inputs, exclusions, escalation reasons, diagnostics, and timing; verify the result schema and actionable path/line/name/rule output.
- [ ] 3.5 Wire naming outcomes into current-head required development validation; verify aggregate tests reject absent, stale, failed, and unexpectedly skipped outcomes while accepting explicit documentation-only not-applicable selection.
- [ ] 3.6 Reconcile architecture and inventory-test ownership so broad validation retains a full audit without duplicating it on ordinary changed-mode PRs; verify all other gates remain and documentation-only changes stay product-build/test-free.

## 4. Implement one neutral private contract

- [ ] 4.1 Separate public identity definitions/validation from private runtime/test/automation definitions; verify neutral private keys load successfully while public spellings, uniqueness, former-product rejection, and path expectations remain enforced.
- [ ] 4.2 Implement the dependency-light current-only launch-context reader/writer using the `LAUNCH_CONTEXT_*` family; verify coherent current input succeeds, required missing/invalid fields fail, and obsolete-only input never supplies current values or triggers default-release fallback.
- [ ] 4.3 Reconstruct owned outgoing context from verified target selection and current intent, handling platform casing and bounded diagnostics; verify stale current values are replaced, conflicting case-equivalent private keys fail, and obsolete ambient values do not affect current context or public settings.
- [ ] 4.4 Establish exact current-contract eligibility from verified target metadata before execution, activation, retained launch, rollback, or recovery; verify missing/different identities reject the target without alternate encodings, version negotiation, payload rewriting, or state deletion.

## 5. Replace complete private producer/consumer chains

- [ ] 5.1 Replace private lookups and definitions in bootstrap, supervisor, guardian/UI entry points, immutable-root verification, warmup, and applicable startup consumers; verify current-contract profile/session selection, guarding, import-only warmup, and negative verification tests without legacy reads/writes.
- [ ] 5.2 Replace confirmed private cleanup-worker, native, development, CI, and test-worker keys with all their current producers/consumers, retaining reviewed public/integration spellings and external protocol markers; verify cleanup holds/continuation, worker behavior, native probes, and workflow consistency.
- [ ] 5.3 Audit owned context propagation and runtime/recovery selection to remove obsolete private execution routes; verify nested current-contract launches preserve intended public/integration settings and unsupported retained/recovery state produces an actionable diagnostic rather than automatic fallback or deletion.

## 6. Prove current-contract behavior and cutover safety

- [ ] 6.1 Exercise installed normal/comparison launch, warmup, and update activation using independently materialized current-contract releases; verify required context, readiness, and immutable-root/digest/ownership/containment checks remain effective.
- [ ] 6.2 Exercise an update with a working current-contract retained session, subsequent launch, verified current-contract rollback, and interrupted replacement/recovery; verify session continuity, target selection, and callable launchers under the existing lifecycle rules.
- [ ] 6.3 Test obsolete-only private inputs, unsupported retained/recovery targets, stale references, and absence of an eligible rollback target; verify no legacy translation, negotiation, migration helper, obsolete execution, or false success is used.
- [ ] 6.4 Verify unchanged public overrides, platform paths, `.a1` profiles, user-data formats, and current history-storage behavior with protected settings/session/history sentinels; verify disposable-state reset instructions exclude user data and require separate confirmation, retaining existing startup/update budgets.

## 7. Acceptance and cutover handoff

- [ ] 7.1 Document the public/private inventory, PR check, and one-time clean cutover with a version-specific direct npm install command, stopped-process prerequisite, and exact disposable reset paths only if needed; verify no instruction relies on the old `a1 update` path or authorizes blanket data-root deletion.
- [ ] 7.2 Obtain successful current-head required CI for naming enforcement, selection/aggregate regressions, current-contract packaged lifecycle, unsupported-target rejection, and protected-data assertions on supported platforms; verify the full audit is clean and existing budgets remain unchanged.
- [ ] 7.3 Record maintainer acceptance of the deliberate cutover and current-contract launch, comparison mode, public overrides, update/session continuity, rollback, and recovery; verify the accepted build is identified and no legacy private support or unapproved reset is included.
