## 1. Establish exposure and compatibility baselines

- [ ] 1.1 Inventory owned identifier forms and all environment definitions/uses across source, entry points, native code, scripts, tests, configuration, and workflows; verify the delivered inventory accounts for the current identity registry and keys declared outside it.
- [ ] 1.2 Classify each environment entry with exact spelling, owner, producers/consumers, exposure evidence, and compatibility obligations; verify public settings remain public, uncertain existing interfaces have exact review entries, and no wildcard exception is introduced.
- [ ] 1.3 Distinguish protocol markers and proven-unused definitions from active environment keys; verify each removal or retained spelling has an explicit inventory disposition rather than an automatic replacement.
- [ ] 1.4 Capture independently traceable pre-transition producer/consumer fixtures and public-setting/path/serialized-format expectations; verify fixture provenance and assertions do not derive legacy behavior from the new implementation.

## 2. Strengthen syntax-aware naming governance

- [ ] 2.1 Extend TypeScript/JavaScript inspection to decoded case-insensitive `a1` matches across bindings, parameters, aliases, classes/types/enums, private identifiers, accessors, and quoted or statically known computed members; verify positive and negative fixtures for each supported source extension.
- [ ] 2.2 Replace blanket uppercase-property exemptions with exact external-contract and boundary classification; verify arbitrary `A1_*` properties and type members fail while declared public/serialized boundary uses pass.
- [ ] 2.3 Add token-aware Rust inspection for relevant identifier forms without misreading raw strings, lifetimes, character literals, raw identifiers, nested comments, or macro tokens; verify valid and unsupported-syntax fixtures produce the expected findings or failures.
- [ ] 2.4 Add environment-contract inspection for string/constant key definitions, registry references, owned JSON/workflow maps, and supported shell assignments; verify unclassified branded private keys cannot pass merely because local bindings are neutral.
- [ ] 2.5 Define tracked-source ownership and explicit dependency/vendor/generated exclusions, including owned source ports and test helpers; verify fixture tests exclude only declared non-owned content and fail on uninspectable owned inputs.
- [ ] 2.6 Refactor real internal-name violations uncovered by the full audit and reconcile governance evidence without grandfathering them; verify forbidden owned-name findings reach zero while intentional test-source snippets and external identity data remain intact.

## 3. Validate new and changed PR code through the required gate

- [ ] 3.1 Extend authoritative validation impact with naming inputs and changed/full mode, preserving base/head identity and complete rename/copy metadata; verify selection tests for additions, modifications, copies, deletions, type changes, and renames into/out of scope.
- [ ] 3.2 Implement complete-file changed-mode inspection and fail-closed PR base/head resolution; verify violations on untouched lines are found, unusual path characters are handled, and missing refs or unreadable/unsupported inputs cannot yield an empty success.
- [ ] 3.3 Add full-audit escalation for policy, exceptions, environment definitions, ownership, parser dependencies, and check-integration changes; verify a policy-only PR can detect a violation in an otherwise unchanged file.
- [ ] 3.4 Emit naming result evidence with revisions, mode, inspected inputs, exclusions, escalation reasons, diagnostics, and timing; verify stable result-schema and actionable path/line/name/rule output tests.
- [ ] 3.5 Wire the naming outcome into current-head required development validation; verify aggregate tests reject absent, stale, failed, and unexpectedly skipped required outcomes while accepting an explicit documentation-only not-applicable selection.
- [ ] 3.6 Reconcile architecture invocation and inventory-test ownership to retain the full audit in broad validation without duplicating it on ordinary changed-mode PRs; verify validation-plan tests preserve all other gates and documentation-only changes remain product-build/test-free.

## 4. Separate public configuration and private launch contracts

- [ ] 4.1 Separate public product identity definitions/validation from private runtime/test/automation definitions; verify module initialization accepts neutral private keys while retaining exact public spellings, uniqueness checks, former-product rejection, and public-path expectations.
- [ ] 4.2 Implement the dependency-light neutral launch-context reader and exact legacy alias mapping; verify canonical-only, legacy-only, equivalent dual-form, missing-required-value, and conflicting-form cases use consistent validation.
- [ ] 4.3 Implement coherent outgoing context reconstruction with the canonical `LAUNCH_CONTEXT_*` family and required legacy dual-write; verify stale inherited release/profile values are replaced from the verified target and public/third-party settings are preserved.
- [ ] 4.4 Handle private key casing and bounded conflict diagnostics; verify Windows duplicate-case conflicts are rejected and diagnostics identify the logical setting without exposing unrelated environment values.
- [ ] 4.5 Constrain alias and exposure-review metadata to exact owning boundaries and retirement conditions; verify new unclassified keys, broad exceptions, and legacy aliases outside their declared boundary fail governance.

## 5. Migrate complete private producer/consumer chains

- [ ] 5.1 Migrate bootstrap, supervisor, guardian/UI entry points, and immutable-root verification to the shared normalized launch contract; verify launch/profile/session-selection and negative release-root/digest/ownership tests still pass with canonical and legacy callers.
- [ ] 5.2 Migrate warmup and applicable startup context consumers without changing unresolved public diagnostics settings; verify required warmup guarding, canonical/legacy activation, and import-only warmup behavior remain intact.
- [ ] 5.3 Migrate confirmed private cleanup-worker keys and any other retained-worker handoffs with their proven lifetime requirements; verify cleanup holds, run identity, continuation, and failure recovery remain correct.
- [ ] 5.4 Migrate confirmed private native, development, CI, and test-worker keys with every producer/consumer, retaining reviewed external integration spellings and stored protocol markers; verify focused native/tooling/worker and workflow-consistency tests.
- [ ] 5.5 Audit private metadata propagation through owned and unrelated child commands and apply only necessary inventoried-key scoping; verify nested new/old launcher chains do not inherit conflicting stale canonical context and public/integration settings remain visible.

## 6. Prove cross-version runtime compatibility

- [ ] 6.1 Exercise an independently preserved old-contract updater/producer against the new packaged warmup, supervisor, and launch readers; verify successful activation and readiness without relaxing release verification.
- [ ] 6.2 Exercise a new caller against a supported old-contract retained guardian/UI/supervisor and a verified rollback target; verify required environment values, selected cohort/profile/session arguments, and rollback readiness.
- [ ] 6.3 Exercise interrupted or cancelled replacement using a preserved legacy recovery capsule/launcher; verify retry and recovery remain callable without changing the persisted capsule format or public launchers.
- [ ] 6.4 Exercise update while a retained older session is active and then launch a new session; verify the old session retains its release/input/transcript/containment while the new launch selects the installed release.
- [ ] 6.5 Verify public overrides, default platform paths, `.a1` profile access, current history-storage behavior, and stored schema/field compatibility on supported platforms; retain all existing immutable-release and startup/update budget assertions.

## 7. Acceptance and maintenance evidence

- [ ] 7.1 Deliver a documented public/private/compatibility environment inventory and describe the PR naming check and its exact exceptions; verify documentation matches the checked definitions and does not claim unresolved keys are private.
- [ ] 7.2 Obtain successful required CI evidence for naming enforcement, selection/aggregate regressions, and applicable packaged cross-version scopes on Windows, Linux, and macOS; verify results belong to the implementation head and no existing budget or assertion was weakened.
- [ ] 7.3 Record maintainer acceptance of normal launch, comparison launch, public overrides, ongoing-session update, rollback, and recovery using the implementation candidate; verify acceptance identifies the tested build and any remaining exact compatibility aliases before declaring implementation complete.
