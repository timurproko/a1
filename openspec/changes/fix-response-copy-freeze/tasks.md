## 1. Establish separate copy and paste baselines

- [ ] 1.1 Reconcile the fresh implementation base with accepted selection/input, large-text chip, image, and `keep-streaming-ui-responsive-during-selection` behavior; verify a compatibility map identifies response-copy, standalone native paste, terminal-provided paste, shared ordering/preparation boundaries, and unchanged comparison/modal behavior without modifying unrelated artifacts.
- [ ] 1.2 Add bounded payload-free operation/phase diagnostics for copy capture/delivery and paste framing, predecessor wait, read, classification/conversion, insertion/presentation, deadlines, and teardown; verify records identify operation/request/transport/result, timing and pending-byte counts without copied/pasted text, paths, images, or base64.
- [ ] 1.3 Capture generated cold/warm copy-only, standalone paste-only with external content, and combined baselines while idle/streaming in short/long sessions, plus large text/path/image controls; verify evidence separates event-loop delay, prerequisite waits, read/preparation/insertion costs, and terminal behavior without claiming a shared or physically reproduced cause prematurely.

## 2. Bound selected-text preparation and preserve input semantics

- [ ] 2.1 Capture an immutable selected-range intent at the Ctrl+C input boundary without slicing unrelated transcript rows; verify forward/reverse, Unicode, ANSI, newline, chrome/transient-tail exclusion, and concurrent reflow/streaming tests produce the original exact text.
- [ ] 2.2 Add bounded selected-source retention and incremental extraction/encoding with cancellation, UTF-8 byte accounting, and the 16 MiB limit; verify deterministic yields during large and long-single-line work, equivalent small-copy work across history sizes, bounded scratch memory, and explicit non-truncating size rejection.
- [ ] 2.3 Keep copy-key consumption and selection-clear presentation independent of delivery; verify following typing/wheel/agent events and animation progress before a stalled copy settles, no unchanged-content reformatting is introduced, and asynchronous callbacks never clear a newer selection.

## 3. Implement bounded delivery and ordering

- [ ] 3.1 Introduce the owned copy coordinator with explicit outcomes, one delivering request and one newest pending request, and a 5,000 ms admission deadline; verify controlled-scheduler tests cover pending supersession, exact deadline settlement, byte/resource bounds, and no obsolete delivery after a newer copy.
- [ ] 3.2 Implement bounded retry, cancellation, 250 ms cleanup grace, and side-effect fencing/quarantine; verify stuck startup/delivery cannot strand UI work, unsafe resources are not reused, late results cannot overwrite newer state, and safe recovery works without restart.
- [ ] 3.3 Capture transaction-specific read/write predecessors and add coalesced non-modal failure feedback; verify a paste admitted during a pending copy fails safely when that copy fails, later independent paste can acquire fresh content without an intervening successful copy, expired reads do not strand writes, and mixed prompt copy/cut, response copy, paste, and `/copy` introduce no cycles or stale barriers.

## 4. Isolate platform delivery and preserve terminal compatibility

- [ ] 4.1 Add destination-aware transport selection and a cancellable child-process clipboard boundary for supported local hosts; verify local/remote routing, native unavailability, busy/denied clipboard, helper crash/hang, bounded IPC, hidden startup, and absence of clipboard payloads in argv, temporary files, logs, or inherited terminal output.
- [ ] 4.2 Retain a bounded destination-correct OSC 52 fallback through the existing single writer, with a 64 KiB complete-sequence ceiling or lower known capability limit; verify exact base64/framing, encoded-limit boundaries, unsupported-route feedback, no duplicate concurrent native/terminal writes, and submitted-unverified outcomes rather than false success.
- [ ] 4.3 Attribute and eliminate copy-specific terminal submission blocking on supported routes; verify backpressure/fault tests permit later UI work, replay shows atomic controls outside rendering transactions, and physical-terminal evidence is required before classifying a suspect route as safe.
- [ ] 4.4 Package the isolated helper and bind all resources to session lifecycle; verify cold built/packaged resolution, startup within the request deadline, no late clipboard/terminal writes across session replacement, bounded exit without orphan growth, and complete terminal restoration.

## 5. Bound standalone paste acquisition, preparation, and insertion

- [ ] 5.1 Admit independent Ctrl+V operations with captured prerequisites, a 5,000 ms acquisition budget inside the 15,000 ms end-to-end paste deadline, and bounded request/payload accounting; verify no preceding A1 copy is needed, empty reads settle safely, eight-request capacity is explicit, and known read/write failures do not poison later independent paste attempts.
- [ ] 5.2 Isolate native clipboard format/text/image reads and platform-command fallbacks in cancellable executors with bounded transfers; verify busy/denied/missing readers, startup failure, genuinely blocked native work, deadline cancellation, 250 ms stop grace, quarantine, and automatic safe recovery without UI-thread waits or orphan growth.
- [ ] 5.3 Bound text/URL/path classification, filesystem probes, image preparation, and UI-side insertion/presentation work; verify 16 MiB text and existing image limits, exact normalized/expanded content and chip semantics, long single-line/multiline yield evidence, slow-path safe fallback, no misleading screenshot marker for text, and no partial insertion or silent truncation on rejection.
- [ ] 5.4 Preserve each admitted paste's ordered reservation, selection replacement, and undo/redo lifecycle; verify reverse completion of two distinct pastes with intervening typing, explicit capacity rejection without superseding accepted pastes, removal/undo cancellation, and no stale result overwriting later edits.
- [ ] 5.5 Route nonempty terminal-provided paste through bounded preparation without duplicate native acquisition; verify valid split delimiters, opaque control-like body text, exact-once insertion, following keyboard bytes, and unchanged modal/replacement ownership and pinned comparison behavior.
- [ ] 5.6 Package the clipboard-read/preparation executors and bind them to session lifecycle; verify cold built/packaged text and image paste, bounded aggregate raw/encoded/prepared retention, session replacement during read/conversion, released reservations, no late editor/terminal writes, bounded shutdown, and terminal restoration.

## 6. Validate the integrated clipboard contract

- [ ] 6.1 Run deterministic copy-only, independent paste-only, and combined fault/content matrices spanning cold/warm small operations, repeated requests, long text/selections/sessions, URLs/paths/images, native and terminal paste, idle/streaming, terminal delay, busy/missing/denied/non-settling read/write/preparation, recovery, and teardown; verify gates fail blocked input/timers, unbounded work, stale deliveries/insertions, incorrect payloads, dropped/reordered pastes, and privacy leaks.
- [ ] 6.2 Exercise mixed copy, independent paste, typing, wheel, new selection, undo/redo, overlay/replacement ownership, and Ctrl+C without selection; verify receipt order, transaction-specific read/write dependencies, agent cancellation ownership, follow/detach preservation, unchanged prompt content/chip semantics and comparison behavior, and installed package identity.
- [ ] 6.3 Obtain required CI results for the exact implementation candidate and retain bounded evidence; verify all required checks and helper/package/terminal conformance pass without treating timing distributions or successful copying alone as proof that both shortcuts are responsive.

## 7. Validate both shortcuts physically and record acceptance

- [ ] 7.1 Hand off the exact built candidate with the color-preserving development entry and separate selected-response Ctrl+C, external-content standalone Ctrl+V, and combined workloads; verify the handoff identifies worktree/commit, terminal/version, topology, geometry, observed key-versus-terminal paste route, known limits, and independent copy/paste content checks without proposing Esc as recovery.
- [ ] 7.2 Record physical cold/warm idle/streaming copying and independent pasting, starting with at least 100 small distinct copy cycles and 100 standalone paste cycles plus combined, large/multiline, and existing image cases; verify typing/scrolling/visible output remain responsive throughout both operations, record subsequent-input-to-paint distributions and status gaps separately from clipboard completion latency, and retain any contradictory freeze as failed acceptance.
- [ ] 7.3 Obtain explicit user acceptance of both response copying and standalone prompt pasting on the exact candidate; verify acceptance requires correct copied/pasted content and sustained UI responsiveness without recovery actions, then seek explicit implementation merge authorization before completing the accepted-change archival follow-up.
