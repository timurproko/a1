## 1. Establish compact modal chrome

- [x] 1.1 Inventory titled framed bare-A1 modal producers and classify untitled or extension-owned custom surfaces that must not be altered.
- [x] 1.2 Add a reusable modal frame component or component adapter whose top rule is followed immediately by its title and whose API cannot add a top-title spacer.
- [x] 1.3 Add focused component tests for bordered and narrow frames, styled titles, title/body spacing, and exact one-row height reduction.
- [x] 1.4 Expand the shared modal frame to own a one-cell global content inset while rendering declared frame/separator rules at full width.

## 2. Adopt the shared frame across modal families

- [x] 2.1 Route owned Models, Skills, Thinking, scoped-model, tree, session, trust, and startup modal families through the shared compact frame without per-dialog top-spacer deletion.
- [x] 2.2 Adapt nested authentication and extension select/confirm/input/editor routes at their existing bare-A1 component factories, source-synchronizing only components whose public presentation is not configurable.
- [x] 2.3 Route title, search/input, list, description, status, and shortcut content through the global inset; remove duplicated outer row padding while retaining relative content indentation.
- [x] 2.4 Preserve title styling, vertical body spacing, reduced-width search/editor and pointer geometry, full-width rules, and natural overlay centering.
- [x] 2.5 Preserve direct pinned-component construction and output for `a1 pi`; verify no installed dependency bytes or prototypes change.

## 3. Prove universal coverage and behavior

- [x] 3.1 Extend modal inventory governance so every applicable A1-authored modal uses the shared compact padded frame and every excluded node has an explicit unframed or extension-owned disposition.
- [x] 3.2 Update representative top-level, nested, startup, authentication, extension-hosted, narrow-width, and resized modal snapshots to assert the global content inset, full-width rules, no blank row above titles, and no unrelated vertical-row removal.
- [x] 3.3 Verify navigation, search/editing, completion, cancellation, nesting, focus restoration, pointer routing, resize, and disposal remain unchanged.
- [x] 3.4 Run focused component/integration tests, source typechecking, architecture and startup-graph checks, strict OpenSpec validation, and applicable terminal presentation checks; record exact results and any explicit environment limitation.
