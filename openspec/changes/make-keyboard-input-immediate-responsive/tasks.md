## 1. Establish input-responsiveness evidence

- [ ] 1.1 Define a bounded input-workload schema for original terminal deliveries, event-loop turns, prepared transcript/surface state, streaming events, barriers, and expected semantic revisions; verify schema tests reject malformed, oversized, or ambiguous workloads.
- [ ] 1.2 Define optional A1-owned phase hooks for receipt, semantic delivery, presentation request, composition, terminal write, and highest presented revision; verify production composition omits the hooks and existing public Pi/runtime boundaries remain unchanged.
- [ ] 1.3 Implement revision/backlog analysis that derives pending presentation depth, accepted-but-unpresented distance, stale frames, first-state presentation, and final-state presentation; verify deterministic analyzer fixtures cover superseded revisions, missing writes, no-op keys, and malformed phase order.
- [ ] 1.4 Build an asynchronous input-responsiveness producer protocol with startup/completion deadlines, bounded output, process-tree termination, and terminal restoration; verify focused protocol tests cover success, timeout, crash, malformed output, and missing checkpoints without calling `renderNow()` after each input.
- [ ] 1.5 Add independent bare-`a1`, `a1 pi`, and untouched pinned-Pi producer paths with equivalent prepared state and source-traceable semantic expectations; verify separate process identities, matching workload inputs, authentic focused surfaces, and the absence of A1-derived pinned expected output.
- [ ] 1.6 Capture a pre-fix matrix for isolated typing, multi-callback rapid typing, repeated menu navigation, empty versus long transcript, and input during a pending stream frame; retain bounded machine-readable evidence and verify it identifies whether delay occurs in routing, scheduling, composition, write, or more than one phase before runtime behavior changes.

## 2. Coordinate proven-safe input bursts

- [ ] 2.1 Implement a finite, surface-aware classification for safe ordinary editor text/editing input, declared built-in navigation, barriers, and unknown input; verify table-driven tests fail closed for fragmented paste, terminal replies, unknown escapes, mode-changing shortcuts, and extension/custom surfaces.
- [ ] 2.2 Implement the bare-custom-viewport input coordinator so original safe deliveries drain once each in receipt order at one immediate event-loop opportunity without concatenating, splitting, decoding, or debouncing them; verify virtual-scheduler tests observe at most one pending keyboard presentation and one current-state frame per coordinated burst.
- [ ] 2.3 Implement barrier handling for submit/activation, Escape/cancel, interrupt, paste framing, terminal protocol input, focus replacement, resize, disposal, and shutdown; verify preceding safe input flushes first, the barrier runs exactly once, and no accepted item survives its owning surface.
- [ ] 2.4 Connect input receipt to the existing stream-presentation preemption contract without adding another frame cadence; verify a pending stream deadline cannot run ahead of keyboard state and separately due spinner or final stream frames still run.
- [ ] 2.5 Scope coordination exclusively to bare A1's custom viewport; verify `a1 pi`, pinned Pi, regular-mode scheduling, installed packages, and source-synchronized Pi components have no production diff or changed input trace.
- [ ] 2.6 Add semantic regression cases for ASCII and multi-codepoint graphemes, cursor movement, Backspace/Delete, history, autocomplete, queued input, repeated arrows, submit, cancel, interrupt, and clipboard paste; verify final text, cursor, selection, focused row, side effects, and action counts match sequential Pi behavior.

## 3. Reuse stable custom-viewport presentation

- [ ] 3.1 Add explicit transcript/document, viewport-interaction, terminal-geometry, dock-allocation, overlay, image, selection, and active-surface revisions to the visible-viewport cache key; verify each relevant mutation invalidates exactly the required cached state.
- [ ] 3.2 Store and reuse established visible transcript rows, viewport metadata, hit regions, scrollbar/sticky state, and dock allocation for geometry-stable dock input; verify typing and fixed-height menu navigation render zero settled transcript blocks and preserve identical transcript cells and metadata.
- [ ] 3.3 Emit and validate a dock-only semantic frame cause through the existing public presentation boundary; verify terminal writes remain within changed dock/cursor rows with zero unexpected fullscreen clears and no scroll transformation for replacement surfaces.
- [ ] 3.4 Re-establish the conservative frame after editor wrapping, autocomplete height changes, menu replacement/resize, queued rows, widgets, transcript navigation, selection changes, overlays, images, stale metadata, terminal resize, or unknown grammar; verify reuse resumes only from a fully established safe frame.
- [ ] 3.5 Verify cached and conservative frames preserve ANSI styling, links, grapheme boundaries, selection, scrollbar overlay, sticky prompts, focus, cursor, editor pointer behavior, auto-scroll, modal ownership, and terminal restoration across open, transition, and disposal tests.
- [ ] 3.6 Compare geometry-stable input over empty and long settled transcripts; verify settled transcript size adds zero block renders and no additional A1-owned viewport composition or terminal-paint work per key.

## 4. Gate comparative responsiveness without flaky timing thresholds

- [ ] 4.1 Complete smoke workloads for isolated text, rapid text/editing, fixed-height menu navigation, submit, and stream preemption; verify all three producers produce equivalent semantic outcomes and bounded phase evidence in one immutable capture per profile.
- [ ] 4.2 Complete full workloads for cursor/deletion/grapheme editing, editor wrapping, replacement surfaces, barriers, empty/long transcripts, and concurrent streaming; verify each workload has independent replayable terminal cells and revision checkpoints.
- [ ] 4.3 Declare deterministic budgets of one pending keyboard presentation, zero final backlog, zero post-drain stale frames, burst-bounded rather than key-bounded input frames, zero stable transcript block renders for dock-only input, dock-bounded writes, and zero unexpected fullscreen clears; verify focused budget tests fail on every individual budget violation.
- [ ] 4.4 Record warmed monotonic receipt-to-application, application-to-compose, composition, write, first-state, and final-state distributions for bare `a1`, `a1 pi`, and pinned Pi; verify reports compare same-run distributions while automated pass/fail remains grounded in deterministic structure plus a bounded hang deadline.
- [ ] 4.5 Integrate the smoke and full input evidence with the existing impact-aware rendering selection and shared immutable capture model; verify ordinary unrelated changes do not launch producers, presentation changes select smoke, rendering/input-core or uncertain changes select full, and no assertion duplicates producer launches.
- [ ] 4.6 Add a deliberate determinism repeat and bounded human-readable diagnostics for semantic, scheduler, backlog, frame, row, byte, clear, and fallback differences; verify identical inputs reproduce structural evidence and failures identify the first divergent phase.

## 5. Preserve architecture and accepted behavior

- [ ] 5.1 Add focused session-shell and runtime tests proving input pre-routing, viewport ownership, request ordering, stream finalization, resize, and disposal behavior remain accepted outside the new coordination boundary.
- [ ] 5.2 Add focused editor, list/menu, replacement-surface, and extension-boundary tests proving shared component geometry, styles, shortcuts, search, selection, activation, cancellation, and focus restoration are unchanged.
- [ ] 5.3 Add governance coverage that forbids private/deep Pi imports, prototype/runtime patches, installed-package edits, direct terminal writes outside the existing authority, and imports that violate the Pi adapter/component direction; verify architecture and provenance checks pass.
- [ ] 5.4 Verify the implementation diff leaves comparison-mode production files and pinned fixtures unchanged except for independent evidence adapters explicitly required by the design, and document every permitted evidence-only difference in provenance.
- [ ] 5.5 Run strict validation for `make-keyboard-input-immediate-responsive` and inspect complete OpenSpec validation; verify this change's proposal, design, delta-spec, and task consistency checks pass, and report any unrelated pre-existing all-artifact failures without folding them into this implementation stream.

## 6. CI, physical acceptance, and completion

- [ ] 6.1 Push the implementation from a fresh worktree and branch based on current `origin/develop`, open a code pull request citing this accepted change, and verify the pull request contains no planning-worktree or unrelated `bin/pi-tui.d.ts` change.
- [ ] 6.2 Run the required impact-selected CI and read its completed current-head result without foreground polling; verify ordinary, documentation, input/rendering, containment, and aggregate checks pass and retain the exact candidate artifact.
- [ ] 6.3 Build the exact candidate and hand off color-preserving Windows Terminal commands using `./scripts/dev` and `./scripts/dev pi`; verify the instructions cover isolated typing, rapid bursts, cursor movement, deletion, submit, held/repeated menu arrows, editor wrapping, long transcript, replacement surfaces, and input during streaming.
- [ ] 6.4 Obtain user-controlled side-by-side acceptance that bare `a1` visibly starts immediately, finishes bursts without catch-up, tracks current menu selection, and preserves all accepted visuals and controls; record any contradiction as reopened work rather than overriding it with diagnostics.
- [ ] 6.5 After required CI passes and physical acceptance is recorded, obtain explicit user authorization before manually merging the code pull request; verify no auto-merge was enabled for the code change.
- [ ] 6.6 Record final structural evidence, diagnostic comparisons, exact artifact identity, Windows Terminal acceptance, and merge identity in `acceptance.md`; mark tasks complete only where evidence exists, then archive and synchronize the change through a separate OpenSpec-only follow-up.
