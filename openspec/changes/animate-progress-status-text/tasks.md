## 1. Lock the progress presentation contract

- [x] 1.1 Add focused formatter cases for semantic labels, existing Unicode ellipses, terminal ASCII-period runs, and idempotent canonical output.
- [x] 1.2 Add deterministic frame cases for the two-grapheme accent band, every-third-update movement, muted pause, stationary ellipsis, stable width, and complete wide/combining/emoji graphemes.
- [x] 1.3 Extend shell evidence for built-in working, retry, compaction percentage, and extension overrides while proving non-spinner and pinned-profile frames remain unchanged.

## 2. Implement the quiet animated status

- [x] 2.1 Change the neutral progress formatter to one Unicode ellipsis and add the theme-independent grapheme-aware frame presenter.
- [x] 2.2 Add an A1-owned status indicator that derives its frame phase from existing spinner updates, uses injected muted/accent roles, and owns no timer.
- [x] 2.3 Select the animated Unicode presentation only for custom-viewport bare A1 while retaining the existing pinned indicator and punctuation for `a1 pi`.
- [x] 2.4 Preserve message replacement, compaction progress updates, extension lifecycle, settlement, mode changes, and disposal without geometry or cadence regressions.

## 3. Record and guard the presentation boundary

- [x] 3.1 Update UI reference provenance to describe the Unicode marker and quiet accent sweep as a declared bare-A1 difference.
- [x] 3.2 Update governance evidence so semantic producers remain punctuation-free, the Pi adapter receives the neutral policy by injection, synchronized Pi code remains unchanged, and pinned mode cannot select the A1 animation.
- [x] 3.3 Refresh only bare-A1 status fixtures and record focused implementation evidence, leaving pinned comparison fixtures byte-equivalent.

## 4. Validate the candidate

- [x] 4.1 Run focused component, shell, lifecycle, extension, parity, provenance, architecture, and timer-cleanup tests.
- [x] 4.2 Run typecheck, build, code-documentation and architecture checks, strict OpenSpec validation, and diff hygiene.
- [ ] 4.3 Physically verify through `./scripts/dev` that the Unicode ellipsis is stable, the cyan sweep is visible but not distracting, all active status kinds share it, and work settlement removes it cleanly.
- [ ] 4.4 Compare `./scripts/dev pi` and confirm pinned spinner text, styling, cadence, and geometry remain unchanged.
