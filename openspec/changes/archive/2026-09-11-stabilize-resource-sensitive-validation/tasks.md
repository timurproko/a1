## Recorded implementation, acceptance, and archive completion

- Initial implementation [PR #218](https://github.com/timurproko/a1/pull/218) merged on 2026-09-02 at `fd66bdc6fcf2f32415f958af93f05b63abab20fe` with required CI passing. The checked tasks preserve that implementation and recorded local evidence; they do not prove that all later hosted executions have reliable margin.
- [Release run 34443509572](https://github.com/timurproko/a1/actions/runs/34443509572) passed Windows Node 22/24, Linux, and macOS and published `0.1.8-dev.289`. Later [verification run 34453931474](https://github.com/timurproko/a1/actions/runs/34453931474/job/102796148347) failed four tests in `cohort-state.test.ts` and `update-live-cohort.test.ts` on Windows Node 24 at the unchanged 5000 ms deadline, despite non-file-parallel execution. The earlier successful publication does not erase this recurring failure or establish its cause.
- Corrective [PR #290](https://github.com/timurproko/a1/pull/290) merged on 2026-09-11 at `f85df1bc5e51e916b3865bda0d12a0c278e68c8c`, from final head `9a2af73a5c754cf10485dfdc61cc95c1553fd816` on `fix/resource-sensitive-fixtures`. It reduces redundant durable setup, retains real durable-transition and endpoint-ownership assertions, and adds fixture-phase timings without production runtime, timeout, retry, or scheduler changes. Its implementation worktree was `D:/Git/a1/.worktrees/fix-resource-sensitive-fixtures`; retained worktrees are eligible for cleanup only after this archive integrates.
- Initial-head [CI run 34571406766](https://github.com/timurproko/a1/actions/runs/34571406766/job/103174165529) failed the semantic identifier inventory because four environment-key source locations moved. It stopped before the resource-sensitive partition, so it was not evidence of another cohort timeout. The final head corrected only those four baseline line numbers, preserving the historical cleanup baseline and assertions; it was a new code revision, not a rerun of the failed commit.
- The corrective PR records three fail-fast local runs of all six resource-sensitive files (55 tests per run, maximum test-body duration 1075.7543 ms), 28 initial focused regression/governance tests, and 36 passing focused tests after the inventory correction. Typecheck, architecture, and changed-file documentation checks passed. These local results supplement rather than replace the historical evidence files. No local fast/full/release tier was run.
- Final-head [CI run 34571948531](https://github.com/timurproko/a1/actions/runs/34571948531) passed all required checks. Its Fast validation log reports successful ordinary and resource-sensitive partitions, with all 55 resource-sensitive tests passing under the unchanged five-second timeout; `cohort-state.test.ts` took 1106 ms for eight tests and `update-live-cohort.test.ts` took 86 ms for three tests. Windows Node 22/24 startup and Linux/macOS containment checks also passed. This closes task 4.2, not the post-merge publication gate.
- After the maintainer reported the merge and explicitly requested publication, `npm run develop` was started once from authoritative `develop` at `f85df1bc5e51e916b3865bda0d12a0c278e68c8c`. [Publication run 34574095821](https://github.com/timurproko/a1/actions/runs/34574095821) is responsible for `0.1.8-dev.290`. It completed successfully on attempt 1 across Windows Node 22/24, macOS, and Linux and published `0.1.8-dev.290` to npm `next`. Downloaded registry bytes match the validated candidate's recorded integrity; full package hashes and job links are in `acceptance.md`.
- The maintainer reported a successful update and then explicitly accepted correction #290 and requested recording/archiving on 2026-09-11. This closes task 4.3. Acceptance is recorded after merge/publication and does not invent an earlier physical verdict or accept another change's UI checks.
- Acceptance was recorded in [PR #293](https://github.com/timurproko/a1/pull/293). Archive preflight initially found two hardcoded active-directory evidence reads. Test-only [PR #294](https://github.com/timurproko/a1/pull/294) resolved that prerequisite and merged at `a576717c9accf0fb4f4a78bb3db6e62163453f66` after [CI run 34577394221](https://github.com/timurproko/a1/actions/runs/34577394221) passed all required checks. The consumer now resolves one unambiguous active or dated archived directory without weakening any evidence assertion. Its focused validation passed 33 tests, including real archive-move regressions; full details are in `acceptance.md`.
- After the maintainer reported #294 merged and explicitly requested archiving, both added requirements were synchronized to `continuous-integration` and `isolated-regression-testing`. Exact comparison verified every delta scenario and preserved all previous main-spec content. The change, metadata, and historical evidence were moved together to `openspec/changes/archive/2026-09-11-stabilize-resource-sensitive-validation/`. This completes task 4.4; retained worktree cleanup waits for the OpenSpec-only archive PR to integrate. No other change's acceptance or archive status is affected.

The proposal/design describe the original scheduling incident and plan. This section records the later serialization-insufficient incident and correction under their existing optimization requirement; it does not alter that requirement or treat passing pull-request CI as completion of post-merge release verification.

## 1. Evidence and Suite Ownership

- [x] 1.1 Add structured regression evidence for runs `33617331350`, `33642848728`, and `33657859943`, including platform, failing test, timeout, comparison lane, and excluded unrelated failures; verify the evidence schema and cited job URLs in a focused governance test.
- [x] 1.2 Declare the initial five evidence-backed resource-sensitive fast test files in the authoritative validation-suite configuration without adding a timeout override; verify every path exists and is otherwise part of the fast universe.
- [x] 1.3 Add configuration invariants rejecting duplicate paths, overlap with explicit/isolated scopes, missing files, partition timeout overrides, and unowned tests; verify focused negative fixtures fail closed with actionable diagnostics.

## 2. Partitioned Fast-Tier Planning

- [x] 2.1 Extend the validation-tier planner to subtract resource-sensitive paths from `vitest-fast` and emit one `vitest-fast-resource-sensitive` invocation; verify exact exclusions, exact paths, stable identifiers, and exactly-once ownership in planner tests.
- [x] 2.2 Apply no file parallelism to the resource-sensitive invocation while retaining the existing five-second fast-test timeout for both partitions; verify generated Windows and non-Windows commands contain no timeout increase.
- [x] 2.3 Preserve fail-fast command execution without retries, log parsing, or result conversion; verify a simulated nonzero resource-sensitive outcome ends the tier unsuccessfully after one invocation.
- [x] 2.4 Include separate resource-sensitive command, duration, and result records in existing plan/outcome evidence; verify serialized JSON distinguishes both fast partitions.

## 3. Regression and Governance Coverage

- [x] 3.1 Add focused validation-tier tests proving pull-request and exact-package selections share the same partition and that full-release composition does not duplicate partition-owned files.
- [x] 3.2 Add a focused execution regression that repeatedly runs the resource-sensitive files without file parallelism under the existing five-second timeout and records clean per-file plus available fixture/subprocess timing without retrying failures.
- [x] 3.3 Optimize repository setup, subprocess count, storage operations, or release fixtures for any isolated test that lacks reliable margin under five seconds; verify repeated focused execution passes without weakening hermetic state or assertions.
- [x] 3.4 Update validation-suite governance tests and operational documentation to explain classification criteria, exact ownership, unchanged timeout policy, optimization requirements, and why timeout increases and automatic retries are forbidden; verify changed-file documentation governance passes.

## 4. Validation and Delivery

- [x] 4.1 Run focused validation-tier, suite-policy, and repeated resource-sensitive regression tests under five seconds plus typecheck, architecture, changed-file documentation governance, and strict OpenSpec validation; record commands, phase timings, and successful outcomes without running local fast/full/release suites.
- [x] 4.2 Push the implementation branch and require current-head CI to pass, including the ordinary and resource-sensitive fast partitions; verify the workflow summary exposes distinct successful outcomes.
- [x] 4.3 After maintainer acceptance and manual merge authorization, merge the implementation and run `npm run develop` once from authoritative updated `develop`; verify one newly numbered exact package passes Windows Node 22/24, macOS Node 24, and Linux Node 24, publishes to npm `next`, and needs no manual rerun.
- [x] 4.4 Record acceptance with merge, package digest, first-attempt platform outcomes, publisher, and registry evidence, then synchronize and archive the completed change under the default complete-change workflow.
