## 1. Approval and original failure

- [x] 1.1 After explicit plan approval and an implementation request, resume the same worktree/branch/draft PR; record authorization and verify version-2 linkage, the reviewed baseline, and absence of unrelated changes.
- [x] 1.2 Record the original Windows Node 22 run, source, job, failed owner, counter value, and available artifact contents; verify the report distinguishes the confirmed interval-attribution flaw from the still-unproven cause of the historical failure and retains the other lane/trust results.

## 2. Frame-accurate evidence

- [x] 2.1 Add deterministic mixed-frame accounting regressions for stream-then-dock and violating-dock-then-stream/geometry; verify the existing final-cause aggregation produces a false positive and a false negative without modifying the zero budgets.
- [x] 2.2 Capture ordered per-composition cause, frame/input identities, render-counter boundaries, viewport regions, and associated writes using existing observational seams or a minimal optional hook; verify no-write compositions and known non-frame controls remain represented and enabling observation does not change semantic state, output, scheduling, or render count.
- [x] 2.3 Update the internal producer protocol and validators together; verify missing, duplicate, reversed, negative, overlapping, out-of-range, discontinuous, and unaccounted evidence fails, while all measured render deltas and write intervals reconcile exactly once within unchanged output/time bounds.
- [x] 2.4 Replace checkpoint-final-cause stable-work accounting with per-frame render/paint accounting and bounded first-failure diagnostics; verify zero-work mixed frames pass, genuine extra dock renders/paints still fail, and the failure names workload, producer, checkpoint, frame/cause, expected/actual values, viewport, and write range from the original capture without recapturing.

## 3. Reproduction and validation

- [x] 3.1 Exercise actual producers with controlled stream-before-input and stream-after-input ordering at existing fixture seams; verify all six workloads and three independent producers retain semantic/cell parity, backlog/frame limits, zero deliberate repeat captures, and unchanged cadence/sleeps/budgets; if a real runtime reuse defect remains, record the precise frame and pause for approved scope refinement rather than masking it.
- [ ] 3.2 Pass strict OpenSpec validation and applicable current-head implementation CI; record check URLs and verify source provenance, dependencies, workflows, rendered baselines, producer error/cleanup protocols, and all budget constants remain unchanged.
- [ ] 3.3 Obtain native Windows Node 22/24, Linux Node 24, and macOS Node 24 input/full-regression evidence and record exact source/run/job identities; coordinate explicitly authorized combined-candidate validation if the separate #390 trust fix is needed, and verify all required lanes pass without hiding another failed owner, adding retries/skips, or increasing timeouts.

## 4. Review and handoff

- [x] 4.1 Provide the exact worktree/commit, focused evidence commands, and build-first color-preserving UI comparison commands for typing, menu navigation, resize, and concurrent streaming; verify the handoff explains expected diagnostic changes, unchanged runtime behavior, and the separate trust/nightly/post-merge package obligations.
- [ ] 4.2 Record actual maintainer review and the reviewed canonical-spec baseline for the exact final head; verify an authorized acceptance comment represents those results and preceding substantive tasks are complete, with auto-merge disabled and explicit manual merge authorization kept separate.

## 5. Mechanical archive preparation

- [ ] 5.1 Record verified implementation acceptance and merge evidence for archive preparation.
- [ ] 5.2 Stage and verify delta synchronization and the archive move in an OpenSpec-only candidate.
