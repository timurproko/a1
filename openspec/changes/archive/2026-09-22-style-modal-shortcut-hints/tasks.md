## 1. Establish the shared shortcut-row presentation

- [x] 1.1 Inventory every shortcut-bearing bare-A1 modal node and classify its current formatter, width policy, ownership boundary, and comparison-profile behavior.
- [x] 1.2 Add boundary-safe semantic hint-entry renderers with dim shortcut labels, muted action names, two-space entry gaps, platform-aware effective labels, ANSI-safe clipping/wrapping, and an exact startup source-byte baseline.
- [x] 1.3 Add focused component tests for semantic color roles, exact spacing, omitted unbound actions, punctuation inside descriptions, narrow widths, and theme invalidation.

## 2. Adopt the presentation across modal surfaces

- [x] 2.1 Convert A1-owned Models, Skills, Thinking, session, tree, trust, settings, Changelog, Hotkeys, and dialog-panel instruction rows to semantic hint entries without changing wording or behavior.
- [x] 2.2 Adapt bare-A1 extension select/confirm/input/editor and authentication/nested modal routes at their existing component factories, source-synchronizing only the minimum components whose public presentation is not configurable.
- [x] 2.3 Give the pre-resource project-trust selector the isolated fixed-color equivalent while preserving startup trust boundaries.
- [x] 2.4 Preserve public pinned component construction for `a1 pi` and verify ordinary shell help, status, transcript, and footer surfaces are unchanged.

## 3. Prove completeness and behavior

- [x] 3.1 Update representative modal snapshots and interaction tests to assert distinct key/action roles, whitespace-only entry gaps, responsive layout, and unchanged effective bindings.
- [x] 3.2 Add inventory-backed coverage that rejects an unmapped shortcut-bearing bare-A1 modal or a modal hint using whole-line color or separator glyphs.
- [x] 3.3 Verify top-level, nested confirmation/input/editor/authentication, startup trust, extension-hosted modal, and full-screen Settings/Changelog/Hotkeys transitions retain navigation, completion, cancellation, restoration, resize, and disposal behavior.
- [x] 3.4 Run source typechecking, focused component/modal tests, strict OpenSpec validation, architecture/startup reachability validation, and the applicable terminal presentation checks; record exact results and any explicit environment limitation.
