# Implementation evidence

## Authorization and baseline

The maintainer explicitly requested implementation with `implement pr 428`. Implementation continues in PR #428, branch `refactor/simplify-pr-validation`, and its existing worktree. The proposal commit is `41b82e7248411a14c7348f8ab7458a873cb26cf8`; its accepted base is `29cc4c03684ca05451c5e6f93b45810f261eb95b`. This authorization is not final acceptance or merge authority.

The machine-readable ownership report at `evidence/ownership-ledger.json` accounts for all retained tests under one coarse owner and records PR-core, complete-fast, explicit integration, and platform/runtime ownership. The durable-oracle classification at `evidence/durable-oracle-audit.json` maps each recurring historical-evidence or exact-prose assertion to current behavior, hermetic evidence tooling, structured stable policy, or one-time finalization evidence.

## Historical observations (not speedup evidence)

These observations are deliberately separate and are not treated as comparable proof of a speedup:

- Development run [34961661496](https://github.com/timurproko/a1/actions/runs/34961661496) succeeded at `1b7859fe08f6b5d1abf2b7953cb706c2445021f4` after conservative manual selection. Fast validation lasted about 4m45s, resource-sensitive validation about 3m14s, startup about 4m54s, and its complete promoted-owner cell about 10m36s. This was a successful broad baseline, not an ordinary selective path.
- Full regression run [34973476437](https://github.com/timurproko/a1/actions/runs/34973476437) succeeded at `4a22f53e4ac00aa89c9156ebfb6df4b4edd54b4a`: macOS Node 24 took about 10m, Linux Node 24 about 11m22s, Windows Node 24 about 30m8s, and Windows Node 22 about 39m32s. It is complete-cadence evidence, not PR-core timing.
- PR #425 Development run [35061215617](https://github.com/timurproko/a1/actions/runs/35061215617) succeeded at `ed6baa1d18d05aa327037e8928b5812deb31e7aa`. Its fast cell took about 4m48s and its complete promoted-owner cell about 15m35s. Earlier runs `35060412082`, `35009856258`, `35009367059`, `35008030730`, `35006946913`, and `35006312931` were cancelled and remain distinct from the successful observation.
- The predecessor archive retains failed Development attempts and their causes separately from its successful runs, including package readiness, workflow envelope, aggregate scanner, resource contention, startup, and Full regression failures. This change neither relabels those attempts nor infers an unmeasured ordinary-path result from them.

No new three-to-five-minute PR-core claim is made before exact-head hosted evidence. Queue time, setup differences, changed test populations, and conservative selection make these historical runs non-comparable.

## Local implementation checkpoint

Exact worktree dependencies were installed with `npm ci --ignore-scripts --no-audit --no-fund`; this did not build or run a product suite.

The first focused selector/aggregate run passed 113 of 115 tests. One rendering fixture still expected an unsupported rendering asset to fail closed to full coverage, and one excluded-owner fixture supplied an incomplete owner scope that the aggregate correctly did not recognize as that owner. The implementation restored unsupported-extension fallback and made any same-target excluded-owner evidence contradictory. The first typecheck also found six test-only implicit `any` parameters in the new ownership audit; those callbacks were typed. These failed local attempts are debugging evidence, not acceptance.

After correction:

- `npx vitest run test/repository-governance/validation-ownership.test.ts test/repository-governance/validation-impact.test.ts test/repository-governance/integration-selection.test.ts test/repository-governance/integration-impact.test.ts test/repository-governance/modular-validation-aggregate.test.ts test/repository-governance/impact-aware-validation-workflows.test.ts test/repository-governance/validation-suite-policy.test.ts test/repository-governance/validation-tier.test.ts test/repository-governance/full-regression-policy.test.ts test/repository-governance/integration-owner-registry.test.ts test/repository-governance/development-validation-required.test.ts test/repository-governance/package-download-cache.test.ts test/features/launch/terminal-colour-fidelity.test.ts test/repository-governance/terminal-host-proof-gate.test.ts test/repository-governance/terminal-host-provenance.test.ts test/repository-governance/validation-receipt.test.ts test/repository-governance/validation-receipt-workflows.test.ts --maxWorkers=1 --minWorkers=1` passed **178 tests across 17 files**.
- `npm run typecheck` passed.
- `npm run check:architecture` passed architecture, product/package identity, pinned Pi provenance, and the stable terminal-host provenance check.
- `node scripts/governance/check-code-documentation.mjs --mode changed --selection .artifacts/validation/impact-local.json --result .artifacts/validation/code-documentation-local.json` passed before subsequent bookkeeping-only evidence edits; the final full tracked-file documentation check also passed.
- `npm run check:docs-governance` passed.
- `npx openspec validate simplify-pr-validation --strict --no-interactive` passed.
- Workflow YAML parsed successfully, `git diff --check` passed, and the regenerated ownership ledger accounts for **354 retained tests**.
- After the first hosted attempt exposed draft-check and Windows command-length defects, the focused workflow/tier run passed **20 tests across 2 files**, followed by an expanded aggregate and branch-policy run passing **64 tests across 6 files**. The workflow YAML parsed successfully; `npm run typecheck`, `npm run check:architecture`, `npm run check:docs-governance`, and the full tracked-file documentation check passed. The workflow fixture proves draft runs resolve to a non-protected check name. The tier fixture constructs the complete regular test selection, proves it is partitioned below 6,000 command characters with every selected test exactly once, and proves a partition failure fails the logical outcome.
- After the next hosted attempt exposed inherited selection payloads and a cold-worker fixture race, the first isolated history check failed against stale emitted paste-helper bytes. `npm run build --silent` refreshed those bytes; an intermediate direct recall assertion then correctly showed that terminal input is asynchronously coordinated. The corrected fixture waits for the service's structured initial snapshots and retains the existing bounded input wait. The isolated scenario passed, the complete `session-shell.test.ts` resource file passed **282 tests**, the workflow/tier check passed **20 tests**, and `npm run typecheck` plus strict OpenSpec validation passed. No timeout or retry was added.
- After the third hosted attempt exposed a stale hermetic validation-impact fixture, `npx vitest run test/repository-governance/code-documentation-command.test.ts` passed all **4 tests** with a current version-2 selection and computed identity. `npm run typecheck`, strict OpenSpec validation, and `git diff --check` passed. Parser schema and identity validation remain unchanged.
- After the fourth hosted attempt exposed filesystem contention, the isolated `release-gc.test.ts --no-file-parallelism` run passed all **21 tests** in 7.31 seconds with unchanged assertions and timeouts. The first three-file suite-policy run then failed only its two obsolete resource-membership expectations; after updating those explicit policy oracles, the same three files passed **33 tests**. `npm run typecheck` passed, and the regenerated ownership ledger still accounts for all **354 retained tests** while assigning release cleanup to the serial resource partition.
- After the fifth hosted attempt exposed secondary governance oracles, the first complete serial repository-governance run passed **967 tests across 95 files** and failed one assertion because the active resource list had incorrectly been folded into retained historical incident evidence. The fixture was corrected to preserve the original six-file incident partition while adding release cleanup only to current policy. Re-running the complete directory passed **968 tests across all 96 files**. A final isolated release-cleanup recheck passed **21 tests** in 6.62 seconds; `npm run typecheck`, `npm run check:architecture`, strict OpenSpec validation, and the full tracked-file documentation check passed. The package workflow oracle now requires the attempt-qualified outcome filename, and all current resource-membership oracles contain the 21-file partition.
- The requested phase-identity refinement initially produced a YAML parse failure because an unquoted expression contained the phase marker's colon; quoting the three expression scalars corrected it before commit. The focused workflow/aggregate/acceptance-route run then passed **36 tests across 3 files**, workflow YAML parsed, and the complete serial repository-governance directory again passed **968 tests across all 96 files**. `npm run typecheck`, `npm run check:architecture`, `npm run check:docs-governance`, strict OpenSpec validation, and the full tracked-file documentation check passed. The fixtures prove Implementation uses `Acceptance validation not active` plus `Implementation validation complete`, while Acceptance and legacy acceptance-only routes retain the exact protected identities.
- After the sixth hosted attempt exposed two asynchronous fixture boundaries, the complete `session-shell.test.ts` file passed **282 tests** after the repeated-paste case synchronized each assertion on its structured request-settlement event instead of a one-second polling proxy. A locally built exact package then passed all **7 session-resume integration tests** after the fixture prepared, certified, and activated those exact bytes before starting the unchanged 30-second public-launch readiness window; candidate preparation took 20.40 seconds and the first bounded supervisor/guardian/UI launch took 2.45 seconds. The complete serial repository-governance directory passed **968 tests across all 96 files**. The first full documentation check rejected an uncategorized explanatory comment; adding the required `Rationale:` category corrected that governance defect. `npm run typecheck`, `npm run check:architecture`, `npm run check:docs-governance`, strict OpenSpec validation, the corrected full tracked-file documentation check, and `git diff --check` passed. No timeout, retry, or semantic assertion changed.
- The Acceptance-routing correction's first focused invocation exposed four malformed nested `Map` fixtures at transform/typecheck time; correcting the fixture delimiters produced **40 passing tests across 3 files**. The first complete serial governance run then passed 971 tests and failed one stale docs-job condition oracle. After adding the new Acceptance-phase exclusion to that semantic expectation, the complete directory passed **972 tests across all 96 files**. Typecheck, architecture, docs governance, full tracked-file documentation governance, workflow YAML parsing, strict OpenSpec validation, and `git diff --check` passed. A live read-only query against PR #428 resolved successful exact-head Implementation authority to run `35082268555`, attempt 1, job `104752165955`. Hermetic fixtures reject missing, stale-head, wrong-PR, current-run, failed, and newer-failure evidence; Acceptance skips all generic lanes and cannot satisfy itself.
- The no-phase-promotion correction passed **70 focused governance tests across 6 files** and the complete serial repository-governance directory passed **970 tests across all 96 files**. Typecheck, architecture, docs governance, full tracked-file documentation governance, workflow YAML parsing, strict active-change validation, and `git diff --check` passed. The fixtures prove one finalized `Implementation` run executes product/governance and finalized-delivery validation, emits the stable protected aggregate directly, retains the legacy version-1/version-2 route, rejects an Acceptance-phase version-3 body, and reruns or blocks on changed head/body evidence. Finalized-delivery policy is read from the trusted base after deployment; this authority-changing bootstrap alone falls back visibly to its exact head when the base lacks the new no-promotion rule. No product timeout, retry, assertion, or test ownership changed.

A local changed-naming invocation was rejected because that gate intentionally compares a committed head diff while the debugging selection included uncommitted worktree files. No naming result is claimed from that unsuitable invocation; exact-head CI remains authoritative.

No local `test:fast`, `test:full`, `test:release`, interactive product UI, or publication command was run. The focused session-resume validation used locally built and packed exact candidate bytes as recorded above.

## Deterministic selection replay

`evidence/selection-replay.json` records ten policy-bound scenarios without GitHub or mutable history: unrelated governance, UI/rendering, launch/startup, release/package/update, Pi, native containment, image/history, shared support, validation authority, and an unknown operational path. Ordinary fixtures select only their coarse owners plus linked integration; shared support and both fail-closed fixtures select complete coverage. The report binds policy identity `4aca3b7475be969b4005494728d4f77582796657fb1ef6723b95e3162d45fc08` and all 354 retained tests.

## Hosted attempts and post-finalization evidence plan

The first finalized exact-head Development run [35068187888](https://github.com/timurproko/a1/actions/runs/35068187888) at `e730b61490c768f58a1a0c8dd039658b087a302e` is retained as a failed implementation attempt. Its acceptance-policy job rejected the required `Implementation` phase as expected. Its PR-core job failed before tests because the complete conservative file list exceeded the Windows command-line limit; this directly caused the bounded-invocation correction. The run was cancelled after the actionable failure was recorded so unfinished peers would not consume more capacity.

Development run [35069809844](https://github.com/timurproko/a1/actions/runs/35069809844) at `dd051dcf94cbf3c99b9b7bbc8863e83f55ab339a` is retained as a failed exact-head attempt. The PR core reached 113 passing files and 965 passing tests before two `update-recovery` assertions failed because consumed `VALIDATION_TESTS_JSON` still enlarged spawned-child environments to `ENAMETOOLONG`. The resource lane reached 281 passing tests before its cold durable-history assertion observed no initial snapshot. Every other selected product/governance owner passed; the acceptance phase gate and final aggregate failed as required. The correction removes both consumed orchestration payloads before child execution and synchronizes the fixture on structured worker snapshots without retries or timeout changes.

Development run [35072062726](https://github.com/timurproko/a1/actions/runs/35072062726) at `3199c1b40be760d07a7493c215a0c9d62a0e7c80` is retained as a failed exact-head attempt. The PR core's first partition passed 114 files and 967 tests; its second passed 96 files and 1,105 tests before the sole `code-documentation-command` assertion rejected its own obsolete `a1-validation-impact-v1` fixture under the required version-2 parser. Resource-sensitive, rendering, package, startup, Pi, image/history, Unix, naming, and changed-documentation jobs passed. The acceptance phase gate failed as required, and the unfinished full-only owner was cancelled after the semantic failure was recorded. The correction upgrades only the hermetic fixture to current PR-core fields and a computed selection identity; parser validation and consumer coverage remain intact.

Development run [35073070682](https://github.com/timurproko/a1/actions/runs/35073070682) at `02fb1962fea8687f4e9659abf93deddc3911336c` is retained as a failed exact-head attempt. In the parallel core partition, `release-gc.test.ts` passed 18 tests but three filesystem-heavy backlog/retry cases exhausted their unchanged 30-second/15-second test budgets under contention; the partition otherwise passed 113 files and 964 tests. Every completed product lane passed, including the resource-sensitive lane, rendering, package, startup, Pi, image/history, and Unix jobs. The expected Implementation-phase acceptance gate failed, and the unfinished full-only owner was cancelled after the actionable failure was recorded. The correction moves the existing release-cleanup file to a fresh serial resource-sensitive invocation without changing assertions, budgets, complete-suite ownership, or retry behavior.

Development run [35074560648](https://github.com/timurproko/a1/actions/runs/35074560648) at `f0dd19f51623a74b79e0091586ec083e2e9747fd` is retained as a failed exact-head attempt. The resource-sensitive lane, including serial release cleanup, passed, as did every integration/product lane and the core's first 114-file/967-test partition. The second core partition passed 94 files and 1,025 tests before three governance assertions exposed two remaining stale current-resource membership oracles and one pre-attempt-qualified package outcome name. The aggregate failed and the Implementation-phase acceptance gate remained blocked as required. The correction reconciles all three semantic policy fixtures and preserves historical incident membership separately from current policy.

Development run [35077023575](https://github.com/timurproko/a1/actions/runs/35077023575) at `62b2beb35fad476576d6809c4f89aa010163d41d` is retained as a cancelled refinement attempt. Its old Implementation-phase acceptance job failed as designed; completed product jobs had no recorded failures when the maintainer requested clearer phase presentation. The run was cancelled and is not acceptance evidence. The refinement reserves the acceptance and protected aggregate names for Acceptance while letting a fully successful Implementation observation finish under distinct non-protected identities.

Development run [35078614306](https://github.com/timurproko/a1/actions/runs/35078614306) at `1356a467dd002bd193bdfbdae801e7a1bfa6069b` is retained as a failed exact-head attempt. The resource-sensitive lane passed 281 of 282 session-shell assertions; its sole failure observed the intentionally hidden provisional paste marker after a generic one-second poll expired before the cold helper's structured settlement. The Pi release/resume lane passed 6 of 7 tests; exact candidate materialization reached `bootstrap-selected` at 29.94 seconds, leaving insufficient time in the unchanged 30-second readiness window for the guardian/UI phase, while every later launch completed in roughly 1.6–2.0 seconds. All other completed product lanes passed, and `Implementation validation complete` failed closed. The corrections synchronize paste assertions on lifecycle settlement and separate exact-candidate preparation from bounded resume readiness without increasing a timeout, adding a retry, or weakening the behavior assertions.

Development run [35082268555](https://github.com/timurproko/a1/actions/runs/35082268555) succeeded at `ccedc73428b92773d32a310b873c38085c0859d0`. Every selected product/governance lane passed, including the corrected resource-sensitive and Pi resume lanes; `Implementation validation complete` succeeded. PR core took 4m09s, resource-sensitive 3m59s, Pi release/resume 3m27s, startup 4m29s, and the longest full-only owner 10m36s. This is valid Implementation evidence for that exact head, not reusable evidence for a later commit.

Acceptance run [35085313376](https://github.com/timurproko/a1/actions/runs/35085313376) at the unchanged `ccedc73428b92773d32a310b873c38085c0859d0` is retained as a cancelled redundant attempt. The phase-only body edit incorrectly started the complete product matrix because Acceptance was excluded only by the legacy acceptance-only route. The run was cancelled when the maintainer reiterated that Acceptance is manual validation after successful Implementation, not a second product-test cycle. The correction made phase Acceptance a separate lightweight route and required prior exact-head Implementation authority before the protected aggregate could pass.

Development run [35087921084](https://github.com/timurproko/a1/actions/runs/35087921084) succeeded at `f585734a26dbdcd1ad38eca4abd82afdbd598be7`. Every selected product/governance lane and `Implementation validation complete` passed; full-only owners took 13m00s and exact-package contracts 11m24s. GitHub still reported the protected `Development validation required` context as expected/pending because the body remained in Implementation, demonstrating that green machine evidence alone did not unlock merge under the superseded two-phase design.

Acceptance run [35089609024](https://github.com/timurproko/a1/actions/runs/35089609024) succeeded against the same `f585734a26dbdcd1ad38eca4abd82afdbd598be7` bytes after a manual phase-only body edit. It ran only `Detect change surface`, `Acceptance record validation`, and `Development validation required`; the protected aggregate reused prior exact-head Implementation authority and made the PR mergeable. The maintainer rejected the required phase ceremony itself: future finalized version-3 candidates must receive that protected result from their one ordinary Implementation workflow and proceed directly to manual review/merge.

Full regression run [35068203404](https://github.com/timurproko/a1/actions/runs/35068203404) was manually dispatched under the superseded evidence plan. It is not ordinary PR acceptance evidence and was cancelled when the maintainer clarified that complete post-merge coverage belongs to nightly and release validation. Its failed and cancelled observations remain visible and are not relabelled or used as a speedup or acceptance claim. No replacement Full regression will be dispatched for this PR.

Acceptance remains blocked until normal `pull_request` Development validation runs against the corrected finalized exact head:

1. Mark the finalized PR ready only after draft-event checks have settled so normal Development validation runs. Because this implementation changes workflow, selector, suite, and aggregate authority, the exact PR run must classify conservatively. Record the run/head/attempt, every job conclusion, selected core/integration owners, aggregate identity, failed attempts, PR-core setup/gate/runner milliseconds, critical path, and total runner milliseconds from `development-validation-impact`, the attempt-qualified core outcome, and `development-validation-aggregate-<head>-<run>-<attempt>`.
2. Report the PR-core three-to-five-minute target as met or unmet from that hosted core job. The deterministic ordinary replay may explain intended selective membership but must not be represented as hosted timing or as a speedup.
3. Preserve every failed Development attempt. If a failed-jobs-only rerun is used, record both attempt numbers and verify the aggregate identifies prior-attempt reuse; never rerun a semantic failure automatically. Any missing result requires an explicit known-gap disposition before acceptance.

This hosted Development gate occurs after version-3 in-branch finalization by repository policy. It is mandatory acceptance evidence even though it cannot be a precondition of the task-completeness check that permits finalization. Complete nightly and release coverage remains unchanged. No three-to-five-minute result is claimed yet.

## Acceptance scenarios

The PR body and eventual conditional manifest use these exact ordered scenarios:

- An ordinary owned-path change runs the bounded PR core and only its reviewed unit and integration owners, while unknown or validation-authority paths select complete coverage.
- Re-running failed jobs on an unchanged run reuses untouched successful attempts, while a current failure or changed head or selection blocks the aggregate.
- Fast, full, and release commands retain complete tests and platform coverage, and exact-artifact gates reject stale or tampered build and package evidence.
