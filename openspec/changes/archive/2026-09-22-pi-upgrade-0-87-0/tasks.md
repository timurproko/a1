## 1. Upgrade

- [x] 1.1 Pin both Pi packages at 0.87.0 and refresh the lockfile.
- [x] 1.2 Re-merge the vendored copies and regenerate the ledger, headers, inventories, baselines, and parity evidence.
- [x] 1.3 Resolve the review items: no conflict marker (the one merged copy, `tree-selector.ts`, took upstream's change cleanly), nothing orphaned, no component unmapped. The three changed public API surfaces A1 consumes need no adoption — `AgentSession` gains `refreshContext()` and private boundary state, `SessionManager` gains `appendContextEdit`, `buildSessionProjection`, and a nullable `appendCompaction` retain, and `ExtensionAPI` gains `context_with_system`, `agent_before_settle`, and an actionable `turn_end` result. A1 assigns no `agent.state.messages`, passes no `shouldStopAfterTurn`, switches exhaustively over neither `SessionEntry` nor `ExtensionEvent`, and never calls `ExtensionRunner.emit`, so every breaking change of 0.87.0 misses A1's source; `buildSessionContext()` stays A1's only session-manager read and now returns the canonical projection.
- [x] 1.4 Record the three new feature rows as reached through the pinned engine: canonical session context and extension boundaries (A1 appends no context edits and registers no boundary handler, and the owned transcript projects session events rather than provider context, so an omitted message leaves the UI history intact as upstream states; the one visible surface, the session tree, follows upstream through the source-synchronized tree-selector copy, which now renders and filters the `context_edit` entry), full-transcript context extensions (A1's one extension registers only `session_start` and `tool_result`), and per-model image input limits (read from the model registry by the engine; the pinned settings selector offers no row for it). Re-evaluate the documented deprecated-dependency exception at this pin; `node-domexception@1.0.0` is still the lockfile's only deprecated package and still arrives through the engine, so it stands.
- [x] 1.5 Re-pin the derived state this version moves: the owned startup graph and Pi artifact totals, the damage-grammar boundary (pi-tui's only 0.87.0 change centers the scroll-to-end indicator and leaves the pinned fullscreen write grammar untouched), the owned command resources, the source ledger hash those resources move, the parity fixtures with their recorded digests, and the pinned identity in the session-shell provenance, parity acceptance, and command workflow outcome baselines — including the interactive-mode line ranges those two baselines cite, which move by 18 to 72 lines because `interactive-mode.ts` grew from 6,780 to 6,852 lines.
- [x] 1.6 Stop two governance tests from reading a literal that the pin itself now satisfies: the session-shell provenance range and the deprecated-exception negative case derive their versions from the pinned identity instead.
- [x] 1.7 Bind `getCrashExtensionHint` in the pinned command-outcome producer: 0.87.0's `handleFatalRuntimeError` calls it for the new crash-diagnostics hint, so the fixture's synthetic owner crashed before it could record the `/import` failure outcome.

## 2. Proof

- [x] 2.1 Every automated gate passes on the resolved head: `npm run build`, `npm run typecheck`, `npm run check:architecture` (all five checks), `npm run check:deprecated`, `npm run check:names`, `npm run check:code-documentation:changed`, `npm run sync:pi-ui`, and `npx vitest run test/repository-governance test/features/owned-ui test/integrations/pi test/app/session-shell`, except for two kinds of failure this checkout causes rather than the tree. `clipboard-packaged`, `editor-text-paste`, `local-cleanup`, `naming-selection`, `release-command` and `validation-impact` fail only under parallel load — EBUSY removing their temporary Git fixtures on Windows, or subprocess fixtures that exceed the vitest timeout — and all pass when run on their own. `session-shell-workflows` reads this machine's real `~/.pi/agent/keybindings.json`, which rebinds `app.thinking.cycle` to `ctrl+l`, so the comparison profile never renders `Shift+Tab`; it fails identically on `develop` at 0.86.1 and is not this upgrade's.
- [x] 2.2 User-visible Pi behavior changes: the session tree lists context edits as their own entries and hides them behind the settings filter; a `context` extension handler no longer drops the prompt and tool declarations, so extension-driven compaction keeps built-in tools and stops Codex emitting raw tool-call text; a retried or length-recovered turn no longer carries its abandoned attempt into later requests; idle prompt-cache warming no longer rebuilds an expired cache when its timer is delayed; a text file beginning with `GIF` is read as text instead of being dropped from `read` and `@file`; malformed prompt-template frontmatter is reported as a resource warning instead of being ignored; crash diagnostics name the loaded extensions in the stack trace; and unknown OpenAI-compatible Chat Completions endpoints stop receiving strict tool schemas they never advertised. The `/bug` offline-upload fix does not apply, since A1 declines that route.
