## 1. Repeatable Finalization

- [x] 1.1 Extend `prepareSinglePrDelivery` in `scripts/governance/openspec-delivery-finalization.mjs` with a re-finalization path: when the body is already finalized and verification fails on target, content, acceptance, or gap drift, restore the active form in the sandbox from the archive (move the archive back to the active path, drop `acceptance.md`, reset each synchronized capability spec to the supplied target bytes), then run the ordinary finalization with the archive's existing date; keep `already-finalized` for a valid current head and keep every existing fail-closed code.
- [x] 1.2 Give `finalize-openspec-delivery.mjs` the same behavior locally by reading target spec bytes with `git show <target>:<path>`, and report the disposition (`finalized`, `refinalized`, `already-finalized`, `would-finalize`, `would-refinalize`).
- [x] 1.3 Add fixtures to `openspec-delivery-finalization.test.ts`: re-finalization after an archived `tasks.md` edit yields a stable archive path and a manifest whose digests match the new bytes; re-finalization after a target advance rebuilds the synchronized spec from the new target; running the operation on its own output is a no-op; incomplete tasks and a missing acceptance list still fail closed.

## 2. Trusted Publication

- [x] 2.1 Add `scripts/governance/openspec-finalization-publication.mjs` with `reconcileFinalization({ reader, publisher, tool, number })`: refuse a closed, draft, forked, non-`develop`, or non-version-3 PR; fetch the `develop` tip and the head into a scratch clone restricted to `openspec/`; classify the head as active-current, finalized-current, finalized-drifted, or behind; produce the restore, merge, and finalize commits the design lists; push with `--force-with-lease` against the head that was read; then `PATCH` the body fence only when the body is unchanged.
- [x] 2.2 Guard the push inside `reconcileFinalization`: every restore or finalize commit must satisfy `assertArchiveDiff` for the change's paths against its parent, a merge commit must have exactly the previous head and the `develop` tip as parents and bring in only paths `develop` changed, and the push carries a lease on the read head; the publisher's REST route guard is unchanged because the body `PATCH` route was already allowed.
- [x] 2.3 Add `scripts/governance/publish-openspec-finalization.mjs` (`--pr <n>`, `--dry-run`) that requires `GITHUB_ACTIONS` with a `pull_request_target` event unless dry-running, mints the App identity from the existing secrets, writes a step summary naming the disposition or the failure code, and exits non-zero without pushing on any failure.
- [x] 2.4 Add `openspec-finalization-publication.test.ts` against a disposable bare Git remote with a fake reader and publisher: each of the four states produces the expected commits and body; a lease failure or concurrent body edit pushes nothing further and reports retry; a merge conflict reports `finalization-merge-conflict`; drafts, closed, legacy, and unassociated PRs are skipped and a forked head is refused; incomplete tasks fail closed without a push.

## 3. Workflows

- [x] 3.1 Add `.github/workflows/openspec-finalization.yml`: `pull_request_target` on `develop` for `synchronize`, `ready_for_review`, `reopened`, and `edited`, skipped for drafts and merged PRs; per-PR concurrency without cancellation; default-branch checkout with `persist-credentials: false`; `npm ci --ignore-scripts`; the publication command with the App secrets; least-privilege `contents: read`, `pull-requests: read` for the token.
- [x] 3.2 In the `delivery` job of `.github/workflows/ci.yml`, when trusted validation reports `delivery-not-finalized` for a non-draft version-3 head, write a step summary and error annotation that automated finalization is pending and link the `OpenSpec finalization` workflow; keep the job failing so the unfinalized head stays unmergeable.
- [x] 3.3 Pin the new workflow in `openspec-archive-workflow.test.ts` (or a sibling test): triggers, draft skip, default-branch checkout, secret names, concurrency shape, and the absence of any PR-head script execution; pin the `delivery` summary text in the workflow fixtures.

## 4. Governance, Documentation, And Evidence

- [x] 4.1 Add the workflow to `config/github-repository-governance.json` with authority `single-pr-finalization-publication`, derive that authority in `github-repository-governance.mjs`, and update `github-repository-governance.test.ts`.
- [x] 4.2 Update `openspec/config.yaml`, `.agents/skills/change-delivery/SKILL.md`, `docs/openspec-archive-automation.md`, `docs/ci-release-runbook.md`, and `docs/validation.md`: finalization happens on the ready PR by trusted automation, the local command is optional and produces the same result, developers pull before pushing, and the `Revert "docs(openspec): finalize"` step is gone; update `change-delivery-guidance.test.ts` accordingly.
- [x] 4.3 Run the finalization, publication, workflow, guidance, and governance tests plus typechecking and the governance commands, and record the commands and outcomes; record as post-merge evidence the first PR finalized by the workflow (its run, the finalization commit, the exact-head validation run, and the manual merge) and the absence of `Revert "docs(openspec)` commits on PRs merged after it.
