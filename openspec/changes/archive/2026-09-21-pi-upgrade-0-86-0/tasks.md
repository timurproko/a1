## 1. Upgrade

- [x] 1.1 Pin both Pi packages at 0.86.0 and refresh the lockfile.
- [x] 1.2 Re-merge the vendored copies and regenerate the ledger, headers, inventories, baselines, and parity evidence.
- [x] 1.3 Resolve every conflict marker (11 hunks in 6 copies: `session-footer`, `session-selector`, `skill-invocation-message`, `status-indicator`, `tool-execution`, `editor-core`); re-anchor the three orphaned entries (`session.resume.all`, `resume.scope.all`, `resume.scope.current`) to the rewritten scope toggle and lift `session.resume.all` out of `orphaned`; adopt the seven changed public API surfaces A1 consumes (`AgentSession`, `ExtensionAPI`, `SessionManager`, `SettingsCallbacks`, `SettingsConfig`, `SettingsManager`, `TuiAltScreenOptions`), wiring `cacheWarmingMode` and `onCacheWarmingModeChange` through the settings bridge, port, metadata, and modal inventory; no component was left unmapped.
- [x] 1.4 Dispose of all seven pending feature rows: adopt prompt cache warming, transcript-aware prompt and tool updates, the offline Radius catalog, per-model compaction budgets, and `onCacheWarmingModeChange` through the pinned engine; decline `/bug` because its collection, upload, crash-log, and session-export modules are absent from the pinned package's public API and `exports` map, leaving it reachable only through forbidden deep imports. Record the decline in the ledger under a new `declined-not-adopted` host-adaptation state, keep the command mapped in the presenter inventory so the coverage gate still sees it, and name it in `PINNED_PI_DECLINED_COMMAND_NAMES` so A1 presents no route while a command upstream adds still fails the manifest gate until it is classified.
- [x] 1.5 Refresh the eight owned-copy hashes the conflict resolution invalidated, and correct the ledger record count 0.86.0 moves from 116 to 118.
- [x] 1.6 Keep the upgrade from being absorbed by the startup budget: load the `/tree` and `/resume` selectors on demand and re-pin `config/startup-graph-baseline.json` (150 files, 1,457,278 bytes, down from 153 and 1,548,441; Pi artifact 2,039 files, 9,408,350 bytes).

## 2. Proof

- [x] 2.1 Every automated gate passes on the resolved head: `npm run typecheck`, `npm run check:architecture` (ledger, inventories, and startup graph current), `npm run check:code-documentation` and the changed-documentation check, `npm run check:docs-governance`, and `npx vitest run test/repository-governance test/integrations/pi test/features/owned-ui test/app/session-shell test/integrations/pi/components`, except for failures this Windows checkout causes rather than the tree: CRLF text assertions (`change-delivery-guidance`, `ci-release-runbook`, `full-regression-policy`, `pi-upstream-sync-workflow`, `release-command`, `release-pipeline-policy`), Node 26 emitting a `DEP0205` warning on the captured stderr of `command-outcome-parity` and `command-message-parity` where CI runs Node 24, and `clipboard-packaged` and `image-worker-package` needing a `dist/` this host cannot link. `npm run build` stops at the Rust process guardian here for the same reason; CI covers it.
- [x] 2.2 User-visible Pi behavior changes: `/bug` is advertised by the engine but not presented by A1; the settings list gains Cache warming (off/streaming/idle), whose values the extractor now resolves so the row is applicable rather than rejecting every choice; a `skill:<name>` command completes from `/skill:` or its bare name instead of `/skill`, so a slash-separated search such as `/sk/rev` no longer offers one; the pinned tool-result renderer re-converts a replaced image instead of keeping the first conversion; `/session` gains a Cache Warming section reporting the mode and the decision behind the current state; and the `/hotkeys` copy row now reads "Copy selection or last assistant message".
