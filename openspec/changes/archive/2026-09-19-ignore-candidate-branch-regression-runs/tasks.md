## 1. Gate

- [x] 1.1 Skip runs whose head branch is not `develop` in the workflow condition and in `triageDecision`, with the reason recorded; test both branches of the decision and the script's skip path.
- [x] 1.2 State the branch rule in the `continuous-integration` requirement and the runbook; close the duplicate candidates #503, #504, #505 and delete their bot branches.
