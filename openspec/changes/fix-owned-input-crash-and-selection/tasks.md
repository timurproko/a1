Implementation status: PR #287 merged at `c7977493dbe82c0802cfe7f393a7021932f02aaa` with all required CI checks successful. The user explicitly withheld acceptance because a large screenshot still fails the early 8 MiB check. Checked tasks below record completed initial work only; affected image-policy tasks are reopened and section 5 adds the background-preparation delta. No acceptance or archive is authorized. Former section 5 handoff tasks are now section 6 and must cover the amended behavior.

Corrective implementation status: the background worker, pending chips, and readiness/cancellation paths are implemented in the `async-image-paste` worktree. Build/typecheck and 251 focused contract, image, shell, viewport, lifecycle, and governance tests pass locally, including a standalone emitted-worker/codec smoke test on Windows. Task 5.3 awaits the added Windows/Linux/macOS CI matrix evidence; section 6 remains pending fresh CI, handoff, and user acceptance. No full local validation tier was run.

## 1. Reproduce the reported boundaries

- [x] 1.1 Add an editor-callback regression reproducing `owned-UI prompt image data is invalid` with a synthetic over-limit canonical image; verify it exercises the real submit callback and detects the escaped rejection before the fix without needing the user's private image.
- [x] 1.2 Add a real fullscreen-runtime empty-transcript drag regression; verify it detects fallback selection paint and clipboard/copy feedback before the fix rather than only checking the viewport controller's return value.
- [x] 1.3 Add isolated child-process fixtures for fatal callback rejection, uncaught exception, failed/stalled disposal, and abrupt UI termination; verify fixtures cannot affect user-owned terminals or process trees.

## 2. Make image submission recoverable

- [x] 2.1 Reconcile the previously shared final eight-attachment/8,388,608-byte canonical-base64 limits with distinct source intake and preparation policy; verify original eligible images above the final limit reach preparation, prepared output still obeys final assertions, padded/unpadded boundaries are correct, and pending/failed chips count toward eight slots.
- [x] 2.2 Extend existing trusted diagnostics to distinguish source bytes/pixels, final output size, count, unsupported conversion, codec failure, and timeout without image data; verify ordinary large screenshots are resized instead of told to shrink manually, and malformed image input preserves text-fallback or unchanged-prompt behavior.
- [x] 2.3 Guard prompt preparation and execution at the owned callback boundary, distinguishing local rejection from uncertain dispatch; verify both synchronous throws and rejected promises produce in-application feedback without unhandled rejection, partial dispatch, or automatic retry.
- [x] 2.4 Preserve rejected pre-dispatch draft text and chip references with editor-revision protection and an explicit recovery path when newer input exists; verify correction/resubmission dispatches once and late failure does not overwrite a newer draft.
- [x] 2.5 Route steering, follow-ups, and compaction-queued submissions through the same validation/recovery path; verify an invalid queued item is reported once, remains recoverable, and does not block or discard valid queued work.
- [x] 2.6 Rerun the reported-boundary shell tests against the prepared-image path; verify an already-small supported image remains byte-identical, a real large screenshot reaches the agent as compliant prepared bytes, and a genuinely rejected source is followed by usable typing and a successful text turn.

## 3. Keep empty-screen selection under A1 ownership

- [x] 3.1 Add complete pointer-sequence suppression for rejected selection starts on the default bare-A1 screen, including no-frame/empty and non-selectable margins; verify press, motion, no-button motion, and release never start fallback selection or copying.
- [x] 3.2 Reset suppression on release, reset/replacement, ownership handoff, and disposal while preserving it across arriving content; verify fresh post-release content drags still use A1 selection.
- [x] 3.3 Cover mixed pointer/keyboard chunks and modal/replacement-surface bypass; verify keyboard bytes arrive once in order and settings/editor controls retain their pointer behavior.
- [x] 3.4 Extend fullscreen integration coverage for empty and populated sessions; verify no fallback highlight, OSC 52 copy, or `Copied!` on suppressed drags, and unchanged A1 transcript/editor selection, wheel, scrollbar, links, and `a1 pi` selection.

## 4. Make terminal restoration crash-safe

- [x] 4.1 Audit modes enabled by A1 and the pinned terminal runtime and define one reusable bounded emergency-restoration path; verify simulated terminal state disables all owned mouse/paste/keyboard modes and restores cursor, wrapping, scroll margins, synchronized output, and alternate-screen state.
- [x] 4.2 Make ordinary shell/application disposal attempt terminal stop despite rendering, backend-unbind, or disposer failures; verify throwing/stalled disposal cannot leave restoration unattempted and normal exit output is not duplicated.
- [x] 4.3 Install a once-only fatal boundary before bare-A1 terminal activation with a bounded teardown deadline and nonzero exit; verify isolated uncaught-exception/rejection fixtures stop normal dispatch, attempt restoration, and cannot recursively re-enter fatal handling.
- [x] 4.4 Add surviving-owned-launch fallback restoration after abnormal child completion; verify reset writes occur after the final child output, unsuccessful status is preserved, and unrelated transparent commands and comparison selection policy remain untouched.
- [x] 4.5 Retain sanitized fatal records with a 16 KiB per-record cap and latest-ten retention and emit a short post-restoration diagnostic; verify useful release/runtime/stack-location fields, absence of prompt/image/credential/input payloads, and safe behavior on unwritable storage/output.
- [x] 4.6 Complete subprocess lifecycle coverage for abrupt child death, failed/stalled cleanup, duplicate cleanup, and normal exit; verify bounded completion, restored modes, correct outcomes, and no duplicated final transcript/resume hint.

## 5. Prepare images in the background with immediate paste feedback

- [x] 5.1 Add reproducible real-image fixture generation and a regression for a valid screenshot above 8 MiB encoded but below source safety limits; verify the merged early-rejection path fails the test and include small byte-identical, text/line, transparent/oriented, high-entropy, and 4K/8K cases without private image data.
- [x] 5.2 Implement source-byte/header/pixel/dimension preflight distinct from final policy in the background acquisition/preparation boundary; verify the 20 MiB compressed-source, 40-million-pixel, and 32,768-dimension limits below/at/above boundaries, malformed inputs, and no full decode for rejected headers.
- [ ] 5.3 Declare and package the worker and pinned codec assets without synchronous UI-startup imports or global filesystem patches; verify a real cold worker can load/decode/encode from development and packaged Windows/macOS/Linux layouts and a missing asset produces a recoverable failure.
- [x] 5.4 Implement the bounded screenshot-quality algorithm and small-image pass-through; verify exact bytes/MIME for pass-through, PNG preference, actual output MIME, orientation/transparency handling, aspect ratio/no upscaling, ordered JPEG candidates, bounded scale/quality attempts, the readability floor, and effective encoded/decoded/dimension limits using real fixtures.
- [x] 5.5 Reserve a stable pending marker and request render before clipboard read/worker initialization, then resolve it in place; verify real paste callbacks acknowledge unresolved cold/warm reads, preserve selection/caret and later typing, and correctly handle text/path fallback, empty results, and malformed image results without phantom chips.
- [x] 5.6 Move source-sized scans/base64 work and decode/resize/encode off the UI event loop, auditing native clipboard acquisition and preserving copy-write-before-read ordering; verify typing, caret movement, selection, streaming, and heartbeat progress while acquisition is stalled and a real background worker is CPU-busy, with no synchronous compression subprocess or repeated normalization during rendering/submission inspection.
- [x] 5.7 Bound acquisition/processing and retained data to one active conversion, eight source jobs, 160 MiB aggregate compressed sources, and a 15-second action-to-settlement deadline; verify paste storms, waiting-submission queue pressure, out-of-order completion, payload identity/order, allocation release, overload diagnostics, timeout termination, and a successful later paste after worker failure.
- [x] 5.8 Add preparing/ready/failed/canceled chip states with stable job/session identity and live-reference ownership; verify chip deletion, retry, newer input, session reset/replacement, and late completion cannot resurrect content or mutate another session, and no unbounded raw-image history is retained.
- [x] 5.9 Gate ordinary, steering, follow-up, and compaction-queued dispatch on all referenced images being ready; verify Enter-before-ready creates a visible cancellable snapshot while new typing continues, duplicate submit callbacks coalesce, model changes trigger current-limit validation, preparation failure never sends a partial prompt, and unrelated valid queued work remains usable.
- [x] 5.10 Integrate worker/acquisition cancellation with normal and fatal lifecycle cleanup; verify canceled waiting submissions never dispatch, unreferenced jobs terminate, no worker/subprocess survives owned disposal, and stalled image work cannot delay the existing terminal-restoration deadline.
- [x] 5.11 Run cross-boundary responsiveness and quality validation through the real editor/session path; verify pending-to-ready/failed state, exact final dispatch payload, no escaped rejection, no selection/terminal-cleanup regression, and deterministic event-order coverage rather than relying solely on mocked promises or flaky CI wall-clock assertions.

## 6. Integration and user acceptance

- [ ] 6.1 After this amendment merges and implementation is explicitly authorized, submit a fresh implementation pull request citing the revised change; verify required CI, packaged worker/codec coverage, resolved change-caused failures, and no installed Pi mutation or unrelated paths. PR #287's green CI does not satisfy this amended gate.
- [ ] 6.2 Provide the exact built worktree/commit and color-preserving `./scripts/dev` handoff for the corrected candidate; verify the checklist covers cold/warm large-image paste, immediate feedback, Enter-before-ready, cancellation, repeated pastes while typing/streaming, small-image fidelity, oversized-source rejection, draft recovery, and existing selection/terminal checks.
- [ ] 6.3 Obtain user-run Windows Terminal/Git Bash acceptance using the previously rejected screenshot and synthetic text-quality fixtures; record cold/warm acknowledgment latency against the 100 ms target and observed input/selection/streaming responsiveness, verify readable prepared output and successful submission, and confirm normal/controlled-abnormal terminal exit plus unchanged `./scripts/dev pi` selection.
- [ ] 6.4 Record explicit acceptance and merge authorization before manually merging the corrective code; verify the new implementation PR is merged and all amended tasks are complete before any acceptance/spec-sync/archive follow-up or retained-worktree cleanup.
