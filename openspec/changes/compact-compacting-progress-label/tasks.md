## 1. Compact the progress label

- [x] 1.1 Remove the space between the semantic compaction message and its parenthesized measured percentage in the bare-A1 custom-viewport status composer while preserving no-progress and pinned-route behavior.
- [x] 1.2 Update focused shell-component coverage for zero, intermediate, and maximum measured percentages, the no-percentage fallback, and comparison-route isolation.

## 2. Validate and hand off

- [x] 2.1 Run focused shell-component tests, typechecking, and strict OpenSpec validation; disposition any failures without weakening the requested behavior.
- [x] 2.2 Build the reconciled candidate and provide a color-preserving manual test through `./scripts/dev`.
