## 1. Authorization and failure ledger

- [ ] 1.1 After separate plan approval and implementation request, resume this same worktree/branch/draft PR; record authorization and verify merged #402 ancestry, intact version-2 linkage, disabled auto-merge, and no unrelated modifications.
- [ ] 1.2 Record all final outcomes of run 34880029189, exact jobs/source/assertions, and the Windows session-shell/release-command diagnostic findings; distinguish confirmed fixture routing/path defects from unproven cleanup or runtime causes, and obtain a reviewed refinement before corrective edits outside this plan's fixture surface.

## 2. Filesystem-identity regression

- [ ] 2.1 Reproduce the lexical-versus-canonical cwd failure with a controlled alias/junction and spaces, retain the original native macOS evidence, and verify a distinct-directory negative control detects an incorrectly launched child without sleeps or weakened argument/environment checks.
- [ ] 2.2 Correct the expected directory identity using independent filesystem resolution while passing the requested alias unchanged; verify ordinary and aliased directories pass, wrong-directory controls fail, and exact argv and npm sanitation remain asserted on native lanes.

## 3. Hermetic clipboard acquisition

- [ ] 3.1 Correct source fixture interception at the actual native/command I/O boundary and verify exact text, empty null, denied error, and first-backend failure with successful Linux fallback while the real source paste helper and worker run.
- [ ] 3.2 Apply equivalent fixture semantics to emitted-JavaScript helper execution after build; verify exact packaged text, empty/denied/fallback controls, real classification/image workers, and existing payload-inventory assertions without a TypeScript-loader substitute.
- [ ] 3.3 Add bounded backend/argument observations and unexpected-operation negative controls; verify no real host clipboard access or server dependency, no production platform spoofing, unrelated process behavior remains intact, and the real blocked-command descendant cancellation regression still executes.

## 4. Failure-path executor lifetime

- [ ] 4.1 Add a controlled early-failure/unfinished-child reproduction to establish whether incomplete teardown explains cross-test admission contamination; verify explicit lifecycle coordination and preserve the primary failure without time-based success or a capacity change.
- [ ] 4.2 Make affected single and batch clipboard tests cancel and await owned stopped boundaries in failure-safe cleanup; verify eight admitted requests, ninth rejection, 12 cycles, 16 recovery forks, conversion serialization, and exited/disconnected child assertions remain intact, including after an intentionally failing scenario.

## 5. Native candidate validation

- [ ] 5.1 Pass locally available focused source/build-first emitted tests on Windows Node 22/24, repository typecheck/build, applicable documentation/architecture/provenance checks, strict OpenSpec, and whitespace checks; record identities without claiming Unix evidence and confirm no production, dependency, workflow, suite, timeout, retry, baseline, or budget change.
- [ ] 5.2 Push the completed implementation in this same PR and mark it ready before normal PR CI; record passing exact-head ordinary validation with no ordinary draft dispatch or duplicate CI campaign.
- [ ] 5.3 Obtain passing existing Full regression on all four native lanes, verifying the focused source/emitted cases on actual Linux/macOS as well as Windows and recording exact source/run/jobs and all required stages; retain every failed run and block merge for unresolved Windows shell/release-command or other findings, seeking approved corrective refinement rather than skipping checks or retrying into green.

## 6. Accepted integration and nightly recovery

- [ ] 6.1 Provide exact candidate and focused maintainer commands, record actual implementation validation and explicit manual merge authorization, and verify integration with auto-merge disabled; do not represent this checkpoint as completed nightly acceptance.
- [ ] 6.2 Verify the real scheduled mode=nightly Release run fully validates a newer numbered merged package containing the repairs on Windows Node 22/24, Linux Node 24, and macOS Node 24, with successful aggregate publication/immutable verification; record source, merged PRs, version, digest/integrity, jobs, registry identity, and applicable next tag without mutating published bytes or substituting reduced-scope development publication.
- [ ] 6.3 Record actual final recovery acceptance and reviewed canonical-spec baseline, reconcile this follow-up with #402 and earlier obligations individually, and verify no unfinished substantive task is silently completed or archived; keep retained worktrees until archive integration.

## 7. Mechanical archive preparation

- [ ] 7.1 Record verified implementation acceptance and merge evidence for archive preparation.
- [ ] 7.2 Stage and verify delta synchronization and the archive move in an OpenSpec-only candidate.
