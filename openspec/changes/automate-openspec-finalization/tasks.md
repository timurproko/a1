## 1. Repeatable Finalization

- [ ] 1.1 Extend `prepareSinglePrDelivery` in `scripts/governance/openspec-delivery-finalization.mjs` with a re-finalization path: when the body is already finalized and verification fails on target, content, acceptance, or gap drift, restore the active form in the sandbox from the archive (move the archive back to the active path, drop `acceptance.md`, reset each synchronized capability spec to the supplied target bytes), then run the ordinary finalization with the archive's existing date; keep `already-finalized` for a valid current head and keep every existing fail-closed code.
- [ ] 1.2 Give `finalize-openspec-delivery.mjs` the same behavior locally by reading target spec bytes with `git show <target>:<path>`, and report the disposition (`finalized`, `refinalized`, `already-finalized`, `would-finalize`, `would-refinalize`).
- [ ] 1.3 Add fixtures to `openspec-delivery-finalization.test.ts`: re-finalization after an archived `tasks.md` edit yields a stable archive path and a manifest whose digests match the new bytes; re-finalization after a target advance rebuilds the synchronized spec from the new target; running the operation on its own output is a no-op; incomplete tasks and a missing acceptance list still fail closed.

## 2. Trusted Publication

- [ ] 2.1 Add `scripts/governance/openspec-finalization-publication.mjs` with `reconcileFinalization({ reader, publisher, tool, number })`: refuse a closed, draft, forked, non-`develop`, or non-version-3 PR; fetch the `develop` tip and the head into a scratch clone restricted to `openspec/`; classify the head as active-current, finalized-current, finalized-drifted, or behind; produce the restore, merge, and finalize commits the design lists; push with `--force-with-lease` against the head that was read; then `PATCH` the body fence only when the body is unchanged.
- [ ] 2.2 Extend the publisher's route guard in `openspec-archive-publication.mjs` to allow the same-repository head-ref push only for a commit that descends from the read head and whose diff against it satisfies `assertArchiveDiff` for the change's paths; keep every other route as it is.
- [ ] 2.3 Add `scripts/governance/publish-openspec-finalization.mjs` (`--pr <n>`, `--dry-run`) that requires `GITHUB_ACTIONS` with a `pull_request_target` event unless dry-running, mints the App identity from the existing secrets, writes a step summary naming the disposition or the failure code, and exits non-zero without pushing on any failure.
- [ ] 2.4 Add `openspec-finalization-publication.test.ts` with a fake reader, publisher, and Git: each of the four states produces the expected commits and body; a lease failure or concurrent body edit pushes nothing further and reports retry; a merge conflict reports `finalization-merge-conflict`; a draft or forked PR is refused; the push route guard rejects a diff outside the change's paths.

## 3. Workflows

- [ ] 3.1 Add `.github/workflows/openspec-finalization.yml`: `pull_request_target` on `develop` for `synchronize`, `ready_for_review`, `reopened`, and `edited`, skipped for drafts and merged PRs; per-PR concurrency without cancellation; default-branch checkout with `persist-credentials: false`; `npm ci --ignore-scripts`; the publication command with the App secrets; least-privilege `contents: read`, `pull-requests: read` for the token.
- [ ] 3.2 In the `delivery` job of `.github/workflows/ci.yml`, when trusted validation reports `delivery-not-finalized` for a non-draft version-3 head, write a step summary that automated finalization is pending and link the latest `OpenSpec finalization` run for the PR; keep the job failing so the unfinalized head stays unmergeable.
- [ ] 3.3 Pin the new workflow in `openspec-archive-workflow.test.ts` (or a sibling test): triggers, draft skip, default-branch checkout, secret names, concurrency shape, and the absence of any PR-head script execution; pin the `delivery` summary text in the workflow fixtures.

## 4. Governance, Documentation, And Evidence

- [ ] 4.1 Add the workflow to `config/github-repository-governance.json` with authority `single-pr-finalization-publication`, derive that authority in `github-repository-governance.mjs`, and update `github-repository-governance.test.ts`.
- [ ] 4.2 Update `openspec/config.yaml`, `.agents/skills/change-delivery/SKILL.md`, and `docs/openspec-archive-automation.md`: finalization happens on the ready PR by trusted automation, the local command is optional and produces the same result, developers pull before pushing, and the `Revert "docs(openspec): finalize"` step is gone; update `change-delivery-guidance.test.ts` accordingly.
- [ ] 4.3 Run the finalization, publication, workflow, guidance, and governance tests plus typechecking and the governance commands, and record the commands and outcomes; record as post-merge evidence the first PR finalized by the workflow (its run, the finalization commit, the exact-head validation run, and the manual merge) and the absence of `Revert "docs(openspec)` commits on PRs merged after it.
