## 1. Establish Impact Selection and Baseline Evidence

- [x] 1.1 Preserve machine-readable diagnostic evidence for PR #201's ordinary, rendering, setup, matrix-launch, and documentation-scan costs plus the representative pre-rendering run; verify a focused evidence test distinguishes structural counts from wall-clock diagnostics.
- [x] 1.2 Define a versioned bounded validation-selection result containing base/head identity, complete name-status entries, ordinary scopes, rendering tier/reasons/fallbacks, documentation destinations, and classifier timing; verify malformed, oversized, and incomplete results are rejected.
- [x] 1.3 Implement complete merge-base-to-head change collection for additions, modifications, copies, deletions, rename sources, and rename destinations; verify synthetic Git repositories cover every status and paths containing spaces or platform separators.
- [x] 1.4 Build TypeScript dependency reachability for declared rendering entries at both base and head revisions; verify direct, transitive, barrel, deleted, and renamed dependencies produce bounded reason chains.
- [x] 1.5 Add the reviewed rendering invalidator and full-critical inventories for dynamic resources, module/package identity, terminal/runtime infrastructure, evidence infrastructure, and classifier inputs; verify each entry selects `full` and an unrelated foundation change selects `none`.
- [x] 1.6 Implement `none`, `smoke`, and `full` rendering classification with conservative fallback for missing history, unresolved imports, unsupported relevant inputs, and classifier errors; verify every fallback reports a reason and never produces `none`.
- [x] 1.7 Make local selection include merge-base commits plus staged, unstaged, renamed, and policy-relevant untracked files without mutating the worktree; verify focused temporary-repository tests cover a clean branch and each local state.

## 2. Make Documentation Governance Incremental and Non-Duplicative

- [x] 2.1 Separate fixture-based documentation policy tests from live-repository enforcement while preserving source-role, public-contract, synchronized provenance, comment-hygiene, diagnostic-ordering, and command-surface coverage; verify the focused policy suite passes without loading every tracked repository source.
- [x] 2.2 Extend the documentation command with explicit changed-file and full-review modes that consume validated selector input; verify invalid modes, missing inputs, and out-of-repository paths fail closed.
- [x] 2.3 Select exactly modified, added, copied, and renamed-to policy-relevant destinations for development checks and exclude deletion-only sources; verify synthetic name-status fixtures prove the exact selected list.
- [x] 2.4 Inspect each selected file in full and resolve only bounded owner/export metadata needed for its obligations; verify changed classes, comments, source roles, synchronized files, and barrel/export relationships produce the same applicable diagnostics as the policy rules.
- [x] 2.5 Remove or convert the Vitest live-repository baseline case so one ordinary validation tier performs zero complete documentation scans and one authoritative changed-file command; verify an orchestration test counts scans and still rejects a changed-file violation.
- [x] 2.6 Keep one explicit complete documentation command that scans every tracked policy-relevant file and reports file count, diagnostics, and duration; verify it detects a seeded violation in a temporary complete repository.
- [x] 2.7 Update local fast and complete validation composition so fast uses changed-file documentation once and complete validation uses the full review once; verify generated validation plans contain no duplicate documentation command IDs or equivalent scans.

## 3. Modularize and Deduplicate Rendering Evidence

- [x] 3.1 Declare rendering smoke and full scopes in the validation-suite authority, with smoke covering independent producers, both comparison modes, replay, semantic parity, synchronization classification, status/initial presentation, streamed prose, and followed movement; verify plan tests assert the exact contract inventory.
- [x] 3.2 Refactor rendering orchestration to capture a keyed workload/producer/mode result map once per selected gate; verify immutable captured results can feed semantic, paint, dock, parity, determinism, and budget evaluators.
- [x] 3.3 Consolidate matrix and logical-damage assertions that currently rerun equivalent workloads, retaining only the deliberate second capture needed to test determinism; verify each ordinary workload key is captured once and the determinism key exactly twice.
- [x] 3.4 Preserve separate short-lived processes for bare A1, `a1 pi`, and pinned Pi and retain serialized cold-worker behavior on Windows; verify process identities remain independent and no live shell, terminal, profile, or module state is reused across producer results.
- [x] 3.5 Preserve all eight full rendering workloads, producer failure/timeout/malformed-output checks, terminal-paint classification, comparison parity, and logical-damage budgets; verify the full scope's semantic coverage matches the pre-optimization inventory.
- [x] 3.6 Verify the smoke scope detects seeded semantic-parity, full-screen-clear, stable-row-damage, blank-frame, and stale-final-state regressions in its representative workloads.
- [x] 3.7 Add structural performance evidence for matrix captures and producer launches; verify full evidence removes the 24 equivalent repeated launches identified in PR #201 without relying on a wall-clock pass threshold.

## 4. Compose Modular Pull-Request and Nightly Workflows

- [x] 4.1 Remove rendering and live documentation enforcement from the ordinary fast scope while retaining typecheck, architecture, ordinary unit/contract/governance tests, and dist integration; verify the generated ordinary plan contains neither a rendering invocation nor a complete documentation scan.
- [x] 4.2 Make the pull-request change-detection job invoke the repository selector once and expose bounded rendering tier, documentation-input, head identity, and selection-artifact outputs; verify workflow governance rejects hard-coded duplicate selection logic.
- [x] 4.3 Add a platform-independent changed-file documentation job that runs only when selector inputs exist and otherwise records an explicit no-input result; verify violations fail the job and no-input is distinguishable from missing execution.
- [x] 4.4 Add a Windows rendering job that selects smoke or full from the classifier and runs in parallel with ordinary validation; verify `none` skips it, while smoke/full cannot report success without the exact selected scope outcome.
- [x] 4.5 Preserve the existing process-containment matrix and ordinary Windows validation as independent modular jobs; verify neither becomes conditional on rendering or documentation selection.
- [x] 4.6 Update the single required aggregate check to bind classifier and modular outcomes to the current head and reject stale, missing, failed, or unexpectedly skipped ordinary, documentation, rendering, or containment results; verify every result combination in focused workflow tests.
- [x] 4.7 Add one platform-independent full documentation job to scheduled nightly validation at the exact authoritative `origin/develop` commit and gate nightly completion on it; verify the four release platform jobs perform zero complete documentation scans.
- [x] 4.8 Preserve one full documentation review for explicit complete local/manual regression and applicable release paths without multiplying it across platform matrices; verify full validation plans and workflow policy tests count exactly one review per requested source.
- [x] 4.9 Keep preview exact-package and stable complete-suite contracts unchanged, including every supported platform and immutable package digest; verify release validation ownership tests pass after modular composition.

## 5. Auditability, Governance, and Quality Gates

- [x] 5.1 Emit machine-readable selection and outcome artifacts plus concise workflow summaries with changed inputs, dependency/invalidator reasons, fallbacks, setup time, gate time, matrix captures, producer launches, documentation file count, scan count, and per-scope result; verify bounded formatting and artifact schemas.
- [x] 5.2 Add repository-governance tests proving rendering classification for direct, transitive, unrelated, added, deleted, renamed, dynamic-invalidator, package-identity, classifier-change, and failure cases; verify the production classifier passes the complete table.
- [x] 5.3 Add documentation-selection governance tests proving changed/new/renamed destinations, deletion exclusion, policy-relevant extension filtering, bounded metadata resolution, ordinary zero-full-scan behavior, and nightly exactly-one-full-scan behavior.
- [x] 5.4 Add validation-plan and workflow governance tests proving workload deduplication, smoke/full coverage, parallel modular composition, current-head aggregation, and no accidental branch-protection check-name change.
- [x] 5.5 Update maintained validation architecture/runbook documentation with the selector schema, tier criteria, conservative fallback, nightly repair procedure, explicit full-review command, and timing interpretation; verify documentation governance and source-role checks pass.
- [x] 5.6 Run focused classifier, documentation-policy, rendering-evidence, validation-plan, workflow-governance, architecture, and release-ownership tests needed for debugging; verify build, typecheck, strict OpenSpec validation, and diff checks pass before push.

## 6. CI Evidence and Integration

- [ ] 6.1 Push the implementation branch and verify required CI proves an unrelated fixture selects no rendering, a rendered-shell fixture selects smoke, a rendering-core/classifier fixture selects full, and each changed-file documentation fixture selects the expected destinations.
- [ ] 6.2 Compare CI structural outcomes with the accepted baseline: ordinary validation has zero rendering launches and zero full documentation scans, smoke has representative coverage, full retains every workload with no equivalent repeated matrix, and nightly configuration has one full documentation review.
- [ ] 6.3 Report wall-clock setup and repository-gate timing separately for the implementation PR, treat runner/cache variance as diagnostic, and investigate any repository-gate regression not explained by selected contract coverage.
- [ ] 6.4 Provide the exact implementation worktree, branch/commit, selector inspection commands, focused changed-documentation command, smoke/full plan commands, expected selections, and known conservative fallbacks for maintainer review.
- [ ] 6.5 Keep the code/operational pull request open after CI; record maintainer acceptance and explicit merge authorization before manual integration, then hand off acceptance recording and archive as a specification-only follow-up.
