## 1. Approval and regression evidence

- [ ] 1.1 After explicit plan approval and an implementation request, resume this draft in the same worktree, branch, history, and PR; record that authorization and verify the version-2 change linkage, base, and absence of unrelated changes.
- [ ] 1.2 Record the original macOS source/run/job and both failing color cases, compare the owned builder with pinned canonicalization and option construction, and verify the evidence distinguishes the trust-path defect from the already-merged scoped-model correction.

## 2. Canonical trust context and action semantics

- [ ] 2.1 Add focused real directory-alias fixtures with different alias/target parents, ordinary paths, filesystem roots, missing paths, and injected real-path failures; verify canonical option expectations detect the uncorrected builder and fixtures isolate/restore filesystem state without silent skips.
- [ ] 2.2 Correct only the owned trust-context path boundary (extracting an owned engine helper if useful); verify project/parent saved paths and updates use the canonical target, the cwd heading remains unchanged, root exclusion and fallback match pinned behavior, and no private production import or unrelated cwd change is introduced.
- [ ] 2.3 Cover direct trusted/denied decisions, inherited parent and higher-ancestor decisions, selected/checkmarked state, explicit project and parent saves, and cancellation; verify parent save clears the canonical child override without trusting the alias parent or changing unrelated entries, and saves never change active-session trust or load project resources.

## 3. Independent and native validation

- [ ] 3.1 Extend the independent command-outcome inputs/captures for equivalent alias topology, saved trust seeds, and action effects; verify exact pinned-versus-owned rows and persistence across 80/28 columns, dark/light, padding 0/1, and both color modes, retaining all existing cases and rejecting negative path/ANSI/wrapping mutations.
- [ ] 3.2 Pass strict OpenSpec validation and applicable required implementation CI; record current-head check URLs and verify architecture/provenance gates pass with dependencies, source ledger, source-synchronized selector, rendered baselines, and approved exceptions unchanged.
- [ ] 3.3 Obtain native macOS Node 24, Linux Node 24, and Windows Node 22/24 Full regression results for the candidate; record source and all four lane URLs and verify the complete command-outcome gate passes without new skips, retries, timeouts, or exception changes, isolating any unrelated failure instead of claiming success.

## 4. Final review and separate legacy obligations

- [ ] 4.1 Provide the exact candidate worktree/commit and build-first color-preserving launch with an isolated profile and alias cwd; verify the handoff covers `/trust` canonical parent labels, 80-column/narrow wrapping, saved/inherited state, trust/deny/parent actions, and cancellation, with known gaps explicit.
- [ ] 4.2 Document the post-merge handoff to the existing nightly/scoped-model validation tasks; verify it requires a newly numbered merged package, source/version/digest and all four full-nightly-equivalent lane outcomes, and does not mark those separate changes accepted or complete from this candidate's evidence.
- [ ] 4.3 Record actual maintainer review outcomes and reviewed canonical-spec baseline for the exact final head; verify an authorized `openspec-acceptance` comment represents that review, preceding substantive tasks are complete, and code auto-merge remains disabled, with explicit manual merge authorization kept separate.

## 5. Mechanical archive preparation

- [ ] 5.1 Record verified implementation acceptance and merge evidence for archive preparation.
- [ ] 5.2 Stage and verify delta synchronization and the archive move in an OpenSpec-only candidate.
