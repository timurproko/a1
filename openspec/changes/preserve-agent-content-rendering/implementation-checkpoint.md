# Continuation after PR #370 — implementation evidence, not acceptance

Base: merged PR #370, `421ceb5042d9da1d69a787ceb6f809ce483f66fb`. Worktree: `D:/Git/a1/.worktrees/complete-content-rendering`. Intended branch: `fix/complete-content-rendering`. The user requested the remaining tasks and explicitly approved the minimal attributed image-lifecycle adaptation after its failing baseline.

## Verified work

- **3.3:** Three baseline regressions failed: JPEG-to-PNG replacement retained old pixels, JPEG replacement disagreed with a fresh independent pinned component, and conversion failure silently hid the image. A fourth independent-pinned test locates the index-cache defect outside owned delivery and root caching. Current-source conversion ownership repairs these paths without replacing invocation identity, original renderer inputs, or extension state. Current sources share conversions, work is serial per mount, obsolete work is discarded, and pending/failure/size fallbacks remain visible. No private access or installed-package change is used.
- Source provenance and the complete adapter boundary are recorded in `docs/architecture/tool-image-presentation.md` and the source-port ledger. Actual public built-in definitions remain the renderers. Independent comparisons cover the seven built-ins, generic fallback, extension default/self shells, complete original data, lifecycle, expansion, and widths 40/80/192.
- An additional root/runtime regression exposed Kitty payloads being counted as visible text. Complete APC payloads now survive width/truncation handling, never enter copied text, and are not replayed as styling by rail overlays. Ordinary text width enforcement is still tested around image controls. Real scheduled image publication now reaches the terminal recorder.
- **4.3–4.4:** Real scheduled tests cover height changes with keyboard or stream presentation pending, semantic-revision stability, document extent, follow/detach behavior, live-tail metadata, current link/copy geometry, bottom-control hit testing, bounded invalidation, off-screen cache reuse, and removal/replacement/disposal. Reentrant invalidation during renderer construction is guarded.
- **6.1–6.3:** The existing isolated producer now has independent actual pinned tool components and separate assistant surfaces, replacing its former text-only tool approximation and single-assistant reuse. New synthetic workloads at 40×54 and 192×54 complete arguments before concurrent tools, coalesce 64 accumulated updates, retain authoritative edit details, restate declarations, emit a second assistant message, retain prior history at run end, delay settlement, and accept keyboard input.
- Scheduled producers use real timers, nextTick, and the pinned monotonic-clock scheduling chain; they do not force per-event paints. The historical stepped diagnostic mode remains. Bounded capture observes actual root composition rather than probing components and accidentally repairing their caches. It correlates block IDs, semantic/presentation revisions, document range, frame/write boundary and fresh write-local damage decisions with replayed cells. Only named synthetic workloads can generate this evidence; nothing is added to normal UI telemetry.
- **6.5:** Boundary-level negative controls remove required live output, prior history, final results and authoritative diff text, and insert blank or stale cached presentation before an unchanged correct final snapshot. The corresponding intermediate content gates reject them. These are deliberately corrupted synthetic boundary observations, not modifications to installed Pi or claims that source mutation testing was performed.

## Validation

- 410 tests passed in 14 focused suites, including the existing 218-test session shell, attachment/data limits, root presentation, public renderer parity, runtime width checks, neutral viewport/text/spans, and validation-tier governance.
- The two scheduled producer cases passed for separate owned and pinned processes. Each capture replays with synchronization honored and ignored, checks final text/cursor equality and intermediate required content, and bounds ordinary paint without new display clears.
- Rendering and input smoke checks passed. Updating the pinned producer initially exposed a test-fixture status mismatch for user `message_start`; its event-state bookkeeping was corrected without changing production semantics or weakening the parity assertion.
- Build, typecheck, architecture/product identity, source provenance, and strict OpenSpec validation passed. Final changed-file documentation and exact-head CI remain handoff gates.
- The optional focused `rendering-budgets.test.ts` run hit the local command's **300-second timeout** before producing a result. It is not counted as passing or as an assertion failure. No `test:fast`, `test:full`, or `test:release` local tier was run. The isolated CI rendering scopes now include the new producer cases; ordinary fast validation excludes that expensive process/replay suite.

## Still open

**22/28 tasks are checked.** Tasks 4.5 and 6.4 remain open for the complete presentation/budget audit and broader acceptance matrix; their passing subsets do not establish full completion. Tasks 7.1–7.4 remain open for exact-head automated validation, complete runnable handoff, user-controlled Windows Terminal content review, and recorded acceptance.

In particular, the image byte comparison and fitting scheduled-image case are not physical evidence for partially clipped images, all terminal capabilities, or all viewport interactions. The original disappearing/flashing-content screenshots still lack an identified application artifact and category. No new Windows Terminal verdict is invented.

Issue #353 remains independently unresolved, including the retained expected wrapped-URL failure. This checkpoint changes neither link policy nor ordinary terminal erase policy. Keep its implementation PR open; successful CI or merging a checkpoint alone must not be reported as completed content acceptance.
