Content preservation and stable presentation close to pinned Pi are the priority. The approved scope split moves link-only work to [issue #353](https://github.com/timurproko/a1/issues/353); see section 5. Retained task IDs support reconciliation with the isolated implementation checkpoint, but progress must be checked against the revised descriptions rather than copied blindly.

## 1. Establish Failing Evidence Before Remediation

- [x] 1.1 Recheck the accepted implementation base and trace pinned message, tool, run, and settlement ordering; verify a source-referenced event fixture completes assistant arguments before execution and preserves intervening modal/editor/selection changes.
- [x] 1.2 Convert the argument-finality and run-local transcript-replacement probes into deterministic engine/shell regressions; verify the baseline rejects required live output and loses earlier history at the intermediate checkpoint, not merely at final settlement.
- [x] 1.3 Add component-to-root regressions for missing result details, unchanged-semantic-revision asynchronous invalidation, and attachments arriving after the call header; verify the first two reproduce the observed failures and record the attachment case's actual baseline without assuming it fails.
- [x] 1.5 Record known runtime and Windows Terminal facts and a bounded physical baseline for disappearing/flashing content, distinguishing legitimate pinned reflow; verify the record includes known build, terminal version, geometry, settings, raw-write context, content category, and explicitly unknown or unreproduced cases without publishing private transcript data or waiting for host-link cleanup.

## 2. Repair Execution State and Transcript Continuity

- [x] 2.1 Separate argument completion from execution disposition in owned block state and its validated component mapping; verify production-ordered start/update/end and abort/error cases keep one invocation surface and show live output after arguments finish.
- [x] 2.2 Update engine and shell stale-update guards to use the correct lifecycle phase plus revision/generation; verify genuine post-execution partials stay rejected while valid running updates survive queued and full-view paths.
- [x] 2.3 Merge repeated message/turn call declarations without downgrading final results; verify serial/parallel tool fixtures retain result data, errors, arguments, attachments, identity, and order without intermediate argument-only replacement.
- [x] 2.4 Distinguish run-local completion collections from full session-authoritative replacement; verify prior history remains continuously present during delayed settlement, unchanged blocks keep identity, and explicit session/branch/compaction reconciliation still follows existing scope rules.
- [x] 2.5 Exercise bounded delivery across those corrected phases; verify same-invocation coalescing, independent concurrent blocks, protected outcomes, generation replacement, overload recovery, and final-state flush preserve the existing queue and responsiveness guarantees.

## 3. Preserve Complete Renderer Inputs

- [x] 3.1 Introduce or refine validated owned rendering payload fields separately from diagnostic summaries; verify supported arguments, text/content parts, partial/final details, and error state reach actual built-in and extension renderers without vendor types escaping the integration boundary.
- [x] 3.2 Restore authoritative edit-result details and prevent a late preview from overwriting a settled result; verify final diffs render when previews are missing, stale, or fail and compare styled output against the independent pinned edit renderer.
- [ ] 3.3 Preserve changing attachment references on existing tool surfaces through the existing resolver; verify late arrival, replacement, hidden/unavailable fallback, supported inline presentation, and disposal without losing invocation identity or duplicating assets.
- [ ] 3.4 Verify large partial and final renderer payloads stay within supported queue/asset limits without per-chunk serialization, arbitrary rendering-data truncation, or silent omission; include bounded fallback/error checks and unchanged text-only tool presentation.
- [x] 3.5 Audit touched payload and component adapters for duplicate reconstruction, competing lifecycle decisions, or text-only approximations; remove or consolidate only proven redundant/faulty paths using pinned public components first, and verify independent styled-row/lifecycle parity, attribution of any necessary source-derived adaptation, and retention of validation and ownership safeguards.

## 4. Make Presentation Invalidation Reach the Screen

- [x] 4.1 Replace no-op transcript-renderer notifications with a typed mounted-instance/session-scoped presentation callback; verify a delayed renderer update requests the real scheduler while semantic revisions, completion counts, and persisted content remain unchanged.
- [x] 4.2 Track presentation dirtiness through block rows, document layout, and pending viewport reuse; verify the root and next terminal frame show a same-semantic-revision asynchronous change without input, resize, or a new agent event.
- [ ] 4.3 Recompute dependent geometry for renderer height changes; verify document extent, follow/detach behavior, selection/copy mapping, live-tail metadata, link ranges, hit regions, and pinned dock remain consistent during pending stream/dock frames.
- [ ] 4.4 Guard old component callbacks and coalesce repeated invalidations; verify replacement/disposal/session races cannot repaint new surfaces, render loops do not occur, off-screen invalidations are lazy, and unaffected block caches remain reusable.
- [ ] 4.5 Audit touched presentation paths for redundant invalidation, stale reuse proofs, or competing layout authority; remove or consolidate only proven faulty/unnecessary paths, and verify current-content composition, stable-history work/paint budgets, existing input/modal/selection behavior, and absence of artificial blank or stale intermediate frames without adding ordinary-streaming full clears.

## 5. Deferred Link Work (Tracked Separately)

Former task 1.4 and tasks 5.1–5.5 are deferred to [issue #353](https://github.com/timurproko/a1/issues/353), not completed here. Their scope includes the wrapped-target baseline, shared pre-layout URL recognition and renderer integration, full targets and native occurrence identity across wrapping, and bounded cleanup/publication of discarded explicit-link state. The issue also retains the physical host-only ghost investigation and its independent acceptance. The original specification PR #348 preserves the detailed link plan for follow-up.

Existing link colors, labels, activation, semantic copy, conservative movement safeguards, and bounded cleanup remain non-regression constraints for this content stream. Preserve existing link counterexamples/probe evidence and identify known failures explicitly against #353; do not silently delete coverage or report a known failure as a fix. A newly introduced link regression still blocks this change.

## 6. Prove End-to-End Behavior Under Real Scheduling

- [ ] 6.1 Extend the existing producer harness with production-ordered multi-message/tool workloads and actual independent pinned renderers; verify ordinary and structured content/tool presentation and lifecycle parity outside documented A1 differences, without text-only approximations establishing parity or merging distinct assistant messages.
- [ ] 6.2 Add scheduled/burst cases without per-event forced renders alongside stepped diagnostic cases; verify delayed settlement, asynchronous preview completion, keyboard/pointer preemption, and final-state flush present current content without blank replacement or stale later frames.
- [ ] 6.3 Correlate bounded event/block, semantic/presentation revision, document range, write-local decision, and cell evidence; verify missing/flashing content is attributable to its earliest failed boundary and diagnostics do not leak into normal UI or include private payloads by default.
- [ ] 6.4 Exercise large geometry including 192 by 54 and narrow wrapping, long settled history, code, existing URL/file behavior, modal coverage, selection, and supported images; verify text/style/cursor correctness, no new link regressions, and existing stable-work/paint budgets in synchronization-honored and synchronization-ignored replay, identifying pre-existing #353 failures separately.
- [ ] 6.5 Seed rejected-live-output, run-local-replacement, discarded-details, stale-cache, and artificial blank/stale intermediate-frame regressions in test-only controls; verify corresponding content gates fail even when final text recovers, rather than merely recording lower frame or clear counts.

## 7. Validate the Exact Candidate and Record Acceptance

- [ ] 7.1 Complete focused contract/type, architecture/provenance, engine/component, viewport, scheduling, and rendering validation and obtain required CI for the exact implementation commit; verify recorded results cover corrected and simplified boundaries without changing the explicit Pi oracle or dependency pins.
- [ ] 7.2 Deliver the exact built candidate with worktree/commit, focused reproduction steps, and color-preserving development/comparison launch commands; verify the handoff directly exercises full tool ordering, earlier-history retention, authoritative diffs, late attachments, delayed renderer output, and ordinary streaming/input stability while identifying #353 as unresolved.
- [ ] 7.3 Obtain user-controlled Windows Terminal review of missing/flashing content on the exact candidate at the recorded environment and representative large/narrow sizes; verify all displayable blocks remain available without resize/reopen or unexplained A1-induced flashing and compare against pinned Pi outside documented A1 differences.
- [ ] 7.4 Record acceptance only after content correctness, stability, parity, required CI, and exact-candidate review pass; verify any remaining content omission, unexplained A1-induced flashing, or new link regression keeps this change incomplete, while pre-existing #353 defects remain separately open. Leave the implementation PR open until the user reports acceptance and explicitly authorizes merging; do not close the link issue as part of content completion.
