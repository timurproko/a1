## 1. Establish Failing Evidence Before Remediation

- [x] 1.1 Recheck the accepted implementation base and trace pinned message, tool, run, and settlement ordering; verify a source-referenced event fixture completes assistant arguments before execution and preserves intervening modal/editor/selection changes.
- [x] 1.2 Convert the argument-finality and run-local transcript-replacement probes into deterministic engine/shell regressions; verify the baseline rejects required live output and loses earlier history at the intermediate checkpoint, not merely at final settlement.
- [x] 1.3 Add component-to-root regressions for missing result details, unchanged-semantic-revision asynchronous invalidation, and attachments arriving after the call header; verify the first two reproduce the observed failures and record the attachment case's actual baseline without assuming it fails.
- [x] 1.4 Add narrow-width bare-URL, styled/explicit-link, and wrapped-file fixtures; verify the current bare-URL path exposes a truncated target or unlinked continuation while the file path preserves its full target.
- [x] 1.5 Identify the affected runtime and Windows Terminal environment and record a bounded physical baseline for disappearing content and URL/file/host-only hover cases; verify the record includes build, terminal version, geometry, settings, raw-write context, content category, and explicitly unreproduced cases without publishing private transcript data. See `baseline.md` for the identified protocol reproduction and the original-content cases that remain unverified.

## 2. Repair Execution State and Transcript Continuity

- [x] 2.1 Separate argument completion from execution disposition in owned block state and its validated component mapping; verify production-ordered start/update/end and abort/error cases keep one invocation surface and show live output after arguments finish.
- [x] 2.2 Update engine and shell stale-update guards to use the correct lifecycle phase plus revision/generation; verify genuine post-execution partials stay rejected while valid running updates survive queued and full-view paths.
- [x] 2.3 Merge repeated message/turn call declarations without downgrading final results; verify serial/parallel tool fixtures retain result data, errors, arguments, attachments, identity, and order without intermediate argument-only replacement.
- [x] 2.4 Distinguish run-local completion collections from full session-authoritative replacement; verify prior history remains continuously present during delayed settlement, unchanged blocks keep identity, and explicit session/branch/compaction reconciliation still follows existing scope rules.
- [x] 2.5 Exercise bounded delivery across those corrected phases; verify same-invocation coalescing, independent concurrent blocks, protected outcomes, generation replacement, overload recovery, and final-state flush preserve the existing queue and responsiveness guarantees.

## 3. Preserve Complete Renderer Inputs

- [ ] 3.1 Introduce or refine validated owned rendering payload fields separately from diagnostic summaries; verify supported arguments, text/content parts, partial/final details, and error state reach actual built-in and extension renderers without vendor types escaping the integration boundary.
- [ ] 3.2 Restore authoritative edit-result details and prevent a late preview from overwriting a settled result; verify final diffs render when previews are missing, stale, or fail and compare styled output against the independent pinned edit renderer.
- [ ] 3.3 Preserve changing attachment references on existing tool surfaces through the existing resolver; verify late arrival, replacement, hidden/unavailable fallback, supported inline presentation, and disposal without losing invocation identity or duplicating assets.
- [ ] 3.4 Verify large partial and final renderer payloads stay within supported queue/asset limits without per-chunk serialization, arbitrary rendering-data truncation, or silent omission; include bounded fallback/error checks and unchanged text-only tool presentation.

## 4. Make Presentation Invalidation Reach the Screen

- [ ] 4.1 Replace no-op transcript-renderer notifications with a typed mounted-instance/session-scoped presentation callback; verify a delayed renderer update requests the real scheduler while semantic revisions, completion counts, and persisted content remain unchanged.
- [ ] 4.2 Track presentation dirtiness through block rows, document layout, and pending viewport reuse; verify the root and next terminal frame show a same-semantic-revision asynchronous change without input, resize, or a new agent event.
- [ ] 4.3 Recompute dependent geometry for renderer height changes; verify document extent, follow/detach behavior, selection/copy mapping, live-tail metadata, link ranges, hit regions, and pinned dock remain consistent during pending stream/dock frames.
- [ ] 4.4 Guard old component callbacks and coalesce repeated invalidations; verify replacement/disposal/session races cannot repaint new surfaces, render loops do not occur, off-screen invalidations are lazy, and unaffected block caches remain reusable.

## 5. Preserve Full URL Targets and Bounded Link Cleanup

- [ ] 5.1 Add shared pre-layout HTTP/HTTPS link recognition at the owned text/span boundary, preserving authoritative explicit targets; verify complete targets survive wrapping, styling splits, punctuation, repeated occurrences, wide/combining text, and clipping without inventing links from dotted identifiers.
- [ ] 5.2 Wire that policy into supported submitted, assistant, and built-in/generic tool text using public renderer injection and any minimum attributed renderer closure; verify sanitization precedes decoration, no post-wrap target reconstruction is needed, non-link styled rows match independent pinned rendering, and installed packages remain untouched.
- [ ] 5.3 Preserve web-link blue, file-link accent, native activation, held-selection behavior, and semantic copy; verify wrapped segments activate the full target, labels are unchanged, and copied text contains no presentation-only characters or padding.
- [ ] 5.4 Reconcile discarded explicit presented occurrences without restoring candidate-signature screen clears; verify moved/shortened/removed/covered links are cleaned even without an observed hover, while candidate-only streaming and stable dock typing retain bounded paint.
- [ ] 5.5 Preserve pending cleanup until the corresponding bytes are forwarded; verify coalescing, unknown grammar/recovery, selection release, overlays, and resize cannot acknowledge cleanup early or restore stale cells, and ordinary cleanup introduces no full-screen clear or separate blank frame.

## 6. Prove End-to-End Behavior Under Real Scheduling

- [ ] 6.1 Extend the existing producer harness with production-ordered multi-message/tool workloads and actual independent pinned tool renderers; verify text-only tool approximations no longer establish structured-result parity and multiple assistant messages remain distinct.
- [ ] 6.2 Add scheduled/burst cases without per-event forced renders alongside stepped diagnostic cases; verify delayed settlement, asynchronous preview completion, keyboard/pointer preemption, and final-state flush present current content without stale later frames.
- [ ] 6.3 Correlate bounded event/block, semantic/presentation revision, document range, write-local decision, and cell/target evidence; verify missing content is attributable to its earliest failed boundary and diagnostics do not leak into normal UI or include private payloads by default.
- [ ] 6.4 Exercise large geometry including 192 by 54 and narrow wrapping, long settled history, code, URL/file links, modal coverage, selection, and supported images; verify text/style/cursor/target correctness and existing stable-work/paint budgets in synchronization-honored and synchronization-ignored replay.
- [ ] 6.5 Seed rejected-live-output, run-local-replacement, discarded-details, stale-cache, and truncated-target regressions in test-only controls; verify the corresponding gates fail rather than merely recording lower frame or clear counts.

## 7. Validate the Exact Candidate and Record Acceptance

- [ ] 7.1 Complete focused contract/type, architecture/provenance, engine/component, viewport, scheduling, and rendering validation and obtain required CI for the exact implementation commit; verify recorded results cover the corrected boundaries without changing the explicit Pi oracle or dependency pins.
- [ ] 7.2 Deliver the exact built candidate with worktree/commit, focused reproduction steps, and color-preserving development/comparison launch commands; verify the handoff directly exercises full tool ordering, earlier-history retention, delayed renderer output, and wrapped URL/file hover transitions.
- [ ] 7.3 Obtain user-controlled Windows Terminal review of the original missing-content and ghost-underline cases at the recorded environment; verify all displayable blocks remain available without resize/reopen and unrelated text/blank cells lose stale underlines while real links remain usable.
- [ ] 7.4 Record acceptance only after both content and link findings pass on the exact candidate; verify any remaining host-only ghost or content omission keeps this change incomplete and triggers diagnosis or an explicitly approved design revision rather than weakened criteria or a silent link-model substitution.
