## 1. Establish heading-relative geometry

- [ ] 1.1 Add a shared test helper or equivalent display-width assertion for the first visible cell of dialog headings and shortcut rows; verify ANSI styling does not affect the measured column.
- [ ] 1.2 Make shortcut-row placement explicit at the component boundary so semantic hint formatting does not add a content-row indent on top of container padding.
- [ ] 1.3 Update the shared Settings dialog panel and full-screen route footers to use their heading edge while preserving frame row counts, clipping, and pointer geometry.

## 2. Align bare-A1 modal families

- [ ] 2.1 Align top-level Models, Thinking, Skills, scoped-model, tree, session, and trust selector hints with their respective headings; verify list/search/content columns remain unchanged.
- [ ] 2.2 Align nested input, editor, rename, label, confirmation, authentication, and extension-hosted hint rows with their local headings; verify transitions, focus restoration, and actions remain unchanged.
- [ ] 2.3 Align the isolated pre-resource trust selector without importing post-trust theme or component code; verify fixed key/action colors and startup isolation remain intact.
- [ ] 2.4 Preserve `a1 pi` and untouched pinned Pi output; verify no installed dependency bytes, exported constructors, or prototypes change.

## 3. Prove complete presentation coverage

- [ ] 3.1 Extend modal inventory coverage so every shortcut-bearing bare-A1 dialog node or full-screen route asserts equal heading and hint start columns or carries an explicit no-heading/no-shortcut disposition.
- [ ] 3.2 Update focused snapshots for representative zero-inset, one-inset, wrapped, clipped, nested, startup, Settings, Changelog, and Hotkeys surfaces; verify shortcut wording, order, colors, and whitespace-only entry gaps are unchanged.
- [ ] 3.3 Run focused component, integration, architecture, startup-graph, and comparison-profile validation; verify dialog behavior and package-boundary conformance remain green.
- [ ] 3.4 Build and hand off the exact candidate for Windows Terminal review; verify shortcut rows visually align with headings across the requested dialogs and record any known gap before finalization.
