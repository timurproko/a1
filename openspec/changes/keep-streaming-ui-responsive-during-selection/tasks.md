## 1. Reproduce and attribute the combined regression

- [ ] 1.1 Reconcile the implementation base with the integrated selection, input, transient-tail, and `eliminate-code-block-streaming-flicker` work; verify a recorded compatibility map identifies the current shared contracts without modifying unrelated change artifacts.
- [ ] 1.2 Add real built-in write argument/execution/completion workloads for 258 and 2,000 lines, cold/warm preparation, and a long single line at 120x30 and 192x54; verify the collapsed hint and final output match the independent pinned producer.
- [ ] 1.3 Add selection-only, wheel-only, typing, stream-only, and combined workload variants with short/long settled histories; verify the fixtures cover duplicate endpoints, retained selection, wheel reversals at boundaries, every scrollbar speed, detached reading, and return to the live tail.
- [ ] 1.4 Instrument bounded preparation, composition, revision, request/frame, timer, event-loop, paint, and fallback evidence; verify it captures a pre-fix combined regression and does not confuse render requests with actual frames or retain private input payloads.

## 2. Preserve input semantics with bounded presentation

- [ ] 2.1 Suppress repaint for truly unchanged motion and boundary-wheel state while retaining pointer coordinates and ownership; verify duplicate-report tests add no composition/write and stationary-cursor reveal still gets correct hover.
- [ ] 2.2 Coalesce superseded selection and viewport presentations against the last presented state without dropping semantic actions; verify leave-and-return drags, release/copy barriers, mixed keyboard/mouse deliveries, and newest-endpoint/position tests.
- [ ] 2.3 Preserve sequential wheel distance, clamping, follow transitions, and modal ownership; verify each speed and boundary reversal against a per-report reference with interleaved content, keyboard, resize, and ownership changes.
- [ ] 2.4 Bound safe-input drains without weakening conservative barriers or edge-auto-scroll timing; verify timers run between batches, edge reports add no unscheduled rows, and release/re-entry/reset stops auto-scroll.

## 3. Retain content and isolate interaction composition

- [ ] 3.1 Introduce revision-aware prepared document/block access and prompt-anchor indexing without per-interaction whole-document concatenation; verify equal visible workloads over short/long history do not scan/copy off-screen rows.
- [ ] 3.2 Reuse unchanged live/finalized base rows for selection, wheel, editor, and status updates, preparing only newly exposed uncached rows on scrolling; verify selection damage and wheel overlap counters and exact terminal cells.
- [ ] 3.3 Replace broad transcript invalidation during unrelated status/editor synchronization with scoped invalidation; verify unchanged built-in call renderers are not invoked and true content/theme/width/expansion/image changes invalidate correctly.
- [ ] 3.4 Carry truthful content, geometry, selection, and presented revisions through retained frames; verify reflow, dock-height changes, sticky/rail/control changes, resize, copy, session replacement, and stale-frame rejection without stale hit regions.
- [ ] 3.5 Bound cache variants and release block/session state; verify removal, theme churn, repeated resize, and disposal do not retain unbounded source or presentation variants.

## 4. Bound built-in tool preparation

- [ ] 4.1 Separate immediate semantic block application from bare-A1 built-in visual preparation at eligible content frames; verify multiple chunks prepare only the newest eligible revision and completion supersedes pending partials without delaying engine events.
- [ ] 4.2 Suppress unchanged tool setters and add public-boundary deterministic built-in renderer reuse with complete context keys; verify renderer-call counts across arguments, execution start, argument completion, results, expansion, and theme changes.
- [ ] 4.3 Implement revision-aware collapsed-write prefix/line metadata and unchanged-highlighting reuse; verify exact pinned partial/final/collapsed output for multiline syntax, trailing blanks, tabs, Unicode, path text, long lines, and non-append replacement.
- [ ] 4.4 Measure the remaining worst synchronous preparation slice against the design's interaction budget and implement the lazy pure-preparation worker fallback wherever bounded exact preparation is insufficient; verify responsive input with real CPU work, latest-result identity, cancellation, failure recovery, and exact expansion, or record evidence that the supported workload needs no worker.
- [ ] 4.5 If isolated preparation is required, package its entry/assets and bound jobs/source retention; verify cold packaged execution, worker failure, theme/expansion changes, removal, session replacement, and teardown. Otherwise verify and record that no worker artifacts or startup dependency were added.
- [ ] 4.6 Preserve opaque extension renderer callback/lifecycle behavior and gate the optimization to bare A1; verify independent comparison output, custom extension callback sequences, installed package identity, and public-boundary conformance.

## 5. Keep content and animation progressing under input

- [ ] 5.1 Replace receipt-based stream acknowledgment with represented/prepared/presented revision accounting; verify no-op motion and boundary-wheel storms cannot cancel or indefinitely postpone the only due content frame.
- [ ] 5.2 Coordinate immediate interaction, due content, and status presentations through the existing single writer with bounded work slices; verify all three progress during sustained selection, wheel, and keyboard bursts and at most one latest interaction presentation remains pending.
- [ ] 5.3 Preserve final-result priority, newest spinner state, off-screen status behavior, and disposal ordering; verify no obsolete partial, animation backlog, pinned detached indicator, or late terminal write after teardown.

## 6. Eliminate forced wheel repaint and prove movement safety

- [ ] 6.1 Remove unconditional forced render/reset for ordinary wheel input while retaining explicit bounded link cleanup; verify scroll-only writes do not clear the whole screen or repaint unchanged dock rows and accepted hover/link-deletion repairs still work.
- [ ] 6.2 Extend semantic frame evidence with selection correspondence for held and released ranges; verify safe followed appends preserve selected cells and copied grapheme/newline boundaries without clearing selection or changing follow policy.
- [ ] 6.3 Extend the finite terminal transformation to proven signed overlapping wheel movement, including detached and selected content, with a distinct damage classification; verify both directions, exposed rows, sticky/rail/bottom controls, changed dock cells, and same-write margin restoration through cell replay.
- [ ] 6.4 Keep non-overlapping jumps and unsafe explicit-link/image/reflow/grammar/resize/ownership cases conservative; verify exact output and attributed fallbacks without weakening the related flicker change's hyperlink or live-tail safety budgets.

## 7. Validate and hand off the exact implementation

- [ ] 7.1 Run the integrated deterministic matrix and independent comparison producers; verify budgets fail repeated unchanged preparation, off-screen interaction scans/copies, starvation, stale positions/endpoints/finals, and unnecessary clears/rewrites while preserving exact preview, expansion, copy, and navigation output.
- [ ] 7.2 Obtain required CI results for the implementation candidate and retain its bounded evidence; verify public-boundary/package conformance and all required checks pass before requesting acceptance.
- [ ] 7.3 Provide the exact built candidate and user-run Windows Terminal comparison using the repository's color-preserving shell entries; verify recorded p50/p95/max selection/wheel/typing latency, visible spinner gaps, and content freshness against the design targets, including the user's wheel-only lag case.
- [ ] 7.4 Record explicit user acceptance of smooth selection, wheel scrolling, animation, and content progress with no catch-up or semantic regressions; verify any contradictory physical result keeps the implementation unaccepted, then complete the accepted-change archival follow-up only after authorized integration.
