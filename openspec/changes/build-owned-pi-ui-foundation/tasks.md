## 1. Establish Contracts and Research Boundaries

- [x] 1.0 Create or switch to `milestone/owned-pi-ui-foundation` before implementation and verify the working tree contains only this change's implementation edits.
- [x] 1.1 Define dependency-free AddOne owned-UI contracts for commands, session events, view models, transcript blocks, editor state, status, dialogs, overlays, themes, component slots, diagnostics, and lifecycle; pass valid, malformed, oversized, and compatibility fixtures.
- [x] 1.2 Add architecture governance that confines Pi SDK and public Pi component types to adapter modules and rejects `InteractiveMode` mutation, prototype patching, private-field inspection, deep Pi imports, distribution-hash gating, Bun-only dependencies, oh-my-pi package dependencies, terminal-host coupling, and stock-Pi extension UI assumptions.
- [x] 1.3 Record the local research baseline for `v2` fragility and `D:\Git\oh-my-pi` architecture: adopted patterns, explicitly rejected scope, source revisions, licenses, and provenance requirements for any future port; pass policy tests.

## 2. Build the Pi Engine Adapter

- [x] 2.1 Implement the public-SDK engine adapter over `createAgentSessionRuntime()` for session construction, service ownership, prompt, abort, retry, compaction, model, thinking, session resume, diagnostics, and shutdown; pass synthetic engine conformance fixtures.
- [x] 2.2 Map Pi events, state snapshots, and commands into versioned AddOne-owned view models without exposing Pi types above the adapter; pass event sequencing, malformed-event, snapshot, command-correlation, cancellation, and cleanup tests.
- [x] 2.3 Implement bounded event queues, backpressure, error mapping, restart reconciliation, and terminal cleanup so one engine failure cannot corrupt AddOne UI state; pass high-rate and failure-isolation tests.
- [x] 2.4 Add Pi-upgrade conformance suites for SDK construction, service creation, event shape, command behavior, public component constructor/render contracts, and adapter mapping; prove failures remain contained at adapters.

## 3. Build the AddOne Terminal UI Runtime

- [x] 3.1 Implement the AddOne-owned terminal runtime over the public Pi TUI mode architecture: `TuiMainScreen` in vanilla-default `regular` mode with terminal-native selection and scrollback, plus `TuiAltScreen` only for the optional experimental `fullscreen` setting. Preserve raw input, focus, invalidation, coalesced width-safe rendering, overlays, resize, restoration, and mode-specific lifecycle without intercepting mouse selection, repainting selected cells, synthesizing clipboard sequences, or forcing fullscreen; pass focused regular/fullscreen runtime tests.
- [x] 3.2 Implement append-only transcript history with explicit live and finalized block boundaries, render caching, resize-safe repainting, selection-safe output, and bounded frame scheduling; pass streaming, long-output, Unicode, alternate-content, resize, and regression fixtures.
- [x] 3.3 Implement the owned prompt editor with text and IME input, queued submissions, paste, selection, clipboard, keyboard bindings, focus, cancellation, and terminal-restoration behavior; pass editor property and interaction tests.
- [x] 3.4 Implement status, header/footer, notifications, dialogs, selectors, command surface, and diagnostics as AddOne-owned components connected through view models; pass rendering and state-transition fixtures.

## 4. Deliver the Vanilla-Style Fullscreen Session

- [x] 4.1 Implement transcript and streaming presentation for user, assistant, thinking, tool-call, tool-result, retry, compaction, error, and system blocks using AddOne view models; pass deterministic streaming and resize fixtures.
- [x] 4.2 Adapt documented public Pi components where their contracts are independently usable and add provenance-recorded MIT-licensed ports only for surfaces that require AddOne ownership; pass component conformance and attribution/provenance policy tests.
- [x] 4.3 Wire prompt, abort, retry, compaction, model, thinking, session creation, session resume, settings, and shutdown workflows through the engine adapter; pass command/state integration fixtures with a synthetic engine.
- [x] 4.4 Make bare `a1` and `addone` launch the owned UI, preserve untouched upstream transparent attachment at `a1 pi` and `addone pi`, preserve sandbox routing, and remove `a1 ui` and `addone ui`; pass CLI routing, alias rejection, startup, exit, and fallback-isolation tests.

## 5. Add Customization, Extension, and Diagnostics Foundations

- [x] 5.1 Implement versioned AddOne slots for themes, transcript blocks, tool cards, editor, status surfaces, commands, selectors, overlays, and future layout composition; pass registration, precedence, isolation, and invalid-slot tests.
- [x] 5.2 Add a vanilla-style preset and prove customizations resolve without mutating installed Pi code or depending on Pi's stock extension UI context; pass preset replacement and rollback fixtures.
- [x] 5.3 Extend the public-SDK resource adapter with complete AddOne-owned contracts for pinned visible extension UI callbacks while retaining non-visual resource discovery; pass exhaustive callback-shape, unsupported-path, diagnostics, malformed-extension, and no-private-context architecture tests.
- [x] 5.4 Add bounded logging, redaction, diagnostics capture, CPU/memory/frame observations, and terminal-restoration failure reporting without exporting raw engine payloads through UI controls; pass diagnostics policy tests.

## 6. Record the Custom Renderer Spike

- [x] 6.1 Run complete automated gates for the initial hand-written runtime spike: build, typecheck, tests, architecture, adapter conformance, terminal runtime, transcript, editor, input, clipboard, resize, lifecycle, resource, packaging, audit, and strict OpenSpec validation; preserve machine-readable evidence and mark the spike unsuitable for production acceptance.

## 7. Mechanically Port the Complete Pinned Pi Interactive UI

- [x] 7.0 Keep the exact public Pi `0.84.2` identities and rebuild `npm run test:pi-terminal-parity` so its primary untouched-Pi producer uses the actual default `regular` mode rather than forcing `--tui-mode fullscreen`; cover optional fullscreen separately. Apply identical cwd, environment, resources, model stream, physical terminal actions, geometry, and settings; compare rows, ANSI, spacing, wrapping, cursor/focus, native scrollback, renderer mode, component composition, errors, resize, settlement, and shutdown; prove intentional mutations fail and keep PTY/cell capture outside production.
- [x] 7.1 Implement `PiTuiRuntimeAdapter` as a mode-faithful adapter over public `TuiMainScreen` and `TuiAltScreen`. Default to the settings-derived vanilla `regular` mode, where the terminal owns text selection, copy, and scrollback and Pi emits no mouse-tracking or alternate-screen controls. Preserve optional fullscreen layout/scroll behavior only when explicitly selected. Remove every AddOne selection ANSI rewrite, retained-coordinate tracker, OSC 52 interception, copy-flash suppression, fake mouse/focus clear sequence, and hardcoded three-row application wheel override. Pass regular native-selection ownership, no-mouse-tracking, main-screen differential rendering, terminal scrollback, input, overlay, resize, restoration, mode switch, optional fullscreen scroll, and architecture tests.
- [x] 7.2 Regenerate an exhaustive inventory of every interactive source module and relevant behavior at Pi `0.84.2` commit `914cf1472e715297caa30db4b9535d534a9eb718`; invalidate all `0.84.1` source hashes and raw evidence; classify each as public reuse, owned source port, or host adapter, and record upstream path, local destination, attribution, tests, modifications, and approved deviations in a machine-readable ledger. Add a source-derived inventory entry for every built-in, nested-settings, authentication, session/tree/fork, command-input/confirmation, custom-editor/input/overlay, and extension-hosted modal branch, including controller state and completion/cancellation/disposal paths.
- [x] 7.3 Add repository-governance validation for the source ledger, pinned identities, MIT attribution, local destinations, forbidden deep imports, stale mappings, and undocumented deviations; prove the gate fails when any pinned source unit, required field, modal branch, stateful action, or route-specific acceptance case is removed, and reject a shared generic-selector fixture as coverage for specialized controllers.
- [x] 7.4 Port pinned theme loading, color resolution, ANSI styles, spacing constants, layout primitives, and terminal-dependent style behavior without AddOne redesign; pass independent fixed-width style and row comparisons against pinned Pi.
- [x] 7.5 Port user, assistant, thinking, system, custom, bash, tool-call, tool-result, retry, compaction, error, status, and command-Markdown presentation as stateful pinned components. Preserve complete ordered assistant content arrays, pinned `AssistantMessageComponent` lifetime, `createMarkdownTransform()`, settings-aware Markdown themes, streaming state, and exact spacing for consecutive list rows, paragraphs, mixed thinking/text runs, tool boundaries, and stop/error/abort content instead of flattening active messages into independently reconstructed blocks. Preserve route-owned Markdown composition such as the bordered bold-accent `/changelog` heading and settings-aware Markdown body instead of exposing raw Markdown through generic workflow rows. Pass independent identical-payload raw-ANSI lifecycle, streaming, replacement, Markdown-spacing, changelog, wrapping, Unicode, mixed-content, tool-adjacency, settlement, and resize comparisons.
- [x] 7.6 Port the pinned editor, autocomplete providers, slash/template/skill/extension completion, keybindings, paste, selection, clipboard, queue submission, cancellation, and current-session prompt-history behavior. Populate history from initial user messages, add accepted inputs at pinned controller points, and preserve Up/Down ordering, adjacent-duplicate suppression, bounded retention, draft restoration, and multiline boundaries. Preserve every modal-specific keybinding, including scoped-model toggle/bulk/provider/reorder actions and `Ctrl+S` persistence, through the pinned component rather than global approximations; pass upstream-versus-AddOne input sequence and editor-frame comparisons.
- [x] 7.7 Port pinned root composition for both renderer modes. In regular mode mount the same ordered document, pending/status/widget/editor/footer components directly into `TuiMainScreen`; in fullscreen mode mount the source-equivalent viewport layout. Preserve startup resources, transcript/status/footer ownership, working indicators, focus, width-safe wrapping, invalidation, resize, renderer switching, and restoration. Pass independent regular-default and optional-fullscreen frames at quiet/verbose, compact/expanded, populated, modal, narrow, wide, and resized states.
- [x] 7.8 Port every pinned modal-like surface discovered from `InteractiveMode`, nested settings components, and public extension UI, including settings/theme/thinking/images; model and scoped-model configuration; trust; fork/user-message; tree/summarization/custom instructions; session/resume/rename/missing-cwd; login auth-type/provider/API-key/OAuth dialogs and logout; command inputs/confirmations; extension select/confirm/input/editor/custom-editor/custom-overlay; and any additional source-discovered route. Replace the generic settings callback selector with the source-traced `SettingsSelectorComponent`, current values, descriptions, search, instructions, nested selectors, change effects, and scrolling. Compose specialized selectors through pinned root-container placement, editor replacement, viewport allocation, transcript relationship, instruction rows, footer relationship, silent close/cancel, focus restoration, and resize behavior rather than generic overlays. Closing a selector SHALL leave transcript, workflow, notification, and status rows unchanged unless that pinned operation explicitly reports cancellation. Scoped models SHALL remain open while Enter applies session-only toggles, expose dirty/unsaved and refresh states, support all bulk/provider/reorder actions, persist only on `Ctrl+S`, and close silently without implicit persistence. Pass route-specific independent populated-transcript open, active interaction, save/confirm, navigation, search, nested replacement, silent cancellation, failure, session-switch, disposal, focus-restoration, scrolling, footer, viewport, and resize parity tests for every inventory entry.
- [x] 7.9 Port the complete pinned built-in command and input controllers while preserving dispatch order, side effects, hidden routes, prompt-template/skill/extension fallthrough, bash inclusion rules, editor-history insertion points, silent selector cancellation, clipboard, queue behavior, and shutdown. Record a visible-route source trace for every built-in command and replace generic text or selector substitutions, including the one-shot `#showWorkflowSelector()` path wherever pinned Pi owns a stateful modal controller; specifically port `/hotkeys` as the complete keybinding-derived heading and styled Markdown tables with pinned transcript insertion, categories, colors, spacing, wrapping, and scrolling; port `/changelog` as the pinned bordered `What's New` plus settings-aware Markdown composition; preserve `/export` failure context and pinned error placement/style; and route `/reload` through dim coalescing status without an AddOne-only checkmark. Pass exhaustive independent success, cancellation, failure, route-frame, history, and outcome conformance.
- [x] 7.10 Port ordinary prompt orchestration from editor submission through visible user turn, editor-history insertion, public session prompt execution, ordered streaming assistant/tool updates, turn settlement, abort, and recoverable failures. Preserve every finalized user, assistant, thinking, and tool component after completion, including settlement events with missing or replacement message collections. Pass synthetic edge cases plus a real SDK-backed prompt proving visible submission, current-session Up/Down history, streaming, completion, retained content, footer usage updates, and continued editor interaction.
- [x] 7.11 Port queued messages, steering/follow-up behavior, retries, compaction, transcript rebuilding, session switches, terminal progress, diagnostics, malformed events, and error recovery. Follow pinned session-authoritative rebuilding and component lifetime when settlement message arrays are absent, partial, or replaced; prohibit rebuilding a completed transcript as an empty approximation. Pass independent high-rate ordering, state replacement, missing-message settlement, cancellation, compaction, session-switch, and failure-isolation outcomes and frames.
- [x] 7.12 Bind the complete pinned public extension lifecycle through the AddOne-owned extension UI bridge, including registration, session switch, cancellation, disposal, and failure isolation; pass callback-coverage and no-private-context architecture tests.
- [x] 7.13 Port extension widgets, status/footer contributions, working messages, terminal input hooks, and focus/invalidation behavior; pass independent rendering, input-consumption, replacement, cancellation, and cleanup parity tests.
- [x] 7.14 Port and exhaustively inventory extension custom editors and inputs, selectors, confirmations, dialogs, notifications, custom overlays, nested/replacement behavior, and overlay/focus lifecycle; pass route-specific independent open, active update, resolve, cancel, throw, replacement, session-switch, disposal, resize, and restoration parity tests.
- [x] 7.15 Port extension custom message and tool renderers, partial-result updates, detail expansion, malformed-output handling, and renderer failure isolation; pass streaming, replacement, error, fallback, and cleanup parity tests.
- [x] 7.16 Remove superseded approximate shell controllers and runtime patches, including the custom terminal selection repaint/interception wrapper, shallow active-render reconstruction, duplicate command paths, generic one-shot workflow/modal substitutions, and obsolete routing, only after public renderer-mode replacements pass focused and containing suites; retain historical evidence with explicit invalidation metadata.
- [ ] 7.17 Prove exhaustive source-to-port coverage with zero unmapped interactive modules and zero unapproved deviations. Record public `TuiMainScreen` reuse as the default vanilla renderer and public `TuiAltScreen` reuse as optional fullscreen; reject the former fullscreen-selection deviation and prove no production selection repaint/interception remains. Preserve complete attribution, behavior mappings, modal mappings, and intentional-failure fixtures.
- [x] 7.18 Run `npm run test:pi-terminal-parity` against untouched Pi and AddOne in the actual default regular mode, plus focused optional-fullscreen parity. Cover all presenter/modal/prompt/extension states, regular main-screen rows and native scrollback, absence of application mouse tracking, terminal-owned selection behavior, current-session history, command presenters, physical terminal scrolling, resize, settlement, restoration, and optional fullscreen viewport behavior; fail on every undeclared divergence.
- [x] 7.19 Run independent exhaustive command, keybinding, input, prompt-history, queue, tool, model, settings, session, authentication, clipboard, extension, and shutdown workflow parity, including every source-inventoried built-in and extension modal transition, scoped-model dirty/session/persisted state, silent modal cancellation, pinned history insertion/navigation boundaries, selection invalidation before ordinary input, route-specific changelog composition, export-error context/style/placement, and reload-status style/coalescing; preserve both producers' raw outcomes and prove the harness fails when a path or transition is intentionally removed.
- [x] 7.20 Run real public-SDK-backed prompt and extension integration with streaming text, thinking, tool calls/results, retries, errors, every extension modal/input/editor/overlay lifecycle, and settlement; prove the owned UI remains interactive and restores focus after success, cancellation, and failure.
- [x] 7.21 Run terminal lifecycle parity for default regular and optional fullscreen modes. Prove regular mode leaves mouse selection/copy and wheel scrollback to the physical terminal, emits neither alternate-screen nor mouse-tracking controls, and cannot transfer an application selection rectangle onto newly rendered command/modal content. Cover selected/unselected `Ctrl+C` at the terminal boundary, scrollback, resize storms, hardware cursor, progress, mode switching, graceful/abnormal shutdown, and restoration; separately cover public fullscreen selection/scroll behavior without AddOne rewriting.
- [x] 7.22 Correct every remaining source, workflow, frame, extension, prompt, or lifecycle divergence; rerun focused and containing gates and record exact commit, package versions, exclusions, tolerances, raw artifact hashes, and machine-readable evidence. Repeat the user-controlled comparisons that exposed focused-editor color, placeholder footer usage, disappearing completed turns, generic settings selectors, abbreviated `/hotkeys`, extra assistant/list spacing, slower wheel scrolling, missing startup Skills/Prompts/resources, incorrect selector/modal composition, AddOne-only `{modal name} cancelled` rows, one-shot/non-stateful modal substitutions, `/scoped-models` closing on toggle instead of retaining session-only dirty state and saving only through `Ctrl+S`, omitted vanilla modal branches, missing Up/Down current-session prompt history, AddOne-only `Copied!` flashes, non-white or syntax-colored character/word/line/area selection, stale retained selections highlighting newly opened command/autocomplete content, automatic copy-on-release, one-row rather than three-row wheel movement, raw/unbordered `/changelog` Markdown, incorrectly prefixed or positioned command errors, missing route context such as `Failed to export session:`, and AddOne-only checkmarks or styling on dim statuses such as `/reload`. Include the latest manual findings: raw JSON or plain command content where vanilla uses structured styled presenters such as `/session`; incorrect working-indicator spacing; informational/status rows with wrong style, checkmark, prompt distance, or blank rows; latest-message chronology not placing the newest pinned transient closest to the editor; prompt-adjacent errors inserted into the persistent content region; deeper dialogs replaced by generic inputs with wrong headings, options, borders, instructions, and parent restoration; selection rendered as disconnected per-line spans instead of one continuous block through trailing/blank cells; source colors reappearing after selection scrolling; and retained selection transferring onto command-menu replacement content. Include multiline prompt-adjacent status rows such as `Share URL`/`Gist` losing continuation-line left padding or remaining embedded in one logical render row, and any resulting regular-mode `TuiMainScreen` row-accounting drift that places subsequent editor-replacement modals or the footer differently from vanilla. Keep each finding open until the corresponding source trace, ownership-plane or modal-graph mapping, identical-payload or physical-input independent raw-ANSI evidence, containing suite, and fresh manual comparison pass.

- [x] 7.23 Regenerate a source-derived presenter ownership inventory for every built-in command result, session/diagnostic document, transcript role, working indicator, status, informational message, warning, error, notification, queue/retry/compaction row, and extension contribution. For each state record whether pinned Pi owns it as persistent document content, prompt-adjacent transient content, or active replacement content, plus its owning controller/component, container, insertion/removal point, sibling order, spacing, style, chronology, coalescing/replacement, scroll/follow behavior, and lifetime. Reject any unmapped presenter, generic workflow bucket, raw-object fallback, or ownership assignment inferred only from AddOne code.
- [x] 7.24 Mechanically port every pinned route-specific structured presenter, beginning with `/session` and then covering every advertised and hidden built-in success, information, warning, and failure outcome. Preserve complete headings, groups, labels, values, ANSI styles, indentation, padding, wrapping, scrolling, and route context; remove raw JSON/object dumps, plain-text flattening, generic success headings, and AddOne-only checkmarks. Pass independent fixed-width, narrow, wide, long-value, empty, populated, success, and failure frames for every route.
- [x] 7.25 Port the pinned root ownership and prompt-adjacent message controller for working, status, informational, warning, error, notification, queue, retry, compaction, and extension rows. Preserve exact blank rows, prompt/footer distance, chronological insertion, newest-message placement, transient replacement/coalescing/removal, persistent-versus-transient classification, scroll/follow behavior, resize, and restoration. Pass independent sequences that interleave multiple message classes with transcript content and prove the newest pinned transient is closest to the editor without moving persistent content into the dock.
- [x] 7.26 Regenerate the modal inventory as a complete directed transition graph sourced from pinned `InteractiveMode`, every nested settings/session/tree/model/authentication controller, and public extension UI. Record every root and nested node; exact heading/body/options/descriptions/borders/colors/instructions/focus/viewport/scrolling; every select/back/cancel/confirm/input/API-key/browser/device/OAuth/failure/session-switch/disposal edge; and the exact parent or editor restoration destination. Governance SHALL fail for any omitted node, edge, state field, specialized controller, or acceptance mapping.
- [x] 7.27 Mechanically port every missing or approximate modal graph node and edge, including all authentication depths and every built-in and extension nested/replacement flow. Eliminate generic selector/input/dialog substitutions even when they return equivalent values. Pass independent open, active, navigation, input, confirmation, back, cancellation, failure, completion, replacement, resize, scrolling, focus, session-switch, disposal, and parent-restoration evidence at every graph depth.
- [x] 7.28 Expand independent terminal/workflow producers so primary acceptance launches untouched Pi and AddOne in default regular mode, with optional fullscreen as a separate scenario. Cover every presenter ownership state and modal edge, renderer-mode switching/restoration, native scrollback/selection ownership, and intentional mutations for forced fullscreen, mouse tracking in regular mode, structured flattening, ownership/order, omitted modal nodes, generic substitution, and wrong restoration targets.
- [x] 7.29 Reconcile all 12 `not-ported` owned-source records to real public-reuse, host-adapter, inline-owned-port, or present coherent-port destinations; mechanically synchronize the two `upgrade-review-required` theme units; update hashes, provenance, modifications, tests, and statuses; and strengthen governance to reject absent planned destinations or stale review states.
- [x] 7.30 Remove all generic visible `workflowSelector()` fallback paths. Expose typed route contexts/actions for settings, model/scoped models, fork, tree, trust, login/logout, resume, import confirmation, and every remaining workflow; keep specialized stateful controllers as the only visible owners and pass complete route/modal parity.
- [x] 7.31 Replace extension working-message rendered-string substitution with a dedicated source-owned status update path, and replace silent `ComponentBridge` width rewriting with source-component width correctness plus bounded diagnostics. Prove multiline, ANSI, narrow/wide, resize, working, extension, and renderer parity.
- [x] 7.32 Replace string-named engine `dynamicCall()` reflection with explicit validated public runtime/session/service façades, eliminate production `as unknown as`, `as never`, and `as any` escapes from Pi adapters and the owned shell, and fail unsupported public shapes through bounded adapter diagnostics. Pass synthetic malformed-shape, public-SDK, upgrade-conformance, workflow, and real-integration tests.
- [ ] 7.33 Mechanically decompose `pi-component-adapter/shell-components.ts` into bounded editor/autocomplete, selectors/dialogs, presenters/transcript, footer/status, extension-UI, and shared-facade modules without visual or lifecycle redesign. Add import-boundary and file-size governance and preserve all public AddOne adapter exports.
- [ ] 7.34 Add a zero-debt customization-prerequisite gate that rejects prohibited patches, absent/stale ledger records, generic visible workflow fallbacks, rendered-string status substitution, silent width rewriting, engine reflection, production adapter type escapes, and monolithic shell recomposition; include intentional-failure fixtures for every class.
- [ ] 7.35 Rerun independent default-regular and optional-fullscreen terminal parity, complete tests, architecture/source coverage, real prompt/extension integration, release, audit, packaging, and strict OpenSpec validation against the immutable accepted vanilla baseline; record exact hashes and prove the debt cleanup changes no accepted frame or workflow.

## 8. Accept the 1:1 Default Owned UI

- [x] 8.1 Run complete automated gates after the regular-default renderer correction: build, typecheck, tests, architecture, source coverage, SDK/runtime/component conformance, default-regular and optional-fullscreen parity, real prompt/extension integration, lifecycle, packaging, audit, and strict OpenSpec validation; preserve machine-readable evidence and reject every forced-fullscreen or selection-patch record as acceptance evidence.
- [x] 8.2 Run user-controlled manual smoke acceptance from bare `a1` against `a1 pi`: startup visuals, prompt and streaming, tools, editor and queued input, commands, abort/retry/compaction, model and thinking controls, sessions, settings, clipboard, selection, extension UI, resize, errors, and shutdown; record exact commit, package, terminal, and extension identities.
- [x] 8.3 Correct every manual finding, reopen every contradicted task, and rerun focused, containing, source-coverage, real-integration, and parity gates; mark the base accepted only when all built-in and visible extension workflows pass.
- [x] 8.4 Update CLI help, user documentation, development instructions, and recovery guidance to state that bare `a1` launches the owned UI, `a1 pi` is the untouched upstream fallback/oracle, sandbox is unchanged, and `a1 ui` is removed; pass documentation and CLI-help consistency checks.
- [ ] 8.5 Merge the accepted slice through `develop` and publish a unique `-dev.N` package under npm `next` with the owned UI as the bare default, AddOne-specific visual customization and structured tabs still disabled, no stable support claim, `latest` unchanged, and registry/package integrity verified.

## 9. Handoff to Multi-Agent Workspace

- [x] 9.1 Record evidence that the parity-safe AddOne shell contracts are composable for future structured tabs without initializing the terminal host or creating PTYs.
- [ ] 9.2 Update the existing multi-agent milestone evidence to identify this change as satisfying its owned-UI prerequisite only after all source-port, extension, parity, and base gates pass; do not begin structured tabs or section 6 of that milestone before this acceptance.
