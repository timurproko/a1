## 1. Establish Contracts and Research Boundaries

- [x] 1.0 Create or switch to `milestone/owned-pi-ui-foundation` before implementation and verify the working tree contains only this change's implementation edits.
- [x] 1.1 Define dependency-free AddOne owned-UI contracts for commands, session events, view models, transcript blocks, editor state, status, dialogs, overlays, themes, component slots, diagnostics, and lifecycle; pass valid, malformed, oversized, and compatibility fixtures.
- [x] 1.2 Add architecture governance that confines Pi SDK and public Pi component types to adapter modules and rejects `InteractiveMode` mutation, prototype patching, private-field inspection, deep Pi imports, distribution-hash gating, Bun-only dependencies, oh-my-pi package dependencies, terminal-host coupling, and stock-Pi extension UI assumptions.
- [x] 1.3 Record the local research baseline for `v2` fragility and `D:\Git\oh-my-pi` architecture: adopted patterns, explicitly rejected scope, source revisions, licenses, and provenance requirements for any future port; pass policy tests.

## 2. Build the Pi Engine Adapter

- [x] 2.1 Implement the public-SDK engine adapter over `createAgentSessionRuntime()` for session construction, service ownership, prompt, abort, retry, compaction, model, thinking, session resume, diagnostics, and shutdown; pass synthetic engine conformance fixtures.
- [x] 2.2 Map Pi events, state snapshots, and commands into versioned AddOne-owned view models without exposing Pi types above the adapter; pass event sequencing, malformed-event, snapshot, command-correlation, cancellation, and cleanup tests.
- [x] 2.3 Implement bounded event queues, backpressure, error mapping, restart reconciliation, and terminal cleanup so one engine failure cannot corrupt AddOne UI state; pass high-rate and failure-isolation tests.
- [x] 2.4 Add Pi-upgrade conformance suites for SDK construction, service creation, event shape, command behavior, public component constructor/render contracts, and adapter mapping; prove failures remain contained at adapters.

## 3. Build the AddOne Terminal UI Runtime

- [x] 3.1 Implement the AddOne-owned fullscreen terminal runtime with raw input normalization, focus routing, component invalidation, coalesced rendering, width-safe sanitized output, overlays, terminal resize, synchronized updates where supported, and restoration on exit; pass focused terminal-runtime tests.
- [x] 3.2 Implement append-only transcript history with explicit live and finalized block boundaries, render caching, resize-safe repainting, selection-safe output, and bounded frame scheduling; pass streaming, long-output, Unicode, alternate-content, resize, and regression fixtures.
- [x] 3.3 Implement the owned prompt editor with text and IME input, queued submissions, paste, selection, clipboard, keyboard bindings, focus, cancellation, and terminal-restoration behavior; pass editor property and interaction tests.
- [x] 3.4 Implement status, header/footer, notifications, dialogs, selectors, command surface, and diagnostics as AddOne-owned components connected through view models; pass rendering and state-transition fixtures.

## 4. Deliver the Vanilla-Style Fullscreen Session

- [x] 4.1 Implement transcript and streaming presentation for user, assistant, thinking, tool-call, tool-result, retry, compaction, error, and system blocks using AddOne view models; pass deterministic streaming and resize fixtures.
- [x] 4.2 Adapt documented public Pi components where their contracts are independently usable and add provenance-recorded MIT-licensed ports only for surfaces that require AddOne ownership; pass component conformance and attribution/provenance policy tests.
- [x] 4.3 Wire prompt, abort, retry, compaction, model, thinking, session creation, session resume, settings, and shutdown workflows through the engine adapter; pass command/state integration fixtures with a synthetic engine.
- [x] 4.4 Make bare `a1` and `addone` launch the owned UI, preserve untouched upstream transparent attachment at `a1 pi` and `addone pi`, preserve sandbox routing, and remove `a1 ui` and `addone ui`; pass CLI routing, alias rejection, startup, exit, and fallback-isolation tests.

## 5. Add Customization, Extension, and Diagnostics Foundations

- [x] 5.1 Implement versioned AddOne slots for themes, transcript blocks, tool cards, editor, status surfaces, commands, selectors, overlays, and future layout composition; pass registration, precedence, isolation, and invalid-slot tests.
- [x] 5.2 Add a vanilla-style preset and prove customizations resolve without mutating installed Pi code or depending on Pi's stock extension UI context; pass preset replacement and rollback fixtures.
- [x] 5.3 Extend the public-SDK resource adapter with complete AddOne-owned contracts for pinned visible extension UI callbacks while retaining non-visual resource discovery; pass exhaustive callback-shape, unsupported-path, diagnostics, malformed-extension, and no-private-context architecture tests.
- [x] 5.4 Add bounded logging, redaction, diagnostics capture, CPU/memory/frame observations, and terminal-restoration failure reporting without exporting raw engine payloads through UI controls; pass diagnostics policy tests.

## 6. Record the Custom Renderer Spike

- [x] 6.1 Run complete automated gates for the initial hand-written runtime spike: build, typecheck, tests, architecture, adapter conformance, terminal runtime, transcript, editor, input, clipboard, resize, lifecycle, resource, packaging, audit, and strict OpenSpec validation; preserve machine-readable evidence and mark the spike unsuitable for production acceptance.

## 7. Mechanically Port the Complete Pinned Pi Interactive UI

- [x] 7.0 Upgrade exact public `@earendil-works/pi-coding-agent` and `@earendil-works/pi-tui` dependencies, lockfile identities, package assertions, and untouched-oracle launch to `0.84.2` at commit `914cf1472e715297caa30db4b9535d534a9eb718`, then rebuild the test-only `npm run test:pi-terminal-parity` gate before further parity corrections. Independently launch the actual untouched pinned Pi CLI/package and the AddOne-owned UI in isolated fixed-size terminal sessions; apply identical cwd, environment, color capabilities, resources, prepared session replay or deterministic scripted model stream, and scripted key/text/wheel/resize/command actions; capture named terminal checkpoints; compare rows, raw ANSI colors/styles, spacing, wrapping, cursor/focus, scroll destination, scrollbar, component geometry, startup resources, editor, transcript, footer/status, selectors/dialogs, errors, resize, settlement, and shutdown; emit bounded JSON and concise side-by-side diff artifacts; enforce timeouts, restoration, and both-process-tree cleanup; expose only the one full command while allowing internal scenario partitioning; prove the comparator fails under an intentional visual and input/scroll mutation; and pass architecture checks proving PTY/process/cell capture remains outside production runtime boundaries.
- [ ] 7.1 Add the exact pinned public `@earendil-works/pi-tui` dependency and implement `PiTuiRuntimeAdapter` over public TUI, terminal lifecycle, input, focus, overlays, differential rendering, resize, and restoration. Reconcile the manual selection/copy and wheel-speed divergences by source-tracing `TuiAltScreen` press/drag/release selection, ANSI highlighting, clipboard and flash behavior, `routeWheel()`, physical mouse sequence parsing/batching, active and primary `ScrollView` routing, overscroll, scrollbar updates, and configured scroll distance. Preserve fullscreen and nested-scroll ownership while retaining selection on release without copying, clearing the retained selection before forwarding every non-copy keyboard input so stale coordinates cannot highlight newly opened autocomplete/commands/selectors/dialogs, rendering dark text on one uniform bright-white inverted background independent of source colors, selected length, granularity, embedded SGR, or write boundaries, making selected `Ctrl+C` copy without a flash while unselected `Ctrl+C` interrupts, and advancing three rows per wheel notch. Pass independent character, word, line, arbitrary-area, plain/styled/linked/inverse/Unicode/whitespace, resource-heading, retained-selection-then-`/` autocomplete, raw-per-write ANSI, stateful SGR, final-emulated-cell, copy, no-flash, physical-wheel, direct-scroll, nested-scroll, boundary, focus, resize, runtime-conformance, and architecture tests without private fields or installed-package patches.
- [x] 7.2 Regenerate an exhaustive inventory of every interactive source module and relevant behavior at Pi `0.84.2` commit `914cf1472e715297caa30db4b9535d534a9eb718`; invalidate all `0.84.1` source hashes and raw evidence; classify each as public reuse, owned source port, or host adapter, and record upstream path, local destination, attribution, tests, modifications, and approved deviations in a machine-readable ledger. Add a source-derived inventory entry for every built-in, nested-settings, authentication, session/tree/fork, command-input/confirmation, custom-editor/input/overlay, and extension-hosted modal branch, including controller state and completion/cancellation/disposal paths.
- [x] 7.3 Add repository-governance validation for the source ledger, pinned identities, MIT attribution, local destinations, forbidden deep imports, stale mappings, and undocumented deviations; prove the gate fails when any pinned source unit, required field, modal branch, stateful action, or route-specific acceptance case is removed, and reject a shared generic-selector fixture as coverage for specialized controllers.
- [x] 7.4 Port pinned theme loading, color resolution, ANSI styles, spacing constants, layout primitives, and terminal-dependent style behavior without AddOne redesign; pass independent fixed-width style and row comparisons against pinned Pi.
- [x] 7.5 Port user, assistant, thinking, system, custom, bash, tool-call, tool-result, retry, compaction, error, status, and command-Markdown presentation as stateful pinned components. Preserve complete ordered assistant content arrays, pinned `AssistantMessageComponent` lifetime, `createMarkdownTransform()`, settings-aware Markdown themes, streaming state, and exact spacing for consecutive list rows, paragraphs, mixed thinking/text runs, tool boundaries, and stop/error/abort content instead of flattening active messages into independently reconstructed blocks. Preserve route-owned Markdown composition such as the bordered bold-accent `/changelog` heading and settings-aware Markdown body instead of exposing raw Markdown through generic workflow rows. Pass independent identical-payload raw-ANSI lifecycle, streaming, replacement, Markdown-spacing, changelog, wrapping, Unicode, mixed-content, tool-adjacency, settlement, and resize comparisons.
- [ ] 7.6 Port the pinned editor, autocomplete providers, slash/template/skill/extension completion, keybindings, paste, selection, clipboard, queue submission, cancellation, and current-session prompt-history behavior. Populate history from initial user messages, add accepted inputs at pinned controller points, and preserve Up/Down ordering, adjacent-duplicate suppression, bounded retention, draft restoration, and multiline boundaries. Preserve every modal-specific keybinding, including scoped-model toggle/bulk/provider/reorder actions and `Ctrl+S` persistence, through the pinned component rather than global approximations; pass upstream-versus-AddOne input sequence and editor-frame comparisons.
- [ ] 7.7 Port pinned root composition, header and startup notices, loaded-resource containers, transcript spacing, footer/status surfaces, working indicators, focus transitions, invalidation, and resize behavior. Port `showLoadedResources()` before initial messages with Context, Skills, Prompts, Extensions, Themes, conflicts/diagnostics, source grouping, sorting, compact/expanded labels, colors, spacing, and persistence across chat clears. Include source-traced `showError()` spacer/output-padding/error-color/prefix placement, `showStatus()` dim spacer placement and consecutive-status coalescing, `updateEditorBorderColor()` thinking/bash mapping, and complete `FooterComponent` cumulative input/output/cache/cost, context, auto-compaction, provider/model/thinking, branch, session-name, and extension-status data flow and formatting; remove literal usage placeholders. Port the shared `showSelector()` editor-replacement lifecycle and every modal's root-container, active focus target, nested replacement, status/footer relationship, viewport allocation, resize, and restoration behavior. Pass independent raw-ANSI quiet/verbose, compact/expanded resources, resource diagnostics, initial-message ordering, clear persistence, focused-editor, thinking-level, bash-mode, busy, idle, populated-footer, every modal composition state, narrow, wide, and resized frame comparisons.
- [ ] 7.8 Port every pinned modal-like surface discovered from `InteractiveMode`, nested settings components, and public extension UI, including settings/theme/thinking/images; model and scoped-model configuration; trust; fork/user-message; tree/summarization/custom instructions; session/resume/rename/missing-cwd; login auth-type/provider/API-key/OAuth dialogs and logout; command inputs/confirmations; extension select/confirm/input/editor/custom-editor/custom-overlay; and any additional source-discovered route. Replace the generic settings callback selector with the source-traced `SettingsSelectorComponent`, current values, descriptions, search, instructions, nested selectors, change effects, and scrolling. Compose specialized selectors through pinned root-container placement, editor replacement, viewport allocation, transcript relationship, instruction rows, footer relationship, silent close/cancel, focus restoration, and resize behavior rather than generic overlays. Closing a selector SHALL leave transcript, workflow, notification, and status rows unchanged unless that pinned operation explicitly reports cancellation. Scoped models SHALL remain open while Enter applies session-only toggles, expose dirty/unsaved and refresh states, support all bulk/provider/reorder actions, persist only on `Ctrl+S`, and close silently without implicit persistence. Pass route-specific independent populated-transcript open, active interaction, save/confirm, navigation, search, nested replacement, silent cancellation, failure, session-switch, disposal, focus-restoration, scrolling, footer, viewport, and resize parity tests for every inventory entry.
- [ ] 7.9 Port the complete pinned built-in command and input controllers while preserving dispatch order, side effects, hidden routes, prompt-template/skill/extension fallthrough, bash inclusion rules, editor-history insertion points, silent selector cancellation, clipboard, queue behavior, and shutdown. Record a visible-route source trace for every built-in command and replace generic text or selector substitutions, including the one-shot `#showWorkflowSelector()` path wherever pinned Pi owns a stateful modal controller; specifically port `/hotkeys` as the complete keybinding-derived heading and styled Markdown tables with pinned transcript insertion, categories, colors, spacing, wrapping, and scrolling; port `/changelog` as the pinned bordered `What's New` plus settings-aware Markdown composition; preserve `/export` failure context and pinned error placement/style; and route `/reload` through dim coalescing status without an AddOne-only checkmark. Pass exhaustive independent success, cancellation, failure, route-frame, history, and outcome conformance.
- [ ] 7.10 Port ordinary prompt orchestration from editor submission through visible user turn, editor-history insertion, public session prompt execution, ordered streaming assistant/tool updates, turn settlement, abort, and recoverable failures. Preserve every finalized user, assistant, thinking, and tool component after completion, including settlement events with missing or replacement message collections. Pass synthetic edge cases plus a real SDK-backed prompt proving visible submission, current-session Up/Down history, streaming, completion, retained content, footer usage updates, and continued editor interaction.
- [ ] 7.11 Port queued messages, steering/follow-up behavior, retries, compaction, transcript rebuilding, session switches, terminal progress, diagnostics, malformed events, and error recovery. Follow pinned session-authoritative rebuilding and component lifetime when settlement message arrays are absent, partial, or replaced; prohibit rebuilding a completed transcript as an empty approximation. Pass independent high-rate ordering, state replacement, missing-message settlement, cancellation, compaction, session-switch, and failure-isolation outcomes and frames.
- [ ] 7.12 Bind the complete pinned public extension lifecycle through the AddOne-owned extension UI bridge, including registration, session switch, cancellation, disposal, and failure isolation; pass callback-coverage and no-private-context architecture tests.
- [ ] 7.13 Port extension widgets, status/footer contributions, working messages, terminal input hooks, and focus/invalidation behavior; pass independent rendering, input-consumption, replacement, cancellation, and cleanup parity tests.
- [ ] 7.14 Port and exhaustively inventory extension custom editors and inputs, selectors, confirmations, dialogs, notifications, custom overlays, nested/replacement behavior, and overlay/focus lifecycle; pass route-specific independent open, active update, resolve, cancel, throw, replacement, session-switch, disposal, resize, and restoration parity tests.
- [ ] 7.15 Port extension custom message and tool renderers, partial-result updates, detail expansion, malformed-output handling, and renderer failure isolation; pass streaming, replacement, error, fallback, and cleanup parity tests.
- [ ] 7.16 Remove superseded approximate shell controllers, shallow active-render reconstruction, duplicate command paths, the generic one-shot modal/workflow controller and every specialized-surface substitution, obsolete `a1 ui` routing, and invalidated parity fixtures only after their ported replacements pass focused and containing suites; retain historical evidence with explicit invalidation metadata.
- [ ] 7.17 Prove exhaustive source-to-port coverage with zero unmapped interactive modules, zero unapproved deviations, complete attribution, and tests linked for every ledger entry. Record the user-directed selection/copy and three-row wheel behavior as an approved deviation from pinned fullscreen `TuiAltScreen`, including the exact source paths, local ownership seam, rationale, and focused acceptance cases. Prove zero unmapped modal branches and zero specialized controllers represented only by generic selector tests; preserve the machine-readable report and intentional-failure fixtures.
- [ ] 7.18 Run the completed `npm run test:pi-terminal-parity` independent two-process gate plus focused two-producer built-in and extension component/composition parity at fixed widths and states, preserving raw ANSI outputs and identical source payloads. Include consecutive Markdown lists, mixed thinking/text/tools, startup resource sections and ordering, every inventoried modal in open/active/save-or-confirm/cancel/failure/restore states, scoped-model session-only toggles and `Ctrl+S` persistence while remaining open, silent selector cancellation with restored editor/focus and unchanged rows, current-session Up/Down prompt history and draft restoration, character/word/line/arbitrary-area bright-white selection over every declared content style, final emulated cells across differential writes, retained selection after release, selection clearing before non-copy input, the select-then-`/` autocomplete transition without inherited highlighting, selected/unselected `Ctrl+C`, absence of copy flashes, `/changelog`, `/export` failure, `/reload` status, three-row physical-scroll destinations, and fail on any row, style, color, spacing, wrapping, focus, editor, selector, dialog, status, footer, renderer, error, scroll, or resize divergence outside named terminal-only tolerances and the recorded user-directed fullscreen interaction deviation.
- [ ] 7.19 Run independent exhaustive command, keybinding, input, prompt-history, queue, tool, model, settings, session, authentication, clipboard, extension, and shutdown workflow parity, including every source-inventoried built-in and extension modal transition, scoped-model dirty/session/persisted state, silent modal cancellation, pinned history insertion/navigation boundaries, selection invalidation before ordinary input, route-specific changelog composition, export-error context/style/placement, and reload-status style/coalescing; preserve both producers' raw outcomes and prove the harness fails when a path or transition is intentionally removed.
- [ ] 7.20 Run real public-SDK-backed prompt and extension integration with streaming text, thinking, tool calls/results, retries, errors, every extension modal/input/editor/overlay lifecycle, and settlement; prove the owned UI remains interactive and restores focus after success, cancellation, and failure.
- [ ] 7.21 Run terminal lifecycle parity for startup, suspend/resume where supported, physical mouse selection and wheel sequences, selected and unselected `Ctrl+C`, active/primary/nested scroll views, three-row wheel distance, overscroll boundaries, scrollbar state, resize storms, hardware cursor, progress, uniform bright-white character/word/line/area selection-safe output across split differential writes and embedded ANSI as resolved final terminal cells, clearing before non-copy input and UI replacement so old coordinates never highlight new content, graceful shutdown, abnormal failure, and terminal restoration; prove selection has no copy flash or source-color leakage, equivalent wheel events expose the user-approved three-row destination, pass focused Windows checks, and preserve deferred platform-certification status.
- [ ] 7.22 Correct every remaining source, workflow, frame, extension, prompt, or lifecycle divergence; rerun focused and containing gates and record exact commit, package versions, exclusions, tolerances, raw artifact hashes, and machine-readable evidence. Repeat the user-controlled comparisons that exposed focused-editor color, placeholder footer usage, disappearing completed turns, generic settings selectors, abbreviated `/hotkeys`, extra assistant/list spacing, slower wheel scrolling, missing startup Skills/Prompts/resources, incorrect selector/modal composition, AddOne-only `{modal name} cancelled` rows, one-shot/non-stateful modal substitutions, `/scoped-models` closing on toggle instead of retaining session-only dirty state and saving only through `Ctrl+S`, omitted vanilla modal branches, missing Up/Down current-session prompt history, AddOne-only `Copied!` flashes, non-white or syntax-colored character/word/line/area selection, stale retained selections highlighting newly opened command/autocomplete content, automatic copy-on-release, one-row rather than three-row wheel movement, raw/unbordered `/changelog` Markdown, incorrectly prefixed or positioned command errors, missing route context such as `Failed to export session:`, and AddOne-only checkmarks or styling on dim statuses such as `/reload`. Keep each finding open until the corresponding source trace, identical-payload or physical-input independent raw-ANSI evidence, containing suite, and fresh manual comparison pass.

## 8. Accept the 1:1 Default Owned UI

- [ ] 8.1 Run complete automated gates after the source port: build, typecheck, tests, architecture, source coverage, SDK/runtime/component conformance, the full `npm run test:pi-terminal-parity` two-process terminal gate, independent built-in and extension frame/workflow parity, real prompt/extension integration, lifecycle, packaging, audit, and strict OpenSpec validation; preserve machine-readable evidence and reject all invalidated approximation-era parity records.
- [ ] 8.2 Run user-controlled manual smoke acceptance from bare `a1` against `a1 pi`: startup visuals, prompt and streaming, tools, editor and queued input, commands, abort/retry/compaction, model and thinking controls, sessions, settings, clipboard, selection, extension UI, resize, errors, and shutdown; record exact commit, package, terminal, and extension identities.
- [ ] 8.3 Correct every manual finding, reopen every contradicted task, and rerun focused, containing, source-coverage, real-integration, and parity gates; mark the base accepted only when all built-in and visible extension workflows pass.
- [ ] 8.4 Update CLI help, user documentation, development instructions, and recovery guidance to state that bare `a1` launches the owned UI, `a1 pi` is the untouched upstream fallback/oracle, sandbox is unchanged, and `a1 ui` is removed; pass documentation and CLI-help consistency checks.
- [ ] 8.5 Merge the accepted slice through `develop` and publish a unique `-dev.N` package under npm `next` with the owned UI as the bare default, AddOne-specific visual customization and structured tabs still disabled, no stable support claim, `latest` unchanged, and registry/package integrity verified.

## 9. Handoff to Multi-Agent Workspace

- [ ] 9.1 Record evidence that the parity-safe AddOne shell contracts are composable for future structured tabs without initializing the terminal host or creating PTYs.
- [ ] 9.2 Update the existing multi-agent milestone evidence to identify this change as satisfying its owned-UI prerequisite only after all source-port, extension, parity, and base gates pass; do not begin structured tabs or section 6 of that milestone before this acceptance.
