## 1. Establish the settings application contract

- [x] 1.1 Extend the agent-settings contract, validation, snapshots, and change outcome with stored/effective values, `live | next-session | next-start | current-exit` timing, internal capability availability, and UI visibility; verify focused contract and settings-section tests reject incomplete or contradictory descriptors and filter unavailable entries without option-specific reason copy
- [x] 1.2 Add an exhaustive reviewed effect registry for every key in generated Pi settings metadata, including owner, timing, and capability predicate; verify an added, removed, duplicated, or unmapped generated key fails the inventory test by name
- [x] 1.3 Implement the settings coordinator's validate, apply, persist, flush, rollback, bind-owner, and unbind-owner behavior; verify focused tests cover live success, deferred success, unavailable settings, effect failure, flush failure, rollback, rollback failure diagnostics, and disposal
- [x] 1.4 Route `PiSettingsIntegration` reads and writes through the coordinator and expose truthful effective state; verify storage-only setters can no longer make a descriptor writable without an effect or lifecycle handler
- [x] 1.5 Update the owned settings session and app to display applied and deferred outcomes with effective values while filtering unavailable entries; verify changing a live, next-start, and current-exit fixture produces the specified screen state and an unavailable fixture produces no row or explanatory copy
- [x] 1.6 Delegate the pinned settings selector callbacks to the same coordinator rather than a second key-to-callback path; verify owned and pinned routes produce the same setting mutation and owner effect for a representative key in every owner category

## 2. Resolve project trust before engine activation

- [x] 2.1 Split launch into global-settings/trust preflight and project-backed engine activation so no project settings or resource loader is constructed before an effective decision; verify order-sensitive tests fail if any project source is read before trust resolution
- [x] 2.2 Resolve saved path decisions and `defaultProjectTrust` with pinned precedence for trusted, untrusted, and ask defaults; verify focused fixtures cover parent/child path decisions, an undecided path, and a changed default applying on the next start
- [x] 2.3 Add the bounded pre-session trust surface for undecided interactive launches and a fail-closed non-interactive path; verify accept, reject, cancel, error, and unavailable-interaction cases load no project resource before completion
- [x] 2.4 Bind the existing `/trust` workflow to the same persisted decision authority for future launches; verify changing trust in-session cannot retroactively claim that already-loaded resources were protected and the next launch honors the saved decision

## 3. Apply active agent and provider settings

- [x] 3.1 Bind `autoCompact`, `autoResizeImages`, and `blockImages` to the active session and verify subsequent compaction checks, prompt images, tool images, and provider context observe live changes
- [x] 3.2 Bind `steeringMode`, `followUpMode`, and `transport` to the active agent and verify subsequent queued prompts and provider requests use new values without session recreation
- [x] 3.3 Bind `thinkingLevel` to the active session and persisted fresh-session default; verify capability clamping, footer/editor indicators, current-session requests, and a later fresh session agree
- [x] 3.4 Rebuild skill command registration and autocomplete when `enableSkillCommands` changes; verify commands appear or disappear live while unrelated extension and prompt commands retain identity and order
- [x] 3.5 Apply `httpIdleTimeoutMs` to both provider streaming and Pi HTTP dispatcher configuration, including zero's disabled semantics; verify focused timeout-option and sequential-launch tests detect an immediate-timeout regression or leaked prior-profile value
- [x] 3.6 Bind structured `warnings` and `showCacheMissNotices` at their decision points; verify each warning flag changes only its declared warning and cache-miss notices toggle without rendered-string filtering

## 4. Apply owned-shell and terminal settings

- [x] 4.1 Replace fixed editor geometry with live `editorPaddingX` and `autocompleteMaxVisible`; verify active editor reflow, autocomplete clipping, focus, cursor, history, and narrow-terminal behavior after each change
- [x] 4.2 Replace fixed presenter spacing with live `outputPad` and a relevant presentation cache revision; verify existing and future status, error, Markdown, tool, and transcript rows re-render without losing block identity or viewport state
- [x] 4.3 Bind `hideThinkingBlock` and `mermaidRenderingMode` to transcript construction and Markdown transformation; verify existing finalized blocks and streaming blocks update for every Mermaid mode while selection, expansion, and semantic order remain stable
- [x] 4.4 Bind `showHardwareCursor`, `clearOnShrink`, and `showTerminalProgress` to the active TUI/terminal lifecycle; verify terminal-operation tests cover enable, disable, resize, active-agent transitions, failure, and unconditional disposal cleanup
- [x] 4.5 Preserve live `doubleEscapeAction` and `treeFilterMode` through the coordinator; verify their next input action uses the changed value and both settings have behavioral rather than persistence-only coverage
- [x] 4.6 Hide `theme`, `quietStartup`, `tuiMode`, and `fullscreenScrollbar` from bare A1 and remove their product-specific unavailability strings while retaining exhaustive internal inventory and pinned comparison behavior; verify no product-fixed key or explanatory row appears in bare A1

## 5. Preserve and present transcript images

- [x] 5.1 Add bounded transcript image-reference metadata and an owned image-asset resolver port without raising payload/view limits; verify contract validation accepts references, rejects raw oversized base64 payloads and malformed metadata, and keeps large valid attachments out of serialized views
- [x] 5.2 Project user-message and tool-result image content into session-scoped opaque asset references backed by authoritative messages; verify references survive streaming/finalization, deduplicate without copying payloads, and are pruned on message removal, session replacement, and disposal
- [x] 5.3 Render resolved images with pinned image components when Kitty or iTerm2 capability is advertised, honoring live `showImages`, `imageWidthCells`, and available width; verify deterministic component tests cover both source roles, resize, hide/show, width changes, and existing-block reconstruction
- [x] 5.4 Render safe informative placeholders when images are hidden, unsupported, missing, or malformed while keeping `showImages` visible as a setting with a supported fallback rather than an unavailable explanation; verify a Windows Terminal capability fixture reports `images: null`, emits no image control sequence, and never silently drops the attachment

## 6. Honor startup, installation, and fullscreen-exit settings

- [x] 6.1 Bind `collapseChangelog` to the next owned startup changelog composition with pinned acknowledged/new-version behavior; verify collapsed and expanded startup fixtures plus `/changelog` remain distinct and correctly rendered
- [x] 6.2 Bind `enableInstallTelemetry` only to the applicable Pi install/update lifecycle, preserving opt-out and payload scope; verify disabled sends nothing, enabled emits only pinned events, and compositions without that lifecycle omit the setting and lifecycle-reason copy
- [x] 6.3 Capture the final textual transcript and resume metadata before fullscreen disposal while excluding overlays, drafts, animations, hidden thinking, and inline-image payloads; verify deterministic snapshots cover empty, populated, failed, and image-containing sessions
- [x] 6.4 Restore the alternate screen before emitting `fullscreenExitOutput`, printing transcript plus hint for `transcript` and only the hint for `resume-hint`; verify terminal byte-order tests cover normal exit, failure exit, progress cleanup, no duplicate rows, and parent-terminal usability
- [x] 6.5 Add the narrow A1 session-selection launch form needed by the formatted resume command and centralize product-aware formatting; verify the printed command resumes the exact persisted session and handles non-default session directories and paths requiring quoting

## 7. Prove complete behavioral conformance

- [x] 7.1 Add a table-driven conformance suite for the complete generated Pi inventory that asserts persistence, timing, internal availability, UI visibility, owner mutation, observable effect, rollback, and environment constraints for every key; verify a storage-only fake effect and a rendered unavailable row both fail the suite
- [x] 7.2 Add containing shell scenarios that change presentation settings during streaming, while detached, with a selector open, and after resize; verify transcript caches stay bounded and focus, scroll, selection, prompt anchors, queued work, and component identity remain intact
- [x] 7.3 Add security and lifecycle integration cases for startup trust, dispatcher isolation, telemetry opt-out, and exit-output ordering; verify no synthetic success can substitute for an owner call or terminal operation
- [x] 7.4 Update Pi source provenance and compatibility ledgers for every minimally ported setting behavior and public adapter use; verify architecture governance rejects private imports, reflection, prototype mutation, and unrecorded copied source
- [x] 7.5 Run strict OpenSpec validation and the required GitHub development validation for the corrected implementation pull request, preserving bounded failure artifacts for any setting whose effect or visibility diverges

## 8. Deliver and accept separately

- [x] 8.1 After this specification pull request merges and the user explicitly requests implementation, fetch the resulting `origin/develop` and create the fresh detached implementation worktree at `{working-dir}/.worktrees/make-pi-settings-effective`, where `{working-dir}` is the session's initial working directory (for working directory `D:/Git/a1`, use `D:/Git/a1/.worktrees/make-pi-settings-effective`, never `D:/Git/a1-make-pi-settings-effective`), with a new branch, commit history, and code pull request citing `make-pi-settings-effective`
- [x] 8.2 Leave the code pull request open with auto-merge disabled after CI and provide exact candidate build/run instructions for bare A1 and `a1 pi`; verify the pull-request diff contains no specification implementation accidentally stacked on this branch
- [x] 8.3 After this revised specification pull request merges and the user explicitly requests implementation, fetch the resulting `origin/develop` and create a fresh detached correction worktree at `{working-dir}/.worktrees/hide-unavailable-settings`, with a new branch, commit history, and code pull request citing `make-pi-settings-effective`
- [x] 8.4 Leave the correction code pull request open with auto-merge disabled after CI and provide exact candidate build/run instructions that exercise unavailable-option filtering in bare A1 while preserving pinned comparison behavior
- [x] 8.5 Record the physical-terminal review that accepted unavailable-option filtering but rejected final change acceptance because settings-controlled A1 frames, trust preflight, fullscreen transcript output, and resume presentation diverged from pinned Pi
- [x] 8.6 Merge the bounded visibility correction only as an intermediate candidate after its required CI passed, while keeping `make-pi-settings-effective` open and unaccepted for the visual-parity work below

## 9. Close complete setting-controlled visual parity

- [x] 9.1 Extend the exhaustive setting conformance authority so every generated key declares `none`, transcript, editor/menu, selector, startup, footer/status, terminal/cursor, restored-parent output, hidden, or other reviewed visual evidence; verify an added key, missing class, hidden key with a frame, or visible key without a pinned source fails by setting name
- [x] 9.2 Replace text-only and self-referential parity baselines with independent pinned Pi `0.84.2` and A1 producers that preserve semantic SGR roles, reset boundaries, borders, padding, row geometry, cursor operations, clearing, restoration, and post-stop ordering; verify stripping ANSI or substituting an A1-generated golden fixture makes the parity suite fail
- [x] 9.3 Port reviewed settings row and control semantics to A1 and Agent sections, including row/value colors, selected state, numeric controls, scalar menus, structured dialogs, deferred and failure notices, wrapping, and narrow-terminal clipping; verify equivalent retained states match raw-style and geometry captures while owned search, status, description visibility, grouping, and hidden entries remain declared differences
- [x] 9.4 Complete setting-driven transcript, queue, Markdown, thinking, image, warning, cache, compaction, retry, provider-error, changelog, command-autocomplete, and selector visual parity for every applicable value and live transition; verify existing and streaming blocks re-render without losing semantic identity, focus, selection, scroll, or queued work
- [x] 9.5 Replace the line-oriented project-trust prompt with a bounded pinned-style startup selector constructed from global settings and reviewed built-in primitives only; verify accept, reject, cancel, interrupt, invalid input, unavailable interaction, and failure clear and restore the terminal in pinned order while no project source is touched before resolution
- [x] 9.6 Replace ANSI-stripping fullscreen transcript capture with a bounded styled transcript renderer and adopt pinned resume wording, dim prefix, compact session id, optional quoted `--session-dir`, and product command substitution; verify empty, populated, hidden-thinking, Markdown, tool, image, failed, default-directory, custom-directory, transcript, and resume-hint-only exits restore the terminal before exact output and never print a raw default session path
- [x] 9.7 Complete editor/footer geometry, cursor, clear-on-shrink, progress, resize, scrollbar reservation, alternate-screen, parent-terminal, selection/copy, and supported/unsupported image parity at representative narrow and wide dimensions; verify terminal-operation captures contain no duplicate rows, stale progress, leaked mouse mode, misplaced cursor, or post-exit input corruption
- [x] 9.8 Update UI reference provenance, pinned source ledgers, parity tolerances, and the manual owned-UI checkpoint for every minimally ported style and lifecycle rule; verify governance rejects undocumented copied source, private imports, broad ANSI stripping, undeclared substitutions, and missing visual evidence
- [x] 9.9 After this revised specification pull request merges and the user explicitly requests implementation, fetch the resulting `origin/develop` and create a fresh detached implementation worktree at `{working-dir}/.worktrees/pi-settings-visual-parity`, with a new branch, commit history, and code pull request citing `make-pi-settings-effective`
- [ ] 9.10 After the owned-settings UX specification revision merges and the user explicitly requests implementation, fetch the resulting `origin/develop` and create a fresh detached implementation worktree at `{working-dir}/.worktrees/restore-settings-owned-ux`, with a new branch, commit history, and code pull request that does not stack on an unrelated implementation branch
- [ ] 9.11 Restore the owned settings composition without reverting shared components: open search only on `/`, render it with the shared ruled line-input and `search settings` placeholder, derive the standing status bar from active shortcut declarations, suppress selected-entry description rows while retaining metadata, and preserve current rows, menus, dialogs, persistence, effects, wrapping, and clipping
- [ ] 9.12 Remove the settings-list wheel literal and resolve its movement from the current effective `scrollbarSpeed`, including an accepted live value pending source reflection, through the shared scrollbar distance policy; verify normal, fast, and high values change settings-list wheel movement immediately and remain aligned with transcript scrolling
- [ ] 9.13 Add deterministic regression evidence for closed/open search input, ordinary printable input, full and clipped shortcut hints, absent description rows, transient save/error status, every scroll-speed value, and shared-component non-regression; update visual provenance so the owned interaction is a declared product difference rather than false pinned-frame parity
- [ ] 9.14 Run strict OpenSpec validation and required GitHub development validation, leave the correction code pull request open with auto-merge disabled, and provide one exact candidate build/run command that exercises the restored settings UX without presenting baseline-only diagnostics as acceptance
- [ ] 9.15 Perform user-controlled physical-terminal acceptance for the corrected settings UX plus the complete setting matrix, startup notices, project trust, transcript, editor/footer, cursor, progress, shrink, fullscreen exit modes, compact resume command, supported-terminal inline images, Windows Terminal fallback, selection/copy, and terminal restoration; record exact A1, Pi, OS, terminal, dimensions, settings values, and explicit merge authorization before integration and archival
