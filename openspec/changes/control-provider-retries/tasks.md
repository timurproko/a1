## 1. Qualify the upstream retry capability

- [ ] 1.1 Add an isolated package-root probe for retry-setting reads/writes, nested-field preservation, validation/defaulting, lifecycle event payloads, and exhausted-turn continuation; verify pinned 0.87.1 records the expected missing-capability outcome without patching or importing private files.
- [ ] 1.2 Obtain and qualify an immutable published Pi family whose declarations and shipped runtime pass the probe for scalar presentation metadata, exact text/image/tool context continuation, and stale refusal; record exact version and registry integrity, or stop with dependency adoption explicitly blocked if no candidate passes.
- [ ] 1.3 Update the exact Pi package family and lockfile only after qualification; verify the ordinary public-API, settings, extension, TUI module-identity, packaging, startup, and session-lifecycle compatibility gates pass without adopting unrelated presentation changes.

## 2. Integrate the bounded retry settings

- [ ] 2.1 Regenerate Pi settings metadata from the qualified package and map `autoRetry` and `retryMaxRetries` into the settings bridge/effect inventory; verify generated labels, order, boolean/number types, 1–10 bounds, defaults, Agent ownership, and live application have no drift.
- [ ] 2.2 Bind the active agent owner and transactional persistence for both fields; verify live eligibility changes, unchanged scheduled delays, flush/rollback behavior, invalid stored-value fallback without eager rewrite, and preservation of every sibling retry and unrelated Pi field.
- [ ] 2.3 Extend owned-settings coverage for defaults, disabled-with-retained-limit state, pointer/keyboard edits, invalid writes, restart persistence, engine unavailability, and comparison-profile isolation; verify the entries appear exactly once in the existing Agent section and never enter A1-owned settings storage.

## 3. Present structured retry progress and exhaustion

- [ ] 3.1 Carry current retry, effective limit, outcome, and bounded final error through the session-event boundary keyed to session/run generation; verify success, cancellation, stale events, and provider-level retries cannot create false agent-level state.
- [ ] 3.2 Render `Retrying (current/limit)` through the existing transient work surface and decorate the final assistant error with total requests plus automatic retries; verify transcript rebuild/settlement preserves one failure, the original diagnostic remains bounded, and non-retryable or disabled cases remain unchanged.
- [ ] 3.3 Add deterministic provider-stream coverage at limits 1, 3, and 10 for success before exhaustion, cancellation, exhaustion, exact total-attempt arithmetic, unchanged backoff/cap, and no eleventh automatic retry; verify tests use declared outcomes rather than live provider retries.

## 4. Add explicit finite continuation

- [ ] 4.1 Route the neutral retry command through the qualified session-scoped exhaustion-continuation operation and generation guard; verify admission atomically consumes only the current eligibility, starts one fresh finite budget, and refuses stale/new-session/new-prompt state without replaying a prompt.
- [ ] 4.2 Declare the bare-A1 keep-retrying shortcut with default `Ctrl+R`, derive its failure hint from the effective binding, and connect it without reading or clearing the editor; verify shortcut-conflict checks pass, an existing draft remains byte-identical, and comparison mode installs no A1 handler.
- [ ] 4.3 Add text, image, and completed-tool continuation coverage plus repeated exhaustion, success, admission failure, prompt replacement, and session replacement; verify the user message/images occur once, completed tools are not replayed, every exhausted budget needs a new action, and no automatic loop starts.

## 5. Certification and handoff

- [ ] 5.1 Integrate retry controls and continuation into exact-package/settings behavioral conformance; verify generated presentation authority, reviewed effect evidence, raw semantic styling/geometry, public-only imports, and known-broken-candidate rejection.
- [ ] 5.2 Run focused typechecking, retry/settings/session/shell tests, build, architecture checks, and the selected compatibility gates; record implementation evidence, exact candidate identity, the approximately 362-second ten-retry bound, and every remaining gap before finalization.
- [ ] 5.3 Build first and provide a color-preserving bare-A1 physical-terminal handoff using a controlled failing provider path; verify limits 1 and 10, Escape cancellation, exhaustion wording/hint, preserved draft/context, explicit continuation, restart persistence, and comparison ownership, then record user acceptance or the concrete gap.
