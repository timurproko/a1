## 1. Baseline and Ownership Inventory

- [ ] 1.1 Inventory every retained test, current suite owner, platform/runtime target, recurring `openspec/changes/**` evidence dependency, and exact-artifact consumer; verify a machine-readable report accounts for every retained test exactly where intended.
- [ ] 1.2 Record existing ordinary, conservative, and complete-validation timing/evidence from identified historical runs; verify failed and successful observations remain separate and no new speedup claim is inferred.
- [ ] 1.3 Define the bounded PR-core contract and initial coarse ownership groups; verify review fixtures cover UI/rendering, launch/startup, release/package/update, Pi, native containment, image/history, governance, shared inputs, and unknown paths.

## 2. Proportionate Selection

- [ ] 2.1 Add an explicit PR-core validation scope without redefining `fast` or `full`; verify plan tests keep the public complete compositions unchanged and prevent unrelated tests entering the mandatory core without declared ownership.
- [ ] 2.2 Replace whole-repository dependency-graph authority with bounded coarse path, test, shared-support, and invalidator ownership; verify additions, deletions, copies, renames, changed tests, and multi-owner support select the expected scopes.
- [ ] 2.3 Preserve fail-closed classification for unknown paths, unavailable history, malformed policy, selector/workflow changes, and manual dispatch; verify each case selects complete applicable coverage or blocks rather than producing an empty result.
- [ ] 2.4 Update job resolution and selection evidence for PR core plus optional owners; verify every selected target appears once and every trustworthy exclusion has a bounded reason.

## 3. Durable Regression Tests

- [ ] 3.1 Classify every test that reads active/archived change evidence, implementation reports, benchmarks, or workflow prose; verify the audit maps each assertion to current behavior, hermetic evidence-tooling coverage, or one-time finalization evidence.
- [ ] 3.2 Replace the integration-selection replay and package-download-cache historical-report assertions with hermetic generator/behavior fixtures; verify selection and cache contracts still cover success, tampering, bounds, and failure cases without repository-history paths.
- [ ] 3.3 Replace exact explanatory-prose assertions in terminal launch guidance with stable semantic or executable checks; verify `scripts/dev`, inherited terminal streams, and the prohibition on direct interactive Node handoff remain enforced.
- [ ] 3.4 Remove only assertions proven historical-only and retain accepted evidence files, current product tests, release contracts, and complete-suite ownership; verify the repository audit reports no permanent test dependent on a mutable active-change location.

## 4. Same-Head Failed-Job Reruns

- [ ] 4.1 Make modular artifacts and envelopes attempt-qualified while preserving run/head/selection/job/target authority; verify two attempts cannot overwrite or ambiguously merge one another's evidence.
- [ ] 4.2 Implement attempt-aware aggregate resolution; verify same-attempt success, prior-attempt reuse for a job not rerun, current-attempt failure precedence, stale head/run/selection rejection, duplicate authority, cancellation, and missing evidence.
- [ ] 4.3 Update Development workflow dependencies and artifact download rules so GitHub failed-jobs-only reruns rerun the failed job and aggregate without rerunning successful peers; verify workflow policy fixtures model both failed-only and all-job reruns.
- [ ] 4.4 Keep retries explicit and non-semantic; verify no workflow or tier automatically retries an assertion or performance failure and aggregate summaries identify reused attempt numbers.

## 5. Proportionate Evidence and Workflow Topology

- [ ] 5.1 Simplify checkout-bound PR-core outcomes to head/selection/scope authority while retaining build/package receipts for every exact-artifact consumer; verify package, startup, update, compatibility, preview, and release gates still reject stale or tampered bytes.
- [ ] 5.2 Schedule the PR core and selected integration owners independently without unconditional complete-fast partitions; verify docs-only skips product work, ordinary fixtures select only owned scopes, and CI-authority fixtures select complete development coverage.
- [ ] 5.3 Preserve complete retained coverage in conservative Development, Full regression, nightly, preview, and stable release compositions; verify an ownership ledger accounts for every retained test and supported platform/runtime with no accidental same-target duplicate.
- [ ] 5.4 Preserve the single stable protected-branch aggregate and all fail-closed states; verify selected failures, cancellations, stale results, unexpected skips, and contradictory exact-artifact evidence block integration.

## 6. Documentation and Implementation Evidence

- [ ] 6.1 Update `docs/validation.md` with PR-core ownership, complete-cadence boundaries, failed-job rerun semantics, attempt evidence, exact-artifact receipt boundaries, and rollback; verify documented commands and artifact names match the implemented workflow.
- [ ] 6.2 Run focused selector, tier, aggregate, workflow-policy, durable-oracle, receipt, and ownership tests plus typechecking; record exact commands and outcomes without running local `test:fast`, `test:full`, or `test:release` absent separate authorization.
- [ ] 6.3 Record deterministic ordinary-path selection replay, exact-head PR-core timing, conservative current-head validation, and applicable complete-regression evidence; report the three-to-five-minute PR-core target honestly and preserve every failed attempt or explicit known gap.
- [ ] 6.4 Prepare one to three implementation-specific acceptance scenarios covering ordinary selective feedback, same-head failed-job rerun, and preserved complete fail-closed coverage; verify the final PR list is concise, behavioral, and suitable for the version-3 acceptance manifest.
