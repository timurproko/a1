## 1. Baseline and Ownership Inventory

- [x] 1.1 Inventory every retained test, current suite owner, platform/runtime target, recurring `openspec/changes/**` evidence dependency, and exact-artifact consumer; verify a machine-readable report accounts for every retained test exactly where intended.
- [x] 1.2 Record existing ordinary, conservative, and complete-validation timing/evidence from identified historical runs; verify failed and successful observations remain separate and no new speedup claim is inferred.
- [x] 1.3 Define the bounded PR-core contract and initial coarse ownership groups; verify review fixtures cover UI/rendering, launch/startup, release/package/update, Pi, native containment, image/history, governance, shared inputs, and unknown paths.

## 2. Proportionate Selection

- [x] 2.1 Add an explicit PR-core validation scope without redefining `fast` or `full`; verify plan tests keep the public complete compositions unchanged and prevent unrelated tests entering the mandatory core without declared ownership.
- [x] 2.2 Replace whole-repository dependency-graph authority with bounded coarse path, test, shared-support, and invalidator ownership; verify additions, deletions, copies, renames, changed tests, and multi-owner support select the expected scopes.
- [x] 2.3 Preserve fail-closed classification for unknown paths, unavailable history, malformed policy, selector/workflow changes, and manual dispatch; verify each case selects complete applicable coverage or blocks rather than producing an empty result.
- [x] 2.4 Update job resolution and selection evidence for PR core plus optional owners; verify every selected target appears once and every trustworthy exclusion has a bounded reason.

## 3. Durable Regression Tests

- [x] 3.1 Classify every test that reads active/archived change evidence, implementation reports, benchmarks, or workflow prose; verify the audit maps each assertion to current behavior, hermetic evidence-tooling coverage, or one-time finalization evidence.
- [x] 3.2 Replace the integration-selection replay and package-download-cache historical-report assertions with hermetic generator/behavior fixtures; verify selection and cache contracts still cover success, tampering, bounds, and failure cases without repository-history paths.
- [x] 3.3 Replace exact explanatory-prose assertions in terminal launch guidance with stable semantic or executable checks; verify `scripts/dev`, inherited terminal streams, and the prohibition on direct interactive Node handoff remain enforced.
- [x] 3.4 Remove only assertions proven historical-only and retain accepted evidence files, current product tests, release contracts, and complete-suite ownership; verify the repository audit reports no permanent test dependent on a mutable active-change location.

## 4. Same-Head Failed-Job Reruns

- [x] 4.1 Make modular artifacts and envelopes attempt-qualified while preserving run/head/selection/job/target authority; verify two attempts cannot overwrite or ambiguously merge one another's evidence.
- [x] 4.2 Implement attempt-aware aggregate resolution; verify same-attempt success, prior-attempt reuse for a job not rerun, current-attempt failure precedence, stale head/run/selection rejection, duplicate authority, cancellation, and missing evidence.
- [x] 4.3 Update Development workflow dependencies and artifact download rules so GitHub failed-jobs-only reruns rerun the failed job and aggregate without rerunning successful peers; verify workflow policy fixtures model both failed-only and all-job reruns.
- [x] 4.4 Keep retries explicit and non-semantic; verify no workflow or tier automatically retries an assertion or performance failure and aggregate summaries identify reused attempt numbers.

## 5. Proportionate Evidence and Workflow Topology

- [x] 5.1 Simplify checkout-bound PR-core outcomes to head/selection/scope authority while retaining build/package receipts for every exact-artifact consumer; verify package, startup, update, compatibility, preview, and release gates still reject stale or tampered bytes.
- [x] 5.2 Schedule the PR core and selected integration owners independently without unconditional complete-fast partitions; verify docs-only skips product work, ordinary fixtures select only owned scopes, and CI-authority fixtures select complete development coverage.
- [x] 5.3 Preserve complete retained coverage in conservative Development, Full regression, nightly, preview, and stable release compositions; verify an ownership ledger accounts for every retained test and supported platform/runtime with no accidental same-target duplicate.
- [x] 5.4 Preserve the single stable protected-branch aggregate and all fail-closed states; verify selected failures, cancellations, stale results, unexpected skips, and contradictory exact-artifact evidence block integration.

## 6. Documentation and Implementation Evidence

- [x] 6.1 Update `docs/validation.md` with PR-core ownership, complete-cadence boundaries, failed-job rerun semantics, attempt evidence, exact-artifact receipt boundaries, and rollback; verify documented commands and artifact names match the implemented workflow.
- [x] 6.2 Run focused selector, tier, aggregate, workflow-policy, durable-oracle, receipt, and ownership tests plus typechecking; record exact commands and outcomes without running local `test:fast`, `test:full`, or `test:release` absent separate authorization.
- [x] 6.3 Record deterministic ordinary-path selection replay and the exact post-finalization evidence plan for PR-core timing and current-head Development validation; verify acceptance does not require a separately dispatched Full regression, still requires an honest three-to-five-minute target report, and preserves every failed attempt or explicit known gap.
- [x] 6.4 Prepare one to three implementation-specific acceptance scenarios covering ordinary selective feedback, same-head failed-job rerun, and preserved complete fail-closed coverage; verify the final PR list is concise, behavioral, and suitable for the version-3 acceptance manifest.
- [x] 6.5 Prevent draft-event workflow runs from emitting the protected aggregate identity; verify a skipped draft run cannot make the merge button appear satisfied and only a non-draft exact-head aggregate can satisfy branch protection.
- [x] 6.6 Partition an exact selected test-file list and remove consumed selection payloads from child environments before they reach platform process limits; verify conservative Windows selection executes every selected test exactly once, spawned test children do not inherit orchestration payloads, and any partition failure fails the logical PR-core outcome.
- [x] 6.7 Synchronize the cold durable-history fixture on its first structured worker snapshot without increasing timeouts or adding retries; verify isolated and resource-sensitive execution observes the committed recalled prompt.
- [x] 6.8 Upgrade the hermetic changed-documentation fixture to the current validation-impact schema and computed selection identity; verify changed-mode success, violations, invalid paths, full mode, unsupported modes, and missing selection behavior remain covered without weakening parser validation.
