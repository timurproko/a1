/**
 * Compatibility export. Owned application route lifecycle belongs to the
 * vendor-neutral UI app owner; the Pi shell only hosts that contract.
 */
export type { UiRouteHost, UiRouteSurface } from "../../../ui/apps/index.js";
